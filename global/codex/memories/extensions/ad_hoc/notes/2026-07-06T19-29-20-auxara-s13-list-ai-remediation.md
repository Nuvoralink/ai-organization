Auxara Sprint 1.3 remediation update (2026-07-06): list ownership must remain exactly-one-owner but is operationally movable, not creation-only. `PATCH /api/lists/:id/owner` supports individual -> team and team -> individual when the caller has server-side `lists.manage` object-scope RBAC over source and target; regular agents cannot delegate team lists. Reassignment preserves the same list/prospect/call history and stops any active shared run from accepting new claims; active calls/wrap-up drain before the run finalizes. Legacy null/null lists are readable cleanup artifacts until repaired by a broad manager; `podId:null` is never personal ownership.

Same remediation tightened AI disposition evidence: webhook ENDED may enqueue the draft job, but the worker now requires usable transcript text before any provider call. Answered call events are lifecycle evidence only, not semantic grounding. No transcript text means no draft/no metered AI call/manual wrap-up. Update future docs/tests against this stricter boundary; do not reintroduce event-log fallback for disposition meaning.

User-standing workflow reinforced: small frontend additions can be implemented without a fresh mock only when they use already approved/built primitives and do not require human visual judgment. Larger visual placements remain mock-gated.
