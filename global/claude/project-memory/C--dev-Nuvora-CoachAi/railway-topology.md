---
name: railway-topology
description: "Railway projects/services for the two CoachAI prod stacks (internal + Auxara), IDs, and queue model"
metadata: 
  node_type: memory
  type: reference
  originSessionId: d72a7c28-760e-4762-8f27-6c7b1798dadb
---

Railway workspace **"nuvoralink's Projects"** (`1b286e96-3a48-4171-8159-2f5d4895f216`) hosts the two CoachAI production stacks. Both use a single `production` environment.

- **internal** = project **`Nuvora-Coach-Internal`** (`2a8d0d08-2dbc-4339-b4aa-29f4133ee696`), env `production` (`4f8a11b0-375f-46f6-900c-c3336b9e8f5f`). **1 service: `API`** (`c50b57c7-fab0-4e05-80a2-62799a618fee`). In-memory queue (`QUEUE_DRIVER=memory`), no Redis/Worker. This is the INTERNAL tenant stack.
- **auxara** = project **`Auxara`** (`ff08bd47-77e3-4fc9-a0a7-8e8ebdd455b6`), env `production` (`3560db20-a695-44d7-bbee-dd12d4f56b94`). **3 services: `API`** (`b8eb556a-1927-4942-a26f-df83e4a84fdc`), **`Worker`** (`88ef1552-49df-4ce0-84c4-5363f5fc91a8`, `node dist/worker.js`), **`Redis`** (`4677539f-f5f9-42dd-8b3c-1b441cec5152`). BullMQ queue — Worker consumes, Redis backs it. This is the paid CUSTOMER stack (has Stripe + R2 + presigned uploads; internal has none of those). Worker pulls most secrets via `${{API-service.KEY}}` references.

**Pricing-gate CUTOVER — DONE 2026-06-08 (session d72a7c28).** `PRICING_ANALYSIS_COUNT_GATE_ENABLED=true` is now **LIVE on BOTH Auxara services** — `API` (`b8eb556a`) + `Worker` (`88ef1552`) — set via `railway-agent` then a manual redeploy (deploys API `48b6f8ea` / Worker `05a5eedf`, commit `4dee9c0e`, both SUCCESS; `/api/health` + `/api/ready` → 200, `stack:paid`, db/queue/ratelimit/storage ok). So the count gate + metered PAYG ($2.00/overage analysis, price `coachai_payg_overage_analysis_v2`) are ON for the paid customer stack. **Both** Auxara services need the flag: the API enforces the intake gate + serves the usage meter + runs the overage-reporter loop; the Worker runs `runAnalysis`, which is where the OverageMeterEvent producer lives. The **Internal** stack was left UNSET on purpose — it's inert there: `isBillingEnabled()` = `getAppStack()==='paid'`, and INTERNAL-tenant users are unmetered, so flipping internal would do nothing. No live paid customers yet → nothing is charged; the system is live + ready for the first signup. (So: do NOT treat the gate as "still off" — the migration is shipped end-to-end.)

**Public API hosts (for live `/api/...` verification):** paid API = **`api.auxara.io`** (frontend `coach.auxara.io`); internal API = **`coach-api.nuvoralink.com`** (frontend `coach.nuvoralink.com`). `GET /api/health` returns `{status:ok, stack:paid|internal}`. NB `api.nuvoralink.com` is a DIFFERENT/old Express service (not the internal CoachAI API). Railway auto-deploys on push to `main` (build → `migrate:deploy` with the `[wait-for-db]` wake-guard → start → healthcheck); a superseded commit shows `SKIPPED`.

**Auxara infra cleanup + card-free trial — 2026-06-11 (session d72a7c28).** Card-free trial shipped (commit `22c24e5b`, both Auxara services SUCCESS): signup no longer forces Stripe Checkout (the trial was always card-free server-side), customer lands in-app; global TrialBanner countdown; on lapse a `TrialExpiredScreen` subscribes in one click; Account gains 1-click "Add payment" + an "end trial now" opt-out (`POST /api/stripe/end-trial`). A **fail-closed `TRIAL_EXPIRED` escape-hatch allowlist** in `authMiddleware` (`isTrialExpiredEscapeHatch`) lets an expired-trial customer still reach identity + billing endpoints to SUBSCRIBE while product routes stay 403 — otherwise they'd be trapped with no way to pay. **Infra:** the API's persistent **`/data` volume was DELETED** (vestigial — `STORAGE_DRIVER=r2`, recordings live in R2; it only held transient upload staging, which the Worker already does with no volume), and **`UPLOAD_DIR` repointed `/data/uploads` → `/tmp/uploads`** (ephemeral; the storage driver `mkdir`s it on boot). Dead **`ORG_MEMBERSHIP_ENABLED` removed from the Worker** (zero code refs; org membership is always-on via `requireOrgMembership()`). Both services redeployed + verified `/api/ready` 200 `storage:r2`. **So the API has NO volume now — don't re-add one;** uploads stage on ephemeral disk like the Worker. Open follow-up **TRIAL-1** (backlog): subscribing AFTER a trial lapses may grant a 2nd Stripe trial (checkout always sets `trial_end`) — more reachable now signup is card-free.

Two other projects in the workspace are unrelated to the backend audit: `nuvora-dialer`, `Nuvora-Link`.

Access via the **railway MCP** (`mcp__railway__*`): `authenticate` (browser OAuth) → then `list-projects`/`list-services`/`get-status`; env-var read/write goes through **`railway-agent`** (no direct variable tool). Variable values are `<hidden_from_agent>` even to the agent. Deleting a var stages a removal (shows as `:null`) that flushes on next deploy. Codex parity gap: Railway MCP noted as not yet in Claude Code at user scope — see [[ux-ui-redesign-audit]] context. The app reads ~170 `process.env.*` vars; `backend/.env.example` only documents 16 (not the authoritative set).
