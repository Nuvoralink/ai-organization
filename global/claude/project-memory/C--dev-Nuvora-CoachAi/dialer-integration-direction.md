---
name: dialer-integration-direction
description: Dialer call-recording auto-ingest = the
metadata: 
  node_type: memory
  type: project
  originSessionId: 52b1b4b2-6011-4052-beed-1a549adb1a9b
---

Dialer call-recording auto-ingest — fix for the signup→never-uploaded activation cliff (see [[gtm-readiness-threshold]]). **Architecture LOCKED + phased plan WRITTEN 2026-06-15:** `docs/app-plan/implementation/dialer-auto-ingest-plan.md`.

- **Auth model: per-tenant BYO credential** (NOT OAuth marketplace — that's the locked dead-end; Amin waited 4+ months for a Dialpad dev account). Provider reality, VERIFIED against live API docs 2026-06-15:
  - **MightyCall** = literal BYO **API Key + Secret** (OAuth2 `client_credentials` → 24h bearer; `api.mightycall.com/v4/auth/token`). `GET /v4/calls` returns `callRecord.uri` (downloadable audio). Agent identity = `extension`. Low onboarding friction. Sidesteps review entirely. ✅
  - **RingCentral** = BYO **JWT credential** (customer mints it in THEIR account, restricts to Nuvora's ONE public app by Client ID) — NOT a literal API key. **CORRECTION to the old "sidesteps review entirely" note:** RC still needs a ONE-TIME Nuvora public-app graduation (a few live API calls, then 1–3 *business-day* staff review). That is NOT the Dialpad dead-end and NOT a marketplace listing or per-customer review — one-time for Nuvora, fast, deterministic. Spirit of BYO holds; "no review at all" was wrong for RC. Start graduation EARLY (lead time).
  - Store credentials encrypted at rest (AES-256-GCM) — **no encrypted-secret-at-rest pattern exists in the repo today; it's net-new.** Never log; tenant-scoped. **A future planner must NOT re-propose OAuth marketplace integrations.**
- **Build order: MightyCall first (Ladan's dialer), then RingCentral.** Kixie/Dialpad/JustCall later via the same adapter interface.
- **Architecture (LOCKED):** ONE ingestion authority `ingestCall()` = the realization of the pre-existing-but-never-built `backend/docs/auto-ingest-api.md` contract. External pushers (n8n/Telnyx) hit `POST /api/v1/auto-ingest/calls` (team key + HMAC); native adapters (MightyCall/RingCentral) are IN-PROCESS feeders that call `ingestCall()` directly with the tenant's BYO credential. Both converge → `createUploadIntent → finalizeUploadIntent → scheduleAnalysis` (EXISTING pipeline, unchanged). Idempotency on `(org, provider, externalCallId)` + `Session.recordingFingerprint` backstop. Agent→rep mapping is DETERMINISTIC + fail-closed (extension/email/explicit table) — NOT an AI decision. **Paid-stack/Worker only** (poll jobs need BullMQ; internal stack has no Worker/Redis).
- **Sequencing — RESOLVED 2026-06-15:** Amin chose **validate-first**: build the seam + push API → validate the activation lift with an **n8n** bridge (he picked n8n over Zapier — self-hosted, no per-task pricing, generic HTTP node speaks MightyCall + native RC node; doubles as the done-for-you lever against Ladan's account) → THEN native MightyCall (poll-first) → RingCentral. The seam is reused by every path (not throwaway). Slices: DAI-000 (seam) · DAI-000b (n8n validation) · DAI-001 (MightyCall) · DAI-002 (RingCentral, +DAI-002a graduation in parallel).
- **Open product/UX calls (need mockups before coding):** agent-mapping UX (rec: auto-map by email + review list); MVP latency (rec: poll-first ~5–15min ok for v1, webhook later); confirm RC graduation starts now.

- **Progress 2026-06-15 — DAI-000 + DAI-001 MERGED + DEPLOYED + healthy.** DAI-000 (seam: `ingestCall`+`secretBox`+schema, PR #82) and DAI-001 (native MightyCall adapter + poll job wired into `worker.ts` as the `coachai.dialer-poll` repeatable cron + `/api/dialer` manager connect/test/map endpoints, **PR #85** — #84 was auto-CLOSED when #82's `--delete-branch` removed the stacked base branch; recovered by opening #85 = `feat/dai-001` → main) are merged to main + auto-deployed: Auxara API+Worker + Internal API all **SUCCESS**, `/api/health`+`/api/ready` 200 (db/queue/storage ok → migration applied to both prod DBs). `CREDENTIAL_ENCRYPTION_KEY` set on Auxara API+Worker (SAME value, required) + local `.env`. **Feature is LIVE but DORMANT** (no dialer connected; no connect-UI yet → poll finds 0 connections). Verified: tsc, doc-code-drift (1381), `mightyCallAdapterRegression` 10/10, `dialerPollRegression` 11/11 (Neon branch), `tenant-security-blackbox` green (43 probes/27 families). MightyCall JSON envelopes parsed DEFENSIVELY (token field, calls list, callRecord.uri auth — `[LV1-3]` markers in `mightyCallAdapter.ts`) — UNVERIFIED against a live account. **NEXT (the real activation test): connect a real MightyCall account (Ladan's) → confirms the [LV1-3] live parsing end-to-end; then DAI-002 (RingCentral).** Follow-ups: FU-1 (dialer-specific tenant-security golden probe + the Connect-MightyCall UI is mockup-gated), FU-2 (paid-stack-gate `/api/dialer` so internal doesn't 500). **Stacked-PR lesson: merging a base PR with `--delete-branch` auto-CLOSES the dependent child PR — retarget the child to `main` BEFORE merging the parent, or merge the parent without `--delete-branch`.** (DAI-000 was built by a background `implementer` that STALLED on verification; orchestrator recovered by copying to main + verifying + path-limited commit.)

- **Connect-UI mockup BUILT + iterated 2026-06-15 (Figma `Xv0RS1wr9j6t7F1VerIIo0`) — awaiting Amin approval** before coding DAI-001-UI. 4 frames: Integrations index · Connect (empty/onboarding: API key+secret, where-to-find, AES-256 + read-only pills) · Connected (sync/health LIVENESS + a held-calls ALERT strip + Gong-style agent→rep mapping, held-not-dropped) · Held queue (the per-call triage, in the MANAGER CALLS VIEW, not settings). **Pressure-test win (Amin):** a per-call "recent activity" feed does NOT belong on the connection/settings page (category error — duplicates the calls list, doesn't scale to 1000s of calls); settings shows aggregate health + a held COUNT/alert, the actual held calls live as a filter + nav-badge in the calls view. Copy states the EXPECTATION not the mechanism ("new calls appear within ~15 min") — we POLL every 10 min (`DIALER_POLL_CRON_PATTERN`, `worker.ts:214`), webhooks are deferred to DAI-003, so never surface "polling" in the UI. Additive backend the surface implies (NOT in DAI-001 — fold into DAI-001-UI): a "Sync now" trigger, a last-call-received / connection-health stat, and a "held" call state surfaced as a calls-view filter + nav-badge count.
- **Post-launch quality follow-ups DOCUMENTED 2026-06-15 (Amin's request — work AFTER integration ships; in the plan doc Phased Slices):** **DAI-004** voicemail / non-conversation detection (every recording auto-flows → don't burn paid analysis or pollute the coaching feed on voicemails; AI-owned meaning judgment at the **analyzability layer**, short-circuit BEFORE the heavy stages; path-independent → also covers manual uploads). **DAI-005** pre-material connect state (org connects before uploading materials → coaching is generic/`craft_excellence`-only; make provenance honest + guided upload + reprocess early calls once materials land; **block-vs-banner = Open Question 4**, rec = banner + reprocess, NOT hard-block).

- **DAI-001b BUILT + verified 2026-06-16 (worktree `feat/dai-001b` off origin/main, NOT yet merged — merge is human-gated).** The ROSTER/allowlist model is implemented: the poll resolves the agent against `DialerAgentMapping` (the allowlist) via the shared `resolveRep` BEFORE `fetchRecording` — an UNMAPPED agent's recording is NEVER downloaded (zero PII/bandwidth for the 8-of-10 agents you don't coach; answers "why download calls we can't place"). New metadata-only `DialerObservedAgent` discovery model powers the roster UI (`callCount` = INGESTED calls, 0 until mapped — not sightings). SSRF/DoS hardening on the recording fetch (manual redirect + per-hop host re-validate + private-IP block + AbortSignal timeout + streamed size cap). Built by a background `implementer` that DIED on verification (produced all files, never reported) → orchestrator took over, reviewed the diff, ran adversarial + security auditors. **Adversarial caught a P1:** the `recordingFingerprint` content-dedup was ORG-WIDE — two reps' byte-identical calls (shared/conference recording, identical voicemail/IVR) would silently collapse into one Session and deny the 2nd rep coaching → FIXED to per-rep scope (matches `uploadFinalizationAuthority`), with a cross-rep regression that bites. Also fixed: callCount sightings→ingest-count; credential sent only to the original redirect host; lag default 15→30. **Verified GREEN: tsc + mightyCallAdapter 16/16 + dialerIngestCall 34/34 + dialerPoll 27/27** (local disposable Postgres). Diff: 11 files, +741, + the `DialerObservedAgent` migration. Flagged follow-ups (deferred): no `Organization.archivedAt` → subscription-lapse DISABLE-on-archive hook (poll-side CUSTOMER-tenant re-check ships instead); robust recording-render re-check beyond the lag window. LESSON: a background agent that must self-verify can DIE/stall after producing files — detect via worktree mtime + git status, recover by taking over the files; and the upload path's per-user dedup scope is the authority to match, not org-wide.

- **DAI-001c MERGED 2026-06-16 (9f26cffa, PR #95) — MightyCall has TWO account types.** Classic (`api.mightycall.com`) vs Call Center (`ccapi.mightycall.com`) differ ONLY in host + what `client_secret` is. We'd built Classic-only → a Call Center AGENT account (like Ladan) would 401 on the wrong host. Fix: added `platform: 'classic'|'call_center'` to `MightyCallCredential`; renamed `secretKey`→`clientSecret` (it IS the OAuth2 client_secret = the **User Key**, or a Classic **extension** — the User Key already had a home, just mislabeled "secret", so NO new field was needed); host picked by platform (MIGHTYCALL_API_BASE env still overrides); token cache keyed `platform:apiKey`. Verified tsc + secretBox 22/22 + adapter 18/18 (platform→host) + poll 27/27 + ingest 34/34. Auth docs: support.mightycall.com/authentication-via-api (Call Center) + /authentication-via-api-mc (Classic). **Only Managers/Admins** can enable Integrations→API + toggle a per-user User Key (you see only your OWN) → getting an agent's creds needs their admin (Amin contacting Ladan's admin). **OPEN — live test resolves: does API Key + a specific User Key return that user's calls only, or the whole account? → agent-self-connect vs account-roster. **Amin 2026-06-16 sharpened this: Call Center admins enter/toggle a User Key PER-USER — there is no "list all account users", so we likely CANNOT auto-detect agents. The whole "detect agents → pick who to coach" roster model rests on whether ONE User Key returns account-wide calls (→ roster) or just that user's (→ per-rep: each rep = their own User Key + mapping, no detection). Do NOT finalize the agent-mapping/roster UX or build DAI-001-UI's mapping section until the live test confirms scoping. The connect form (platform + API Key + User Key) + status/sync-health are model-agnostic and safe to build; the mapping section is NOT. A config page stays lean — no discovery banners.** Connect-form platform selector + "User Key" / "User Key or Extension" labeling lands with DAI-001-UI (the mock).

- **DAI-001-UI MERGED 2026-06-16 (711f3652, PR #99) — the Connect-MightyCall manager page is LIVE.** Manager/owner-gated `/integrations/dialer`: account-type (Classic/Call Center) + API Key + User Key (MASKED, write-only) → connect/test/disconnect + minimal agent→rep mapping. HELD (pending live test): detection/roster, Sync-now, rich sync-health stats. Built by a frontend implementer; reviewed by **adversarial + security (CLEAN) + source-to-screen** (which earned its keep — fixed: a missing DISABLED status that white-screened, unmasked secret inputs, "Last synced" mislabeled as `updatedAt` (poller-bumped) → now `lastSyncAt`, a `take:25`-capped rep count presented as a total → dropped, hardcoded provider label → derived) + a 6/6 `DialerIntegrationPage.test.tsx`. **CADENCE LESSON (Amin had to remind me): run the security-auditor on EVERY auth/secret/route surface + the domain auditors per the rule — not just adversarial. A whole-surface dialer security + tenant-isolation sweep (the full subsystem, the tenant-security-blackbox + a dialer golden probe) is DUE before the first live connection.** A customer's admin can self-serve enter their OWN keys (give them manager/owner access; credential is never readable back) — so Amin needn't hold Ladan's raw keys. **NEXT = the live test:** Ladan's admin enters API Key + User Key → resolves the OPEN account-wide-vs-per-user scoping question, which decides the roster vs per-rep-connection model. (GitHub Actions side-note: Nuvora's CI is already correct — `pull_request` + `push:main`, no double-trigger; the Actions-minutes drain was the Auxara repo.)

- **SCOPING — CONFIRMED LIVE 2026-06-22 (PR #100); was Amin's 2026-06-16 hypothesis, now FACT — the crux of the whole feature's direction:** Call Center = **PER-USER** (a User Key returns ONLY that user's calls → the key effectively IS the rep; no agent detection possible) ; Classic = **ACCOUNT-WIDE** (API Key + extension → the whole account's calls → match by extension → roster works). **→ the `platform` selector is the MODEL fork, not just a host switch.** Consequence: **Classic = what's SHIPPED** (one connection/org + agent→rep map-by-extension). **Call Center per-rep needs a BACKEND change** — multiple credentials per org, each User Key → one rep — because `@@unique([orgId, provider])` allows only ONE connection/org today (this is the already-flagged "multi-connection per org" follow-up; now we know WHY). Call Center works for ONE rep with what's shipped (one key → one user → map them); MULTI-rep Call Center is the gap. Ladan's account is Call Center → her live test confirms the per-user half (poll once → only her calls = confirmed). The connect UI will BRANCH by platform once confirmed (Classic = map-by-extension roster; Call Center = add-a-rep-by-User-Key). **Do NOT build the Call Center per-rep path until the live test confirms.**

- **LIVE TEST 2026-06-22 (Ladan's real Call Center keys in gitignored `backend/.env` as `MIGHTYCALL_ORG_API_KEY`=API Key/36-char GUID + `LADAN_USER_ID`=User Key/12-char) — FULLY RESOLVED + FIXED + MERGED (PR #100, on main):** ✅ **AUTH WORKS** against Call Center `ccapi.mightycall.com/v4/auth/token` (HTTP 200, bearer `{access_token,token_type,expires_in,refresh_token}` → `[LV1]`=`access_token` CONFIRMED). **DAI-001c host + credential model VALIDATED live.** ✅ **Classic `/calls` spec confirmed VERBATIM** from MightyCall dev docs (developer.mightycall.com `llms.txt` + `/reference/calls_list.md`): `GET api.mightycall.com/v4/calls`, params `pageSize/skip/startUtc/endUtc/callFilter/customFilter` (dates `yyyy-MM-ddThh:mm:ss`), envelope `{data:{calls,total},isSuccess}`, recording at **`callRecord.uri`** (direct audio URL) + `callRecord.fileName` → matches our adapter's `[LV2-3]` parsing. ✅ **SCOPE = PER-USER, CONFIRMED (hypothesis is now FACT):** Ladan's User Key returned **643 calls over 60d, ALL one extension (114)** → a Call Center User Key returns ONLY that user's calls. ✅ recording host = `console.mightycall.com` (passes the `*.mightycall.com` SSRF allowlist). **The blocker was Amin's catch: Call Center serves calls at `/v4/`*`api`*`/calls` — the `/api` segment our adapter was missing** (auth stays `/v4/auth/token`; the ccapi Redoc doc is auth-gated at `ccapi.mightycall.com/v4/api/...` — Amin downloaded the page). The live test caught **TWO real bugs that made Call Center ingest 0 calls on main**: (1) PATH missing `/api` → 404; (2) ENVELOPE — real shape is NESTED `{data:{calls:[...],total},isSuccess}` but `extractCallArray` treated `data` as the array → 0 calls even on HTTP 200 (the `[LV2]` 'defensive parse of an unverified envelope' guess was WRONG; a fake unit fixture masked it). **Both fixed + merged (PR #100, commits 38ab25fb/6d4022ec/2cd5ed77 on main):** `resourceBaseFor()` adds `/api` for call_center only; `extractCallArray` extracts `data.calls`; duration-as-string tolerated; regression fixtures switched to the REAL nested envelope + new asserts bite the `/api` path (both platforms) + the envelope shape. Verified: regression 20/20, backend tsc 0, adversarial-reviewer ACCEPT. **Two PRE-EXISTING latent-red CI gates surfaced + fixed in the same PR:** doc-code-drift (DialerIntegrationPage.tsx unclassified since DAI-001-UI #99) + the gates workflow's `prisma generate` failing on a missing `DATABASE_URL` (placeholder added — generate never connects). LESSONS: (a) a live API throttles a burst of discovery probes — read the OpenAPI/Redoc doc FIRST, probe sparingly; (b) "defensive parsing of an UNVERIFIED envelope" is theater — validate adapters against the REAL provider payload, not an invented fixture (the fake `{data:[...]}` fixture hid the real `{data:{calls:[...]}}` shape); (c) **the `gates` CI check is NON-REQUIRED (mergeStateStatus UNSTABLE = still mergeable) → red gates silently accumulate on main** (TWO were latent-red here) — RECOMMEND making `gates` a required status check in branch protection so a red gate blocks merge. **STILL OPEN — multi-rep Call Center:** `@@unique([orgId, provider])` allows ONE credential/org; each Call Center rep = their own User Key = a separate credential, so multi-rep Call Center needs the multi-credential-per-org change (ONE rep works on what's shipped today).

- **DAI-004 voicemail + short-call gate SHIPPED + MERGED 2026-06-22 (PR #101, `889aa48e` on main).** Stops auto-ingest from spending paid analysis on non-coachable calls. In `dialerPoll.ts`, AFTER the roster gate (mapped reps only) + BEFORE `fetchRecording`, a call is skipped (never downloaded/transcribed/coached) when: (1) provider `callStatus` is a voicemail (MightyCall `VoiceMail`, case-insensitive via `isVoicemailStatus` — a STRUCTURAL provider fact, NOT an AI meaning judgment, so deterministic is correct; the AI transcript backstop `lib/voicemailSkip.ts` is unchanged for the manual path + unflagged VMs), or (2) `durationSeconds < DialerConnection.minCallSeconds` (default 120, manager-editable via `PATCH /api/dialer/connections/:id/settings`, `0` disables the duration gate). **Scope (Amin): auto-ingest ONLY** — nobody manually uploads a voicemail. **Audit = NO UI** (Amin): `DialerCallAudit` is a metadata-only (no PII), idempotent (`@@unique(orgId,provider,externalCallId)`) backend decision log (`COACHED`/`SKIPPED_VOICEMAIL`/`SKIPPED_TOO_SHORT`) — the debug trail AND the skipped-call idempotency key (re-list no-op). Retention-bounded (`pruneOldDialerCallAudits`, 180d, both sweeps). **Verified:** backend+frontend tsc 0, `dialerPollRegression` 43/43 against a REAL Postgres (Docker) with the migration applied via `migrate deploy`, doc-code-drift/check:ui/script-imports 0, **4 auditors ALL ACCEPT/CLEAN** (adversarial + ai-decision-boundary + source-to-screen + security). **Plan-doc reconciliation:** the plan's "DAI-004" (analyzability-layer AI voicemail) was ALREADY the existing `voicemailSkip`; what shipped is the complementary dialer PRE-DOWNLOAD gate. **Follow-ups — BOTH DONE + MERGED 2026-06-22:** (1) PATCH route test `dialerSettingsRouteRegression` (16/16, real Express over HTTP — valid update persists+audits, bounds→400, cross-tenant→404 IDOR, no-CSRF→403, 0 accepted) + `policyAuditHardeningRegression` source-presence backstop → **PR #102**. (2) Manager UI — the "Call filtering" panel on the dialer page (editable "Minimum call length (seconds)" + Save, wired to `dialerApi.updateSettings` → the PATCH endpoint; approved mockup, Amin's copy edit applied = dropped the "usually quick hang-ups" line; 8/8 tests inc. the PATCH-payload + out-of-range-blocked cases) → **PR #105**. **PLUS the systemic CI gap closed (PR #104):** a Docker-Postgres `Backend DB regressions` workflow (postgres:16-alpine service container → migrate deploy → run the 3 DB-backed dialer regressions) — they SKIP without a DB so were CI no-ops before; the lane self-verified by failing twice on real env gaps (`GOOGLE_API_KEY` validated at import, `STORAGE_DRIVER=r2` demanding creds) then going green. **Reusable: GitHub Actions `services: postgres` is how to actually run the DB-backed `test:regression:*` in CI; provide the boot env (provider keys + app secrets) at the JOB level since some tests validate at import before their own env setup.** LESSON: a background `adversarial-reviewer` can DIE on an API ConnectionRefused (infra) and return NO verdict — fall back to an explicit manual adversarial self-check of the load-bearing risks (do the tests bite? value flow? validation parity? tokens? copy?) + the independent gates, don't merge blind and don't burn another 20min retry on a likely-recurring outage. **Disposable-DB technique (reusable for dialer regressions):** Docker Desktop start → `docker run postgres:16-alpine -p 55432:5432` → `DATABASE_URL=…localhost:55432 prisma migrate deploy` → `DIALER_INGEST_TEST_DATABASE_URL=… tsx scripts/dialerPollRegression.ts` (localhost ⇒ no confirm flag) → `docker rm -f`. CI does NOT run backend regressions, so this is the local proof path. **Parallel-agent note:** during this build the main checkout was switched by another session to `feat/coachplus-internal-haiku-default` (tier-policy work) — worktree isolation kept my build clean; synced local `main` via `git fetch origin main:main` (ref-only, no checkout switch) so I never disturbed their branch.

- **END-TO-END PROVEN + recording naming SHIPPED 2026-06-23 (PR #106, `ee3cc4f3` on main).** The full dialer→coaching loop ran on a REAL Ladan Call Center call against a disposable local Postgres: live list (375 calls w/ recordings) → **download from `console.mightycall.com` (8.4 MB `.wav`) → CLOSES `[LV3]`, the last unverified piece** → `ingestCall` → the REAL analysis pipeline IN-PROCESS (`fully_analyzable` 79) → **coaching generated** (`authoritative`, grounded/behavior-changing — it pinned the missed acknowledge-and-reframe at 1:01 that lost the close). **The whole chain works on real data — the #1 activation lever is proven.** Caveat: ran **`craft_excellence`-only — NO materials seeded**, so the `manager_accountability` lane (rep vs approved script) was NOT exercised (offered Amin a material-seeded re-run). **Recording naming (the merged code):** dialer sessions are now named **`<counterparty phone> · <UTC date+time>`** (outbound→`toNumber`, inbound→`fromNumber`, `Call · <stamp>` fallback) on the SEARCHABLE `Session.uploadOriginalName` (ILIKE) so a rep finds feedback by the number — the STORAGE filename (`originalFilename`) stays the safe machine name (no PII in object keys). Proven live as `+15308592297 · 2026-06-23 02:55`; biting `dialerIngestCallRegression` assert (outbound `toNumber`+`occurredAt` ⇒ exact name) 35/35; CI `dialer-db-regressions`+`gates` green. UTC stamp — localize to org/rep tz later if Amin wants. **Reusable e2e technique:** disposable Docker PG + `dotenv` the main `.env` for REAL provider+MightyCall keys + **HARD-override `DATABASE_URL` to `localhost` with a guard** (never touch dev/prod) + `QUEUE_DRIVER=memory` runs the real analysis IN-PROCESS → poll for the **Feedback row** as the completion signal (NOT a status regex — `"analyzing"` contains `"analyz"`, which broke an early version of the wait). **STILL OPEN — multi-rep Call Center** (`@@unique([orgId,provider])` → one credential/org; each Call Center rep = own User Key) unchanged from PR #100; ONE rep works on what's shipped.

- **MULTI-REP CALL CENTER + PAUSE + SELF-CONNECT — DONE 2026-06-23 (PR #107; supersedes the "STILL OPEN — multi-rep" notes above).** **Model:** `DialerConnection.repUserId` (nullable) — set ⟺ PER-REP Call Center (a User Key returns only that rep's calls, so the connection IS the rep → the poll attributes every call straight to repUserId, NO roster gate/mapping/DialerObservedAgent); NULL ⟺ account-wide Classic (the extension roster, unchanged). Unique `(orgId,provider)` → `(orgId,provider,repUserId)` PLUS a PARTIAL unique index `(orgId,provider) WHERE repUserId IS NULL` (migration 20260623000001) for single-account-ness — Postgres NULLs are unique-distinct so the compound unique can't cover the account row; **Prisma can't model a partial index → it's raw SQL, KEEP it if `migrate dev` proposes a DROP** (a regression test bites if it's lost). `ingestCall` gained a `repUserId` override that VERIFIES OrgMembership first. `pausedAt` (nullable) → the poll WHERE `status:ACTIVE,pausedAt:null` skips it (new calls stop, existing coaching untouched). Routes: connect `repUserId?`, `PATCH /:id/pause`, `POST /pause-all`. **SELF-CONNECT (Amin insisted — the PRIMARY case, not an edge):** a manager/owner connects their OWN User Key — `getScopedMemberIds` EXCLUDES the actor (an owner is PAID_OWNER, not in its PAID_MEMBER set), so the connect route allows `repUserId === actor.id` explicitly + the frontend offers "You — <name>" FIRST in both Call Center pickers. **LESSON: any "pick a member incl. yourself" surface must add `actor.id` — `getScopedMemberIds` is team-only.** **WEBHOOK (Amin asked): DEFERRED — poll (~10min) is enough;** MightyCall webhooks fire on CALL events not "recording-ready" (renders minutes later) → pure latency on an async product; `providerAccountRef` reserved for it. Pause v1 = manual (no schedule). **3 auditors ACCEPT; 2 should-fixes FIXED in-PR:** (1) a removed rep's per-rep connection was a POLLING ZOMBIE — the repUserId onDelete:Cascade only fires on a HARD User delete, but team-removal + self-delete KEEP the User row → fixed by deleting the connection in `revokeAndReconcileMembershipSeats` (the ONE member-removal choke point all 3 paths call); (2) single-account TOCTOU → the partial index + graceful P2002 re-find/update in the route. **Tests:** poll 55 / ingest 39 / route 31 (Docker PG) + frontend 17 + suite 327; all gates 0. Orchestrated build (Claude implementers for the frontend + self-connect; 3 auditors) off a worktree. **MERGED `27f2f375` (PR #107).** Feature is LIVE in code but DORMANT until a real Call Center account connects (the live-preview of the multi-rep view was offered but not run — needs a seeded multi-rep org; verified via the 17 page tests + 327 suite + the design gates instead).

- **FIRST-PARTY AUXARA DIALER INTEGRATION — cross-system architecture DESIGNED 2026-07-07 (PROPOSED, awaiting Amin's decisions):** `docs/app-plan/implementation/auxara-dialer-first-party-integration.md`. **The "no OAuth" lock is SCOPED: it forbids third-party marketplace supplication only — for our OWN dialer we ARE the platform, and OAuth is the chosen mechanism** (auth-code + PKCE, org-level admin-consented grant, opaque rotating tokens). Shape: the dialer (${PROJECT:auxara-dialer|backslash}) becomes OAuth AS + `/api/v1` integration API (serving its existing `CallDTO`, which already declares itself the CoachAI-handoff contract) + signed-webhook emitter (`call.recording.finalized` from an outbox in the `recordingFinalize` transaction); CoachAI adds an `AUXARA_DIALER` provider on the SAME `ingestCall()` seam (OAuth client flow, webhook receiver = the DAI-003 generic seam, roster auto-map by email + invite-unmatched). Push-primary (works on the INTERNAL stack in-process — Amin's PH team dogfoods first), poll-reconcile on paid. The dialer's ADR-AUTH-006 bearer seam + `api_integration` role + permission registry are the authz substrate (scopes map to existing permission keys; no parallel authz). Dual-channel recordings (`channelRoles A=agent/B=prospect`) ride the contract → F-1 fast-follow kills the mono-diarization weakness. **DECISIONS SETTLED + BUILD GREEN-LIT (Amin 2026-07-07):** SSO after data plane (popup slice in P4); backfill with CONFIGURABLE window (presets + custom start date), quota-previewed; auto-map immediately; dialer "View coaching" = in-place POPUP with silent scoped SSO (window.open, not iframe — 3P-cookie/frame-ancestors fragility) + narrow `coach_links.write` write-back when the review is ready; internal client branding "Nuvora Coach". NEW D-1 slice: recording-disclosure clip is TTS-GENERATED (pre-generate → R2 → disclosureAudioRef; text+voice+hash snapshot in ComplianceConfigChange — evidence-stable; NOT call-time TTS). Corrections: dialer Sprint 1.2 recording pipeline verified LANDED (README status line was stale); the dialer's Sprint 2.4 IS the reserved CoachAI-handoff sprint — this work realizes its integration half early (supersedes push-audio + hand-issued keys + transcript handoff + opt-out flag; beta half untouched). Rep-self-OAuth: NO for ingest (org grant only; solo rep = owner of own tenant so same flow); reps authenticate individually only at P4 viewing time. Build launched 2026-07-07: Codex P1 (dialer worktree ${WORKSPACE:dev|backslash}\wt-nuvo-int-p1, feat/int-p1-oauth-platform) + Codex P2 (CoachAI worktree .claude/worktrees/int-p2-auxara-connect, feat/int-p2-auxara-connect) in parallel; P3 data plane next; design PR #168 open awaiting Amin's merge; 4 mockup widgets presented for UI approval.

**BACKBONE FULLY SHIPPED 2026-07-07 (one long orchestrated session).** All 4 slices built by Codex + adversarially/security/compliance/source-to-screen reviewed + orchestrator-verified on disposable DBs + merged: **P1** dialer OAuth AS + bearer seam + /api/v1/org|users (auxara-dialer#200); **P2** CoachAI AUXARA_DIALER provider + OAuth connect + adapter + email auto-map (Nuvora-CoachAi#172); **P3a** dialer producer — /api/v1/calls + /recordings/{id}/download (reuses recordingStorageAuthority) + signed-webhook transactional outbox + delivery worker (auxara-dialer#201, migrations 0032+0033); **P3b** CoachAI consumer — POST /api/v1/webhooks/dialer/auxara receiver + DialerWebhookEvent dedupe + shared dialerIngestProcessor→ingestCall + reconcile/Sync-now + configurable backfill (Nuvora-CoachAi#173). Design doc current on main (#168 + #174). CoachAI paid stack + dialer both deploy-healthy; feature DORMANT (env-gated, no UI yet, no client connected). Webhook signature proven byte-identical cross-repo (hmac(secret,"<t>."+rawBody), header t=<t>,v1=<hex>). **Real bugs the reviews caught + fixed:** (source-to-screen) recoverable fail-closed states written to the terminal-idempotency DialerCallAudit → a call arriving before its rep is mapped was uncoachable-forever → fixed with a terminal-vs-recoverable audit split + recovery test; (cybersec) the durable outbox drain used a context-less prisma.tenant.findMany → 0 rows under NOBYPASSRLS → dead backstop → fixed with a SECURITY DEFINER fn + drain-entrypoint test; (cybersec+compliance) outbound SSRF via tenant-controlled webhook URL → egress guard; (source-to-screen) Auxara 'outbound' vs ingestCall's 'outgoing' → outbound calls named by rep's own number → adapter-boundary direction normalization; (adversarial) poll left as inline duplicate of the shared gate chain → migrated + a check:dialer-ingest-authority grep gate. **REMAINING (additive, not yet built): D-1** TTS recording disclosure (dialer, pre-generate clip→R2→disclosureAudioRef, snapshot text/voice/hash in ComplianceConfigChange); **P4** in-dialer "View coaching" popup + coach_links.write write-back + scoped OIDC prompt=none SSO; **the approved UI builds** (CoachAI connect card+mapping+backfill — backends merged so buildable now; dialer consent screen; dialer connected-apps; TTS settings; popup). **F-1 fast-follow flagged:** channelRoles/operatorType parsed-then-dropped at the adapter (NormalizedDialerCall has no field) → dual-channel transcription provenance decision needed (persist now vs accept no-history for P3-era calls). **Lessons captured** (design doc Implementation Lessons + this memory): rotating-credential refresh must re-source token from store under advisory lock; rebuild shared/dist before backend tsc after a rebase; the dialer's filemap gate makes committed report files a footgun (git rm + regenerate in one commit; briefs must tell Codex not to commit reports); Codex sandbox has no DB so orchestrator MUST run the DB regressions (caught test-only bugs in P2 route + P3b job-backfill that shipped green-but-wrong).
**PROD INCIDENT 2026-07-07 (my sequencing error, fixed):** P3a's F7 hardening made a BRAND-NEW env var `WEBHOOK_SECRET_ENCRYPTION_KEY` production-REQUIRED at boot (env.ts superRefine) in the SAME merge that introduced it — but it was never set on the dialer's Railway → both the dialer WORKER (explicit `WEBHOOK_SECRET_ENCRYPTION_KEY is required in production` crash on `tsx src/worker.ts`) AND the API (manifested as a Railway "Network > Healthcheck failure" — the boot-crashed process never binds the port, so the health probe times out; Build+Deploy pass, Healthcheck fails = a runtime boot crash, NOT a build issue) crash-looped after deploy. A boot-time env-validation `process.exit(1)` happens BEFORE Sentry.init, so it produces NO Sentry issue (absence of Sentry errors + a Railway crash = suspect boot/env). Fix: Amin set `WEBHOOK_SECRET_ENCRYPTION_KEY`=<a base64 32-byte value; same on api+worker since they seal/open the same webhook-subscription secrets> on both dialer Railway services → recovered. **RULE for D-1/P4 + any slice: NEVER make a new required env var/secret land in the same deploy that first requires it — set it on Railway (all services that boot the image) FIRST, then merge the code that requires it; add the var to the dialer's `docs/app-plan/engineering/22-environment-config-secrets.md` + srp-004-style ops ledger in the same slice.** Railway MCP (`railway` server) needs re-auth per session to query deploy status/logs; when unavailable, diagnose from the Railway UI screenshot + reason from the merged code + Sentry-absence.

**Why:** removing manual-upload friction is the likely fix for the activation cliff Ladan named; the BYO constraint is hard-won (4-month OAuth dead end); validate-first avoids weeks of native build before proving the lever.
**How to apply:** implement from the plan doc; start at DAI-000 seam; do not suggest OAuth-marketplace integrations **for third-party dialers** (first-party OAuth per the 2026-07-07 doc is the deliberate exception); remember RC ≠ literal API key (it's JWT + one-time graduation).
