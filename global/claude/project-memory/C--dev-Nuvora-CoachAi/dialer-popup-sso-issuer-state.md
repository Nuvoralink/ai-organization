---
name: dialer-popup-sso-issuer-state
description: "Auxara popup-SSO OIDC id_token issuer lives on the dialer's UNMERGED feat/p4-coaching-loop branch (not main), emits no email_verified; CoachAI consumer is dormant."
metadata: 
  node_type: memory
  type: project
  originSessionId: c430c2ef-2ef6-4670-844f-f1b9a65fc7dd
---

The popup-SSO OIDC flow (CoachAI `scope=openid email`, `prompt=none`, id_token `{sub,email,tenantId}`) is split across two repos and both halves are pre-enablement:

- **CoachAI (consumer)** — `backend/src/lib/dialer/auxaraOidc.ts` (`verifyAuxaraIdToken`) + `backend/src/routes/auxaraSso.ts` (`matchOrgMemberByEmail` is the ONLY `identity.email` consumer, the SSO_LINK_CONFIRM path). Built but **dormant** — no `AUXARA_DIALER_*` envs set in prod. Frontend does NOT enumerate `ssoError` codes (only `ProtectedRoute` starts SSO; `LoginPage` reads only `returnTo`), so new fallback codes have no visible-copy/mockup gate.
- **Dialer (issuer/OAuth AS)** — `${PROJECT:auxara-dialer}`. The id_token issuer **IS on `origin/main`** — P4 merged via **PR #224 (2026-07-11)** "feat(integrations): P4 dialer coaching loop". `backend/src/lib/oauth/oidc.ts` `signOidcIdToken` (HS256, iss `auxara-dialer`, aud=clientId, sub=userId) + `service.ts` openid branch (only issues for `user.status==='active'`, else `invalid_grant`, selects `{id,email}`). It signs `{email, tenantId}` — **no `email_verified` claim**. ⚠️ STALE-BASE TRAP (hit 2026-07-12): a `git fetch`-less LOCAL `main` checkout LAGS server merges, so it can look like the issuer "only lives on an unmerged `feat/p4-coaching-loop` branch" when that branch is actually a post-merge leftover — always `git fetch origin` and read `origin/main` before concluding dialer repo state (see [[git-ff-main-sync]]).

The frozen `docs/integration/coach-consumer-contract-v1.md` (dialer) has **no OIDC/id_token section** — its P4 "Coach links" is the `POST /api/v1/coach-links` writeback API, a different thing. So the popup-SSO issuer is ahead of the frozen consumer contract.

**INT-P4 F5** closes account-takeover vector F2: CoachAI hard-requires `email_verified===true` before trusting `identity.email` (fail closed → no link); the dialer must emit `email_verified` TRUTHFULLY. ⚠️ **The task's premise "active = invite-provisioned, login-proven ⟹ verified" is FALSE** — the dialer performs ZERO email verification (only path to `status:'active'` is public self-signup, email from request body, no inbox proof; `routes/auth.ts:215`; invite-accept path unbuilt). So the dialer emits **`email_verified: false`** (honest); the CoachAI email-link fallback stays fail-closed / INACTIVE until the dialer builds real email verification. (Deriving `email_verified` from `status==='active'` = a forgeable-verified-email vector — caught by the dialer cybersecurity-auditor BLOCK/Critical + compliance-auditor.)

**Status 2026-07-12 — BOTH halves DONE, unmerged, no PR:** CoachAI on `claude/zealous-saha-b893ff` (5 commits): `verifyAuxaraIdToken` returns `email: string|null`, withheld unless `claims.email_verified===true`; route fails closed. 29/29 unit + 49/49 route regression (disposable Neon) + full `verify` exit 0; adversarial-reviewer VERDICT DONE. Dialer on `claude/int-p4-f5-dialer-email-verified` off `origin/main` (1 commit `9e246d3`): `signOidcIdToken` emits `email_verified: false`; typecheck/eslint/gates:all/632 non-DB tests green; ran the dialer's OWN cybersecurity + compliance auditors (via general-purpose adopting their `.claude/agents/*.md` defs — they're not in a CoachAI session's registry). **Residual SEC-P4-1** (High Priority, `COACHING_BUG_BACKLOG.md`): the roster auto-map (`dialerRoster.ts`) creates `DialerAgentMapping` from the SAME unverified dialer emails → feeds the SSO `sub` path (3a) which mints a session with no email check — a second F2 door F5 doesn't close, now CONFIRMED real (not conditional). **DURABLE fix for the email-link + roster + a truthful `email_verified: true` = build real dialer email verification (emailVerifiedAt/confirm-email) — a separate product decision surfaced to Amin.** See [[coachai-worktree-setup]], [[dialer-integration-direction]], [[agent-fleet-setup]].
