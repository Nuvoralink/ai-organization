---
name: ux-ui-redesign-audit
description: "Ongoing UX/UI redesign audit of CoachAI — deliverable location, data source, driver, and the biggest finding."
metadata: 
  node_type: memory
  type: project
  originSessionId: 3259338e-3bb2-478f-9966-bdd9f9834515
---

UX/UI redesign of all CoachAI pages (started 2026-06-05). Goal: less info-dumping, accurate/proper charts, and a "restrained futuristic" 2026 look while keeping the rep/manager **Call Review** + **Improvement Plan** editorial style as the north star.

**Phase 2 (2026-07-03): information-design audit COMPLETE** — deliverable `docs/design-system/02-PAGE-DESIGN-AUDIT.md` (`PD-*` findings + phased plan). Headline verified facts: rep practice CTAs destination-less (Coach+ gating residue → 2-state CTA decision); "16 points to coach" = uncapped manager Call Review clusters; ~700-line unreachable manager OverviewTab body; paid Prompt-A editorial columns + `synthesis.rep.headline` never rendered; synthesized fake waveform; owner Business Health = 24 surfaces/16 tiles in 3 forms; Dialer repo holds the un-back-ported CoachAI-origin layout standard (`layout-and-ia.md` + 2 gates). Plan: Phase 0 truth fixes → Phase 1 structural substrate (Dialer transfers) → Phase 2 four page restructures (manager cockpit, Business Health, manager Call Review, rep This-week) → Phase 3 rep Call Review verdict layer + Account. **All 7 decisions SETTLED by Amin 2026-07-03** (doc §5): orphan table as recommended; rep objections row YES; render Prompt-A copy; drop lead-type conversion strip; bulk-approve shows lines; brand-connect line (no rebrand); delete notifications API. NEW finding PD-CR-12 (from Amin's report "it's not telling the booker when they're off script"): the off-script signal dies silently behind a 6-gate chain (script-match → audit ran → accepted → rep-lane → severity → dedup) with NO honest absent-state — directive: always-present script state in the verdict header + a Phase-0 DB probe for which gate eats it. Triage lens: [[audit-bugs-are-decision-residue]].

- **Audit deliverable:** `docs/design-system/00-UX-UI-AUDIT.md` (findings ledger, IDs G-/REP-/MGR-/PUB-/IA-/VIS-) + `docs/design-system/retrofit/data-inventory.md` (items-sent-to-UI + metrics ledger). VisualForge retrofit method (skill suite at `${DEPENDENCY:visualforge|backslash}`, not registered — read its SKILL.md directly).
- **Live audit setup:** local app (frontend `:5173`, backend `:3001`) — local backend already points at the **`Coach-Internal` Neon project (`snowy-glade-91475703`, ap-southeast-1)**, which holds the only real team with data ("Amin Sharifkhani's Team": ~8 reps, ~52 analyzed calls, mostly LEGACY/pre-score-authority). Read-only Playwright driver at `frontend/scripts/_uxAudit.mjs` (temp; reads creds from gitignored `scripts/local/manager-smoke.local.ps1` (manager=Andrea) + `scripts/local/audit.local.env` (rep=Audrey)).
- **Biggest finding (G-1):** real `Feedback.overallScore` values exist (16/17 of Audrey's calls, 43–67) but every surface hides them because legacy rows have `scoreAuthorityStatus=null` and the read model treats null-authority as "no score" → cascades to "0 eligible calls / upload 4+ / SCORE UNAVAILABLE / 0 calls analyzed." A read-model/migration bug, not absent data. Candidate for `COACHING_BUG_BACKLOG.md`.
- **Open decisions:** product brand name is inconsistent (UI says "Auxara Coach", company is **Nuvora**, scripts say "AIL") — must lock before visual redesign (PUB-1). Owner/`/admin` pass deferred (no admin login). Mobile pass pending (`AUDIT_VIEWPORT=mobile`).
- Reps have **no real dashboard** (REP-1) — user wants one built. Lesson: manager surfaces load via skeletons with no "Loading…" text — always re-capture before trusting any "0/blank" reading. See [[decision-framing-preference]].
