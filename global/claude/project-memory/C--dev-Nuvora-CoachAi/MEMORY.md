- [Decision framing preference](user_decision_framing.md) — Wants capability + cost framing, not fear/risk-driven thinking. Mention risks but don't let them gate the design.
- [Side project: LinkedIn outreach agent](project_linkedin_agent.md) — Building a self-hosted agentic LinkedIn outreach system, non-commercial, hobby/personal use.
- [UX/UI redesign audit](ux-ui-redesign-audit.md) — Ongoing all-pages UX/UI audit; deliverable at docs/design-system/, data in Coach-Internal Neon, G-1 score-authority bug, Auxara/Nuvora brand question.
- [Local frontend smoke setup](local-frontend-smoke-setup.md) — How to run the manager frontend smoke here (auto-lands on 4173 now, PR #165); local Redis points at prod (noisy); focused Call Review ~28s to mount in dev (TOOL-3/PERF-1); M5 needs rep creds.
- [Backend DB-regression sandbox](backend-db-regression-sandbox.md) — Raw pg/psql/`prisma migrate` time out in this sandbox (protocol handshake), but the npm `test:regression:*` scripts (Prisma pg-adapter) work against a warm disposable Neon branch. Warm the compute first, then migrate+run.
- [Analysis-failure observability](analysis-failure-observability.md) — Analysis-run failures now reach Sentry tagged `route:analysis.run` (system-only; upload/cancel/completed-race suppressed) via `shouldAlertAnalysisFailure` (PR #182); email-alert on that tag. Event-driven auto-investigate webhook DEFERRED at 0-traffic; claude.ai trigger API is research-preview + the research confabulated its specs.
- [Railway topology](railway-topology.md) — Prod stacks: internal=Nuvora-Coach-Internal (1 API svc, memory queue), auxara=Auxara (API+Worker+Redis, BullMQ); project/service IDs + railway MCP usage. **Pricing-gate CUTOVER DONE 2026-06-08: `PRICING_ANALYSIS_COUNT_GATE_ENABLED=true` is LIVE on both Auxara services (count gate + metered PAYG ON); internal left inert. Gate is NOT off anymore.** 2026-06-11: card-free trial shipped (22c24e5b); Auxara API `/data` volume DELETED (API has NO volume now — uploads on ephemeral `/tmp/uploads`, storage=R2); dead `ORG_MEMBERSHIP_ENABLED` removed from Worker.
- [FF main sync](git-ff-main-sync.md) — When FF-landing worktree work on main, also sync the LOCAL main ref; pushing HEAD:main alone leaves the local checkout (and new worktrees off it) stale.
- [Agent fleet setup](agent-fleet-setup.md) — Global orchestrator/implementer/auditor model (~/.claude/agents + ~/.claude/rules, mirrored to ~/.codex/AGENTS.md); CoachAI project fleet now 10 agents (2 domain auditors + drift/test-runner/ui/perf/release + functionality-parity + user-journey, latter two instantiated 2026-07-08 from the universal templates; NO shared endpoint registry → no mechanical wiring gate) + settings/PostToolUse gate hook; built 2026-06-14.
- [Coach+ seat audit findings](coachplus-seat-audit-findings.md) — 4-auditor pass found 2 Gate-4 misses (new SeatEntitlement authority not wired into member-delete lifecycle + not reusing manager-scope authZ); fixed; pattern + sweep targets for a follow-up orchestrator.
- [Quality bar: exceed the baseline](quality-bar-exceed-baseline.md) — Don't cap quality at the reference/incumbent output (gpt-5 is "mid"); judge against absolute elite + pursue beating it, not parity.
- [Haiku coaching eval verdict](haiku-coaching-eval-verdict.md) — Haiku 4.5 vs gpt-5 for Prompt A: WINS craft 11-1-4; the "29% fabrication" was INFLATED by transcript-only judging — material-aware re-judge → 7 real fabrications in 5 mechanical classes (appt-day, numbers-vs-script, invented stat, cross-call, wrong-lane) → VIABLE drop-in CONDITIONAL on a grounding guard. GROUND-1 guard layer (unwrap + laundering + appointment-day + numeric-duration) BUILT + verified + live-validated + MERGED to main (PR #80); the merge also unbroke the 57e5c34b split-commit CI failure (missing contract → Vercel tsc → app crashed) + evolved DCD-TEST-5 to the editorialHeadline-authority model. Base-model swap DONE (PR #81: Haiku IS the base) but INERT until `AI_TIER_POLICY_MODE=enforce` — prod still serves Sonnet. Broad re-validation + Haiku-vs-Sonnet DONE 2026-06-15: Sonnet beats Haiku 3-2 head-to-head → Haiku confirmed as base/Lite-only (with the guard), NOT the everyone-default.
- [OpenAI gpt-5 deprecation exposure](openai-model-deprecation-exposure.md) — Our OpenAI models + shutdown dates: mini-fleet/`gpt-5-mini` & `gpt-5` fallback **Dec 11, 2026**, `gpt-4o` fallback Oct 23, `gpt-5.2` insights not yet dated, `gpt-4o-mini` safe; coaching = Claude (unaffected). No OpenAI→OpenAI auto-failover. 2 fixes shipped 2026-06-16: price correction (PR #92 — gpt-5-mini was 2× under-metered) + **reasoning_effort `'minimal'`→`'low'`** (PR #93 — the mini-fleet was silently 400ing on the current gpt-5-mini snapshot; a hardcoded provider-enum churn that monitoring couldn't catch, an eval did). Cutover harness `miniFleetModelEval.ts`: gpt-5.4-mini ≈ gpt-5-mini (100% on the general surface), ~1.9× cost; the checklist-SCORE path = TODO before the Dec-11 cutover.
- [GTM readiness threshold](gtm-readiness-threshold.md) — App verified self-serve-ready (signup→trial→upload→coaching GREEN); bottleneck is distribution/validation (0 real users), not polish. Backlog is quality-tuning, not usability. Push users over debugging.
- [Amin perfectionism / ship](amin-perfectionism-ship.md) — Wants me as the voice of reason vs pre-launch perfectionism; ground reassurance in evidence, give a finite "good enough" bar + one concrete next step.
- [Dialer integration direction](dialer-integration-direction.md) — Auto-ingest = #1 activation lever; model = per-tenant BYO API key (NOT OAuth marketplace — Dialpad dev-account dead end), MightyCall+RingCentral first, funnel into UploadIntent; full architecture deferred until demo page ships.
- [Coaching surface altitude](coaching-surface-altitude.md) — Call Review = to-the-point fix (problem→say-this→drill); the "why it matters"/rationale/pattern depth belongs in the Improvement Plan, not the per-call review.
- [Off-script Call Review v2 SHIPPED](offscript-callreview-deferred.md) — MERGED & live in main via PR #86 (rebase, 2026-06-15); the earlier "deferred/unmerged" record was a rebase-leftover misread (a rebased branch's original SHAs look "ahead" of main even when merged; merged PRs also show closed). Verify a merge with `git cherry`/patch-id, NOT `git log main..branch`. Stale branch deleted 2026-06-16; do not revive.
- [CI gates / doc-code-drift](ci-gates-doc-drift.md) — **MERGE GATE since 2026-07-11: GitHub Actions is billing-blocked (2-4s infra reds); `npm run ci:local` (scripts/ci-local.mjs) is the interim gate + accurate local mirror — run before EVERY PR (`--gates-only` for frontend-only; DB job needs CI_LOCAL_DB_URL disposable PG, else loud skip); release-verifier mandatory-blocking post-merge while Actions is down.** CI "gates" job = check:ui + check:ui-continuity + lint:typography + test:doc-code-drift + frontend vitest + check:script-imports (+ eslint, greened 2026-07-11); doc-code-drift/vitest/script-imports are CI-only (NOT in the edit hook). New/removed files under callReview/v2, pages/, dashboard/tabs|myCoaching must be classified in v2-authority-source-inventory.json or it fails.
- [Audit bugs are decision residue](audit-bugs-are-decision-residue.md) — Triage lens: dead CTAs/unrendered fields are usually deliberate-decision changes with unswept wiring; complete the new decision's states, never restore the old path.
- [Tier pricing/packaging direction](tier-pricing-packaging-direction.md) — TESTING-STAGE decisions 2026-06-18: Haiku base via `AI_TIER_POLICY_MODE=enforce` (currently shadow → everyone gets Sonnet free); Improvement Plan → Coach+ per-seat ($19); Sonnet = enterprise-only; "unlimited (fair use)" marketing + hidden 200/seat cap; lower prices Solo $59/Team $179/Growth $399. Measured Haiku COGS ~$0.30 (old $0.70 anchor stale). Value-play margins. NOT yet implemented — pending the enforce-readiness audit + one impl pass.
- [Worktree stale shared/dist false-red](worktree-stale-shared-dist-falsered.md) — A CoachAI worktree "red build" about a `@ail-sales-coach/shared` contract is usually a stale/foreign `shared/dist` resolved by an uninstalled worktree, NOT a source bug. Diagnose (source vs main dist) before editing; fix = install+generate, never reintroduce removed fields. `check-shared-resolution.mjs` gate now wired into build (2026-07-04) + bootstrap template.
- [Design system vs actual app gap](design-system-vs-actual-gap.md) — The cobalt/gold/Geist design SYSTEM (DESIGN.md/foundations/recipes/tailwind) is aspirational/half-migrated; AccountPage + most surfaces still render the older neutral slate-950/Inter/.input-field style. Amin LOVES the target theme (2026-07-07 integrations mock) + wants the app migrated to it; but mock the CURRENT app off the real rendered surface, not the token files.
- [CoachAI worktree setup](coachai-worktree-setup.md) — Fresh worktree needs npm ci + backend/.env copy + prisma generate + shared build (each missing step = a real cryptic failure, 2026-07-08); plus: live Call Review reads analysis.json FILE-first (volume shadows the DB sidecar), so on the internal stack only a SERVER-side rerun/reprocess changes what the deployed surface serves.
- [Dialer popup-SSO issuer state](dialer-popup-sso-issuer-state.md) — Auxara popup-SSO OIDC id_token issuer lives on the dialer's UNMERGED feat/p4-coaching-loop branch (not main), emits no email_verified; CoachAI consumer dormant; INT-P4 F5 closes takeover vector F2 across both repos.
- [Merge model: local CI is the gate](merge-model-local-ci-gate.md) — CoachAI + auxara-dialer are free-plan private repos → NO branch protection → GitHub checks never gate a merge; ci:local / dialer local CI is the REAL gate; merge via `gh pr merge --rebase`. GH Actions billing-halted 2026-07-11 (red ≠ code signal); main can go red silently (the ba7648c6 DCD-TEST-5 incident, fixed PR #195).
- [Overlay-parity append-only fix](overlay-parity-append-only.md) — The orchestration overlay-parity gates (CoachAI `check-overlay-parity.mjs` + dialer `check-organization-overlay.mjs`) drifted red on every agent `## Learned classes` append; fix = `append_only_markers` strip-before-hash. Shipped CoachAI #224/#225 (merged), dialer #269 (open), universal source `${PROJECT:control-plane}` branch `chore/coachai-overlay-append-only-parity` (unpushed). Do NOT `capture` to sync the universal — it resyncs drift + reds its 209 tests; edit surgically.
