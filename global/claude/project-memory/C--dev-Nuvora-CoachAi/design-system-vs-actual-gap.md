---
name: design-system-vs-actual-gap
description: The cobalt/gold/Geist design SYSTEM is aspirational; most surfaces still render the older neutral slate look — Amin loves the target and wants the app migrated to it.
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 64013ed9-e44d-40d4-bcaa-5cd9d2067f3a
---

CoachAI has a documented **target** design language (cobalt `#0284C7` primary, gold=AI, Geist headings, glass-on-navy) in `DESIGN.md` + `frontend/src/design-system/foundations.ts` + `recipes.ts` + `tailwind.config.js`. **But that is the system the app is MIGRATING TOWARD — it is NOT what most surfaces actually render today.** The real `AccountPage.tsx` (and much of the app) still renders the older neutral style: `bg-slate-950` (near-black) primary buttons, `.input-field` global inputs (13px, 10px radius, **slate** `#64748B` focus ring — not cobalt), 12px muted `--coach-text-muted #4E6782` labels, `rounded-2xl border-slate-200 shadow-sm` white cards, section headings in Inter (not Geist). The design-system primitives (`Button role="brandPrimary"` cobalt, `Panel`, `StatTile`) exist and are used on *newer* surfaces (Call Review v2), so the app is visibly **inconsistent** — half-migrated.

**2026-07-07:** Amin saw an Integrations mock I built grounded in the *token files* (vivid cobalt/gold/Geist) and reacted: "i fucking love it but its completely not in the current app." He wants the loved theme captured (this note) AND a mock that matches the CURRENT actual app. The loved cobalt version is preserved at `...-dialpad-ingest-adapter/tmp/design-mocks/integrations-hub-mockup-TARGET-designsystem.html`.

**Why:** the token docs are aspirational and misled me into mocking a theme that isn't shipped. See [[ux-ui-redesign-audit]] (the all-pages redesign that would close this gap) and [[amin-perfectionism-ship]].

**How to apply:** when mocking/building for the CURRENT app, ground on the ACTUAL rendered surface (`AccountPage.tsx` classes, `index.css` `@layer components`, a live screenshot) — NOT the design-system token files, which describe the not-yet-shipped target. If Amin greenlights the migration, the cobalt/gold/Geist system becomes the real target and this inverts. Verify against the rendered page, not the design docs (the doctrine's "decide from outputs, not summaries" applied to CSS).

**RESOLVED 2026-07-07 — the app is being retuned to match the Auxara DIALER's design family (not the old CoachAI cobalt system).** The dialer (`${PROJECT:auxara-dialer|backslash}`, tokens in `frontend/src/design-system/tokens/primitives.css`+`semantic.css`) has a locked direction **"Calm-led with cockpit atmosphere": iris accent OKLCH hue 274, cool canvas hue 266 + a soft iris/cyan/magenta `--ambient` wash, Plus Jakarta Sans, frosted-glass, light+dark.** Amin's "indigo I love" = the dialer's iris brand. Chosen scope = **Option A** (retune ONLY hue + canvas; KEEP CoachAI's Inter/Geist, gold=AI, orange=upload, navy band; light-only, dark later). **PR #175 (`feat/iris-design-tokens`) propagates it:** `brightBlue` scale → iris (OKLCH + `<alpha-value>`), `canvas` → cool, `index.css` primary/info/links → iris + body → the `--ambient` wash, cobalt gradient bands → iris. Verified (build+check:ui+continuity green) then **MERGED to main 2026-07-08** (rebase, commit 4d27af3a) — the iris design is LIVE. (Screenshot tooling is dead in the build env → verified via computed styles on the running dev server + the test suite.) Full dialer design DNA reference lives in the `-TARGET-designsystem` + `coachai-in-dialer-family` + `coachai-option-a-iris-retune` mocks under the dialpad worktree `tmp/design-mocks/`.

**Integrations hub→provider-page FRONTEND BUILT 2026-07-08 — PR #176 (`feat/integrations-hub-frontend`, STACKED on #175; retarget to main before #175 merges or merge #175 without --delete-branch).** Amin scoped it "build the frontends, I'll have Codex do the wiring." Shipped under `frontend/src/components/integrations/`: `providerRegistry` (data-driven: auxara/mightycall/dialpad) · `IntegrationsHub` (directory, live status via `dialerApi.listConnections`) · `IntegrationsTab` (hub↔detail router on `?provider=`) · `ProviderDetailShell` · `AuxaraConnectPanel` (OAuth authorize, `onAuthorize` seam) · `DialpadConnectPanel` (BYO-key form, `onConnect` seam); MightyCall REUSES the existing `DialerIntegrationPanel`. `AccountPage` integrations tab → `<IntegrationsTab/>`. **Codex seams:** wire Dialpad `onConnect`→connect+test endpoint + Auxara `onAuthorize`→OAuth redirect; `BACKEND_PROVIDER_TO_ID` already maps `DIALPAD`/`AUXARA_DIALER` so hub status auto-lights when those enums+connections land. Verified: build+check:ui+continuity green, AccountPage.test (7) + IntegrationsTab.test (4) pass. Presentational only — connect actions toast honestly until wired. **MERGED to main 2026-07-08** (stacked-order handled cleanly: retargeted #176→main, merged #175, rebased #176 to drop the dup token commit, merged; both show MERGED; local main synced to fe8f14bf). **DISCOVERY at merge:** Codex has ALREADY landed the Auxara Dialer BACKEND on main in parallel — `auxaraDialerAdapter`/`Config`, `auxaraWebhookJob`, `auxaraDialerWebhook` route, big `dialer.ts` OAuth-route changes, `dialerOAuthRouteRegression`, migrations `auxara_dialer_connect`/`_webhook_events`, `dialerAdapterRegistry`. **REMAINING WIRING GAP (route to Codex):** the frontend `onAuthorize`/`onConnect` seams still toast — they must be wired to Codex's live OAuth/connect routes (a manager clicking Auxara/Dialpad Connect in prod gets a "being wired up" toast until then). Post-merge: **Vercel frontend prod deploy succeeded (Amin confirmed); Railway N/A** — this was a frontend-only change (design tokens + React components), so no backend deploy. CI green pre-merge on both PRs.

**Follow-up fixes (the ambient reveal surfaced hidden opaque chrome):** (1) **#178 MERGED** — the ambient was on `body` but the `Layout` shell painted an opaque `bg-canvas` over it → invisible ("why there is nothing"). Fix: `app-ambient` tailwind token painted on the shell + `<main>` made transparent. (2) **#179 (open)** — revealing the ambient exposed the rep **DashboardPage sticky header** (line ~997) as an opaque `bg-canvas` slab over the wash ("white background on the hero") — the EXACT bug the dialer already fixed (its `design-system/app-header.css`: an opaque header slab "covered the ambient glow… the user-reported 'black background in the hero'", removed). **Reusable lesson: page chrome over an ambient canvas must be a FROST PANE — `background: transparent` + `backdrop-blur` (the blur frosts content scrolling under it; over a static gradient it's inert so the wash flows through), never an opaque fill.** Blast radius: shared `WorkbenchHeader` is already transparent; `surfaces.pageHeader` (bg-canvas) token is DEAD (0 consumers). **KEY: CoachAI ALREADY HAS the gate — `scripts/check-header-surface.mjs` (part of `gates`) REQUIRES a pinned header to carry a SURFACE (a `bg-*` OR `surfaces.pageHeader`/`.glass`/`.glassStrong` OR `app-header` OR a `// header-surface-ok:` marker).** My first attempt (`backdrop-blur-lg`, no fill = fully-transparent frost pane, dialer-style) FAILED that gate as the "transparent header bug" → CI red. **Fix that passed: a TRANSLUCENT GLASS — `bg-white/60 backdrop-blur-xl`** — satisfies the gate (has a bg) AND shows the ambient through (40% translucent + blur frosts scrolled content), instead of the opaque slab. #179 CI green, MERGE PENDING. **Tension noted:** CoachAI's gate wants headers to be a surface (not fully transparent), UNLIKE the dialer's fully-transparent `app-header`; the glass is the compromise. To go fully dialer-transparent would need the escape marker or extending the gate's SURFACE_RE to accept `backdrop-blur` as a valid frost-pane surface (offered to Amin). Still OPEN with Amin: migrate AccountPage's legacy inline `bg-slate-950` buttons → design-system `Button role=brandPrimary` (iris) so the Account page isn't the low-iris outlier.
