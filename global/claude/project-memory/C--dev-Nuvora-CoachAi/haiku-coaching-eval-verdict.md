---
name: haiku-coaching-eval-verdict
description: "Verdict of Claude Haiku 4.5 vs gpt-5 base-coaching eval (Prompt A): WINS craft 11-1-4; the '29% fabrication' was INFLATED by transcript-only judging — material-aware re-judge → 7 real fabrications in 5 mechanical classes → Haiku is a VIABLE drop-in CONDITIONAL on a grounding guard. GROUND-1 guard layer BUILT + MERGED (PR #80). Base-model swap DONE (PR #81): Haiku IS the base coaching model — but INERT until AI_TIER_POLICY_MODE=enforce; prod still serves Sonnet to everyone."
metadata: 
  node_type: memory
  type: project
  originSessionId: 72cdc28d-460a-4655-9c29-9a1c20fde6db
---

Evaluated Claude Haiku 4.5 as the BASE model for Prompt A (structured coaching decision) vs a gpt-5 oracle on 20 internal-DB golden sessions (`backend/scripts/coachingGoldenEval.ts`; gitignored PII data in `backend/scripts/goldenEval/`). Verdict 2026-06-15 (CORRECTED after a material-aware re-judge):

- **Craft: Haiku WINS 11-1-4** (material-aware re-judge of 16 sessions; was 10-1-6 transcript-only). Drills "elite"; picks the sharper moment more often; cleaner than gpt-5 on paralinguistic-tag fabrication. gpt-5 is "mid" — Haiku beats that floor. [[quality-bar-exceed-baseline]]
- **Structural completeness: FIXED (uncommitted)** — Haiku mis-nests the decision JSON: a single-key wrapper envelope whose key NAME VARIES (`{"json":{…}}`/`{"call_json":{…}}`/`{"object":{…}}`) + a double-encoded `primary_fix_bundle`. A deterministic, key-agnostic UNWRAP + un-stringify in `coerceFooterShapeSlips` (`machineCoachingFooter.ts`) cut primaryFix-null **65%→15%** (13→8→3/20). Pure shape recovery. Regression: `npm run test:regression:machine-coaching-footer`. 3 residual nulls = content-null (Haiku leaves the bundle null even on retry).
- **Grounding — RECALIBRATED (the headline finding was a MEASUREMENT ARTIFACT):** the first 17-judge panel checked grounding against the TRANSCRIPT ONLY (no approved script) and reported 10 fabrications / 5 sessions (~29%) → "not a drop-in." Re-judged WITH the org's approved script+playbook (the rep is trained on it), **12 of those flags were FALSE POSITIVES** — script-grounded, rep-trained moves a transcript-only judge can't see (the 4-step objection method, approved acknowledge language "I hear you"/"that makes sense", the verify-the-file frame, the "I already have a will" reframe, the two-time sandwich callback, "no-cost"/"complimentary" per the never-say-free gate, lawyer/notary-fee framing). Only **7 REAL fabrications survive (5/16 sessions)**, in 5 tight mechanical classes: (1) **appointment-day conflation** — names an OFFERED-then-REJECTED day as booked (Saturday vs the actual Thursday) in rep-sayable cementing lines [2bb39d59, 44329be4] — highest harm; (2) **invented numeric specific vs material** — "15/20-min" duration where every script says 30-40 min [7602f71b (Haiku's OWN gate flagged it invented_fact), 77bbdc3c]; (3) **invented statistic** — "91% of the talking", no source [644d3176]; (4) **cross-call claim from a single-call payload** [644d3176]; (5) **wrong-lead-type procedure** — POS-script "beneficiaries/signatures" applied to a Will-Kit call [44329be4].
- **Decision (corrected): Haiku is a VIABLE drop-in CONDITIONAL on a THIN grounding-guard layer** (date/number/lane/scope), NOT a rewrite. The guard must: (a) reconcile any stated appointment date/day against the LAST-CONFIRMED slot; (b) hold rep-sayable durations/numbers/stats to a material allow-list + reject unsourced figures; (c) scope claims to the evidence in the payload (no cross-call patterns); (d) validate procedural claims against the ACTIVE lead-type script (not the union). **CRITICAL: do NOT clamp the script-grounded content the transcript-only panel over-flagged — that was the artifact; suppressing it would gut Haiku's best coaching.** Plan: `docs/app-plan/implementation/grounding-guard-layer-plan.md`; backlog GROUND-1. Route in prod: `OPENAI_COACHING_MODEL_BASE` in `analysisConfig.ts` + `AI_TIER_POLICY_MODE=enforce` (Haiku already proven for Prompt C). Eval spend ~$18.9 / $20 (incl. the 2026-06-15 broad re-validation + Haiku-vs-Sonnet head-to-head).
- **Guard layer status (2026-06-15): BUILT + MERGED to main** (PR #80, fast-forward `57e5c34b`→`94ad3af6`, main CI green). Commits `023a76bc` guard + `c27f777d` eval harness, PLUS the fix for the `57e5c34b` split-commit CI breakage it surfaced: that commit shipped frontend consumers of `callSituation.hasVerifiedAppointmentTime` (sample fixture + smoke tests) WITHOUT the GROUND-1 contract that defines the field → Vercel `tsc` failed → app crashed; landing GROUND-1 supplied the contract, plus typography (`/sample-review` eyebrow → `editorialEyebrow` token), the ManagerCallReviewV2 fixture field, and **DCD-TEST-5 evolved to the editorialHeadline-authority model** (the v2 "tighten The Moment" made the AI `editorialHeadline` the authoritative headline with `fix.headline` as the grounded fallback; editorialHeadline is validated vs proof evidence in `openai.ts`, so it can't hide the focus — Amin chose "update the gate"). Lesson: a split commit that lands consumers WITHOUT their contract is a CI-red footgun; never push to `main` directly (it's PR-only — a direct-push was correctly auto-denied).
- **Base-model swap DONE (2026-06-15, PR #81 `28bc70f1`):** `OPENAI_COACHING_MODEL_BASE` `gpt-5`→`claude-haiku-4-5-20251001` (the model Prompt C already runs), in `analysisConfig.ts`. Verified build + ai-metering. **INERT** — `resolveCoachingModel` only returns the base under `AI_TIER_POLICY_MODE=enforce`; shadow/warn (current) gives everyone `OPENAI_COACHING_MODEL`=Sonnet, so prod coaching is UNCHANGED. Making Haiku actually serve coaching = flipping enforce = a tier/billing decision (the Coach+ paywall activation), deliberately NOT done. **Owed-before-enforce items NOW DONE (2026-06-15):** (1) **broad re-validation** — Haiku-as-base on all 20 golden sessions: 20/20 produce coaching, 15% primaryFix-null (known content-null residual), guard fired on 5, $0.186/call — validated. (2) **Haiku-vs-Sonnet** (5 material-aware judged pairs): **SONNET WINS 3-2** (gap wider than count — Haiku twice shipped null/empty lines + fabricated premises + script-forbidden phrasings); **Haiku < Sonnet for the UNIVERSAL coaching task → CONFIRMS Haiku = base/Lite only (with the guard), Sonnet STAYS the everyone-default; do NOT promote Haiku to the universal model.** Eval-DB ROOT CAUSE (corrected — it was NOT a column-drift; `migrate diff` showed the eval DB MATCHES main schema): a silent-noop `prisma generate` left a STALE client that failed ALL Session queries. Fix per eval run: `rm -rf node_modules/.prisma/client && npx prisma generate` right before the reprocess, and keep the dialer-WIP schema OFF the on-disk schema. Side-finding → spawned `task_cdb3f20f`, RESOLVED 2026-06-15 — it was a JUDGE FALSE POSITIVE, not a guard gap: the "invented" manager name 'Sahar' is verbatim in fb970b2f's transcript ([9:12] booker manager-handoff, spelled [9:21]/[9:26]), and the judge HAD the full transcript (buildJudgeInputs threads the UNFILTERED `transcript_clean`; the role-filtered view is never persisted). The requested deterministic name guard was REJECTED as mis-specified (names are ASR-mangled + usually absent from scripts → not string-verifiable; demoting a name-only line is rep-HIDDEN in strict mode `callReviewMapper.ts:461` with the transcript always present in prod → it would over-block real coaching; "did the call establish this person?" is an AI meaning judgment). Durable root fix BUILT instead: `crossCheckJudgeFabricationClaim` (`backend/scripts/judgeClaimGroundingCheck.ts` + `npm run test:regression:judge-claim-grounding`, 15/0, mutation-proven + live-proven on real fb970b2f data) — grounding-checks a judge "X is fabricated" verdict against transcript+material via the SHARED `coachingGroundingMatch` authority before it's trusted; advisory/non-authoritative, never masks a real fabrication. Lesson: a judge fabrication verdict is a LEAD — grounding-check the disputed token before it drives a guard/backlog task (journey Pattern 23 sequel thread 11). FLAGGED follow-up: `backend/scripts/goldenEval/` is untracked-but-NOT-gitignored despite its "gitignored" comment and holds full transcripts (PII) — a `git add .` would commit PII. Covers: (a) Haiku envelope unwrap; (b) Phase 1+2 laundering gate (`verifiedFactsUsed` validated vs transcript+material, replacing the self-referential check); (c) Phase 3 appointment-day (committed-slot authority — offered-vs-accepted, AI disambiguation for multi-offer, rep-sayable date cross-check); (d) numeric-duration (meeting-duration vs the script's, span-scoped to meeting context so incidental durations aren't clamped); (e) DIAG-1 mandatory-topic note-leak. Each adversarially reviewed — **2 BLOCKING bugs caught + fixed + mutation-proven** (a Phase-3 accept/decline INVERSION that locked a rejected slot; a numeric-duration OVER-BLOCK that clamped "save 15 minutes"/"20-minute drive") — and live-eval-validated (0 fabrications shipped on canary calls, 0 over-block on clean booked calls). **Phase 4 (speaker) DROPPED** (re-judged grounded). **Remaining small classes (backlog GROUND-1, not built):** #3/#4 invented-statistic + cross-call-from-single-call payload (payload-scope check) and #5 wrong-lead-type lane (active-script AI audit) — 1-2 cases each, lower harm. Decision still open: actually SWAP the base model to Haiku (`OPENAI_COACHING_MODEL_BASE`) — the guard is model-agnostic and improves gpt-5 today, so the swap is independent of the guard being done.
- **Working-tree note → RESOLVED (final consolidation 2026-06-15, last-agent wrap-up):** the guard work was committed OUT of a tree tangled with ≥3 other uncommitted efforts. All now consolidated to `origin/main` (b617ecb9): callback-guard **builder→validator** landed **PR #89** (`b67716ea` — `guardCallbackCoachingCopy` line-builder → `detectCallbackCoachingIssues` pure validator that signals into bounded AI repair, fails the line closed; legacy `callReviewMapper` read-path now fails closed on committed-day conflict); the `.claude/agents`+`settings.json` agent-fleet config + the goldenEval `buildJudgeInputs.mjs` harness (dangling ref of the merged #87 guard) + the dialer **DAI-004/DAI-005** plan slices + 2 doc-staleness fixes (BLAST_RADIUS callback entry, BUG_BACKLOG PII-closed) landed **PR #90**; the `auto-ingest-api.md` "REALIZED by dialer plan" Status fix landed **PR #91**. Objection/CQ-004 + the dialer module + judge-claim guard were already merged by other agents. The WIP stash was verified fully-redundant and dropped (reflog-recoverable). **STILL DEFERRED (preserved, NOT abandoned):** the off-script Call Review v2 feature (compact rep rhythm + `OffScriptSection`) lives on local branch **`feat/rep-call-review-tighten-offscript`** (3 commits ahead: `a2bdcabe`+2 doc-drift; PR #86 closed unmerged) — a visible frontend feature → mockup-gated, awaiting Amin's call to revive as a fresh slice. [[offscript-callreview-deferred]] [[git-ff-main-sync]]
- **Method lessons (capture):** (1) **A grounding judge MUST have the approved material, not just the transcript** — a transcript-only grounding check over-flags every script-trained move as a fabrication (inflated 29%→ the real ~half). (2) A single salvage-agent claimed "0 fabrications"; an independent per-session panel is sharper — but BOTH are wrong without the material. (3) Checking an entity's PRESENCE isn't enough — verify SPEAKER + REFERENT + LEAD-TYPE LANE of the load-bearing line.
