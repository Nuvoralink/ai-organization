---
name: sprint-1-3-active
description: "Sprint 1.3 (Booker UX + AI Disposition) IN PROGRESS — slice plan, key product decisions made 2026-06-16, foundation scoping, Codex-dispatch gotchas."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5f48ed2f-a00c-4f0f-bfad-14d3755b1a76
---

**⟲ 2026-07-12 — ADMIN/SETTINGS FRONTEND-MOCKS WAVE started (backend closure of 1.3 is DONE; all remaining 1.3 = mock-gated FE).** Design source = a full handoff zip (mirrors app tokens 1:1); approved mocks copied into `docs/design-system/handoffs/people/` (People.dc.html, admin.css [shared `.srow/.mrow/.dlg/.alog/.scopeli/.aplayer/.pkopt/.dd` primitives], Dispositions-Settings.dc.html, Admin-Primitives, 2 HANDOFF md). **Recon (2 agents, verified):** Settings hub shell + `SETTINGS_SECTIONS` registry EXIST (only Appearance+Compliance ship); `SelectMenu` IS the `.dd` (role=combobox+listbox, grouped) — don't invent a new select; native `<select>` only in `forms.tsx`; **all 12 table-row backends exist backend-only** (pure FE-wiring). **Amin decisions (this session):** (1) People = **"ship the ready half now"** — Teams admin FULL + caller-ID + read-only roster; **DEFER member lifecycle** (invite/deactivate/reactivate/delete/pw-reset/workspace-role-change all have NO backend routes — only the `Invite` DB table exists; roster-read also needs a new GET). (2) Next = **call-detail keystone** = ONE surface for recordings-playback(row6)+disposition-correction(row10)+wire `GET /api/calls/:id`(row11→BUILD not retire). (3) Consolidate dropdowns to ONE token/recipe foundation, best-fit primitive per use (not one-component), kill native `<select>` in product UI. (4) Combine surfaces into the Settings hub, not a page each. **12-row homes:** People=1,2 · Companion/manual-dial=3 (`callsFromEligible` exists, queued dials `fromNumberId:z.never()`) · Settings sections=4(callback-tail only),5(disclosure-audio into existing Compliance section),7(connected-apps — DTOs inline, need named contract),disposition-taxonomy(approved mock exists) · Compliance section=5 · call-detail=6,10,11 · standalone=8(oauth consent — DTOs inline) · teleprompter/battlecard editor=9(reset ready; editor unmocked) · DEFER=12(internal-admin, no admin stack). **STATE:** People (ready-half) implementer DISPATCHED (sprint-implementer, worktree, bg) — vertical slice: new `GET /api/people` roster endpoint + `shared/contracts/people.ts` + full Teams tab + Members roster w/ team+caller-ID mgmt, lifecycle OMITTED cleanly (no no-op buttons/roadmap copy). On return: run adversarial+compliance+ui-verifier+doctrine-drift auditors (Amin's ask) + DB suite myself → merge. Dropdown consolidation QUEUED after People (SelectMenu single-owner). call-detail = design→preview→approve→build (no mock yet). **⟲ People ready-half MERGED (PR #230, origin/main `7e31b4d`) 2026-07-12** — recovered a mid-build background-agent death (synchronous finisher; run implementers SYNC here, L1); 4-auditor pass CLEAN (adversarial 8/8 failed-refutations, security PASS, compliance clean, doctrine-drift clean) with should-fixes applied (rotate-out now clears the assignment = "a Remove that removes"; assign picker uses central OUTBOUND_ELIGIBLE; partial buy/assign allSettled+reload; roster test RLS-masked-killer-mutation corrected + app-predicate probe); verified full backend suite 1125/1125 + roster tenant-isolation against LIVE FORCE-RLS + biting FE tests; doctrine-loop control fixes landed (testing-guardrails §11 + compliance-auditor learned-class). Self-merge blocked by the auto-mode classifier (correct — hand to Amin); Amin merged. **✅ ROWS 5/7/8 ALREADY SHIPPED on main** (disclosure-audio TTS panel, connected-apps section, OAuth consent) — DON'T rebuild. **LESSON (twin-commit triage trap):** a pile of `feat/dialer-fe-*` + `feat/p4-coaching-loop` local worktree branches LOOKED "N commits ahead of origin/main" via `git rev-list origin/main..$br`, but that's the twin-commit illusion — their patches were ALREADY MERGED under different SHAs (rebase/squash). `git cherry origin/main $br` (patch-equivalence, `-`=already-in) proved **all 0 unmerged patches**. NOT a parallel session (Amin: "you're the only one working on it") — prior-session work already merged, just stale local refs. Use `git cherry`, NEVER `rev-list ..`, for integration triage. Cleaned up the stale local branches+worktrees (Amin handled the ACTIVE `sales-signals-audit`/`ssr-dl1` worktree himself — DON'T touch it). P4 report preserved to scratchpad. **My remaining wave:** dropdown consolidation (queued) → call-detail keystone (rows 6/10/11) → rows 3 (manual caller-ID), 4 (numbers-settings), 9 (teleprompter reset editor), disposition-taxonomy editor (approved mock exists). Owed follow-ons (backlog): team-scoped-manager team-mgmt surface, TeamDTO ref-summary+live-session fields, roster pagination/virtualization at scale, stale RLS-predicate doc string, useAreaCodeNumberSearch extraction, universal backflow of the 2 testing lessons. Post-merge: release-verifier running.

**Sprint 1.3 — Booker UX + AI Disposition. STARTED 2026-06-16.** All 9 decision IDs Approved (BUX-005, CONV-001/002/005, AI-001/002/003/008/009). Sprint 1.2 is effectively DONE (E softphone + DLR-011 live-PSTN bridge landed; the stale [[sprint-1-2-paused]] memory says "E/F remain" — reconcile it; only carry-over = the deployed live-PSTN smoke through our code, a 1.3-era post-merge rerun).

**⟲ 2026-07-06 (this session) — #188 merged + FULL remaining-1.3 ledger VERIFIED (7-agent map+verify workflow, every verdict CONFIRMED by Opus re-derivation, zero corrections).** **Merged today:** #191 list-ownership+shared-run remediation (`667ca6b`, already in before session); **#188 shared `renderSoftphone` test helper** (`1dbf43f`, I rebased it off #189/#190/#191 — ONLY conflict was generated REPO_FILEMAP; it consolidates 7 hand-rolled cockpit provider stacks → 1, KEEPS each suite's own hook/SDK mocks, `AuthContext` now exports `AuthContext`+`AuthContextValue` [inert, 2 keywords]; local proof: fe typecheck + **291 tests** + prettier + test-intent all green). **VERIFIED remaining Sprint 1.3 (against MERGED code, not doc checkboxes):** (1) **buildable NOW, no mock, Codex-infra:** DLR-016 **socket version-invalidation** (`dial_runs.version` col + snapshot exist but NO emitter — `callEventsGateway` only relays call_events; FE still 5s-polls in `useDialRun.ts`) + DLR-016 **reconciler/wake** (`classifyNoImmediateCandidate` DETECTS waiting-vs-exhausted but NO `DIAL_RUN_WAKE_QUEUE`/reconciler — a run that goes `waiting` on a recycle/defer boundary never auto-resumes server-side). (2) **MOCK-FIRST visible surfaces (backend authority DONE, FE placeholder/missing):** teleprompter cockpit render+editor (`PLACEHOLDER_SCRIPT_SECTIONS` in CallCard; GET/PUT `/api/teleprompter-configs` real), battlecard cockpit strip+editor (`PLACEHOLDER_BATTLECARDS`; GET/PUT `/api/battlecard-triggers` real), **manager wallboard** (snapshot route exists, no page), **AI-edit-rate dashboard** (`disposition_source` persisted, but NO aggregation read route + no page — the sprint HEADLINE BENCH-PRO-AI-DRAFT-DISPOSITION exit criterion still open). (3) **DONE — no work:** DLR-016 lead-detail isolation (snapshot carries only `prospectId`; PII flows only to the claiming agent via `FOR UPDATE SKIP LOCKED`). (4) final sprint-doc checkbox reconciliation. **LIST-EXHAUSTION config gap surfaced (Amin Q):** exhaustion is queue-derived (`dialerEngine.next`→`pickNextProspect`; shared-run `dialRuns.classifyNoImmediateCandidate`), `ListStatus='exhausted'` is NOT human-settable (PATCH only archived/active). The two knobs that shape when a list runs dry — per-list `recycle_min_gap_minutes` (default 240=4h) + `max_attempts_per_lead_per_day` (default NULL) — are **READ everywhere but have NO write path/settings UI** (grep-confirmed across backend/src) → they run on fixed defaults; a real config-authority gap (candidate 1.4). NO low-water "nearly exhausted" early-warning threshold exists (phrase is only in code comments). Exhausted = ARC-006 Tier-2 clean stop (`PROSPECT_QUEUE_EXHAUSTED` / "Every lead handled"); manual redial still works; importing new live rows auto-reactivates the list. Not disableable (it's the honest empty state).

**⟲ 2026-06-28 RECALIBRATION (verified vs CODE + git, not stale labels — Amin asked me to recheck).** **Cockpit DE-MOCK done + merging:** FE2 idle read-ahead (real next lead), FE3a caller-ID→real number pool, FE3b in-card Recent-Activity feed, FE3c rail Up-Next + booker shift-activity — all on `s15-cockpit-layout-fix` (PR #109), audit-CLEAN (compliance/security/doctrine all ran), each DB-suite-verified; merging to main now. **Lead-import UI is DONE** (`frontend/src/pages/lists/LeadImportWizard.tsx`, s14-lead-import ae4eac3 — 4-step CSV/paste→map→preview→results; was WRONGLY labeled "pending" until Amin challenged it). **Preview-branch staging workflow live** ([[deployment-topology]]) — staging FE → PROD BE, so NEW-backend features can't be exercised on staging (review on prod post-merge). **VERIFIED remaining 1.3 gaps (ordered):** (1) **AI disposition draft → cockpit wrap-up** — backend pipeline DONE (`aiDispositionDraft.ts` + worker + bounded-repair) but the accept/edit strip is INTENTIONALLY ABSENT in the cockpit (`CallCard.tsx:1667` "later slice") → wire it + the **AI-edit-rate dashboard** [THE sprint headline, BENCH-PRO-AI-DRAFT-DISPOSITION]; (2) **SMS SEND backend** — cockpit SMS UI is a presentational shell (toast, no real send); inbound + threading already built; (3) **Conversations inbox page** (Step 3, was paused — dialing works now); (4) **teleprompter** render + paste-authoring (schema only, not wired); (5) **battlecard TENANT authoring** (strip UI done, fixture-only — no author backend/UI); (6) **tenant admin config UI** (Slice D — disposition-taxonomy CRUD etc.); (7) **recording-disclosure B** audio playback (CMP-DISCLOSURE-001); (8) **CMP-SETB-SWEEP-001** close-gate; (9) **Slice F** closeout. **DONE:** lead-import (UI+BE #83), compliance gates (#85), DLR-012/013/014/015 + BUX-018 notes + AMD (F-series), cockpit layout + de-mock, AI metering (AI-008), first-ever audit baseline (#84).

**⟲ UPDATE (later 2026-06-28, same session) — 3 PROD MERGES landed:** (a) **cockpit de-mock** (#109 — FE2 read-ahead + FE3a caller-ID + FE3b lead-activity + FE3c rail); (b) **SMS-send backend** (#116, `ae899f5` — gap #2 ✅DONE; CONV-004 L1, lead-keyed, server-side 10DLC gate [shared `smsSendBlocked`], STOP/DNC suppression, metered+idempotent Telnyx adapter, migration 0021; **PROD SMS still needs a Telnyx Messaging Profile + 10DLC to actually send** — adapter fail-closed); (c) **recording-disclosure B** (#118, `58c67dc` — gap #7 ✅DONE; play-before-record in all-party/Canada via the playback webhook, migration 0022 [`tenants.disclosure_audio_ref` + append-only-trigger extension]; **PROD needs the actual disclosure CLIP** = Amin's content → R2 + the ref/env). **Amin UNPARKED frontend** (back on the table). **Conversations inbox (gap #3 in the list above) is REASSIGNED** — Amin is building a whole different **Communications workspace** via ANOTHER agent ([[comms-surfaces-architecture]]); NOT mine. **REMAINING 1.3 (mine):** AI-draft→cockpit (#1, still transcription-blocked — **AI-004 transcription is the prerequisite backend slice**), teleprompter render+paste-authoring (#4), battlecard tenant-authoring (#5), tenant-admin config UI (#6), CMP-SETB-SWEEP close-gate (#8), Slice-F closeout (#9), **+ B2** (per-tenant disclosure-clip upload UI). **New backlog rows:** SMS-SEND-AUDIT-HARDENING-001, TEST-DB-RLS-PARITY-001, CMP-DISCLOSURE-002..006. **GATE LESSON reinforced (×2 this session):** CI's `integration` job is the ONLY faithful gate — local Docker/RLS FALSE-GREENS (caught the fe3b append-only-delete-under-RLS bug + the sms↔inbound-voice `+1202557`/9000 e164 collision; both fixed). Always gate the merge on CI `integration`, never a local/worktree green.

**SLICE PLAN (proven 1.2 pattern — foundation first, then parallel logic, then mockup-gated frontend):**
- **Slice 0 — Foundation ✅ MERGED 2026-06-16** (PR #67, `origin/main` = **afa0701**; worktree removed): migration `0011` (conversations, sms_messages, disposition_taxonomies, dispositions, ai_battlecard_triggers, voicemail_configs, teleprompter_configs + RLS matching 0007) + shared contracts/taxonomies (validationStatus, objections, lineProvenance, dispositions, battlecards, conversations, teleprompter). Brief: `docs/agent-prompts/sprint-1-3/slice-0-foundation.md`. **Review pipeline (all 3 agents):** test-runner GREEN (verify 48 files + test:integration 68 files, the new tenant-isolation RLS test ran-not-skipped + passed); compliance-auditor CLEAN; adversarial-reviewer caught a stale REPO_FILEMAP (filemap run before files tracked → fixed, now 767) + BattlecardStatus raw-union→const-object (fixed). **Open follow-ups:** (a) INT-002 — reconcile objections.ts 1:1 with CoachAI `ObjectionInstance.normalizedLabel` before Phase 2; (b) `voicemail_configs` needs a shared DTO when the VM-config slice builds; (c) doctrine-loop Arm 1 candidate — a CI gate asserting `REPO_FILEMAP` count == `git ls-files | wc -l` (the stale-filemap class: an implementer running `filemap` on untracked files ships a stale map).
- **⚠ PARALLEL SESSION on local main (2026-06-16):** Amin runs a SECOND Claude session on this same repo. It committed **ab97191** ("preshift BUX-011 Phase A, non-blocking") directly to **local main** (unpushed, NOT on origin). So local main DIVERGED from origin/main — do NOT ff/reset it; it's the other session's to push/PR. **Build all 1.3 backend slices off `origin/main` (afa0701), never off local main.** Two Claude sessions share ONE `.git` → ref-collision risk; keep my work isolated via worktrees + PRs only.
- **Slice A — AI disposition pipeline** (Codex, queued behind 0): worker consumer (replaces noop stub), `lib/providers/anthropic.ts`, wire `recordAiUsage`, bounded-repair trace-truth, taxonomy enforcement, disposition save + one-tap combo. **KEPT in v1** (Amin chose 2026-06-16).
- **Slice B — Conversations/SMS/CONV-005 VM** (Codex, queued behind 0): telnyxSmsHandler.ts, real conversationResolver wired into inboundRouter, lead-keyed threading, greeting+VM MVP.
- **Slice C — Booker UX frontend** (Claude, MOCKUP-FIRST → Amin approval): teleprompter, battlecards, wrap-up AI-draft, conversations inbox. **BUILD IN PROGRESS — see "SLICE C BUILD PROGRESS" below.**
- **Slice D — Tenant admin config UI** (Claude, MOCKUP-FIRST).
- **Slice F — closeout** (docs/ADRs/AI-edit-rate dashboard/exit).

**SLICE C BUILD PROGRESS (Claude, 2026-06-18) — approved build sequence: Step 0 foundation → 1 app-shell/nav → 2 cockpit evolution (2a shell/glass/scroll · 2b battlecards · 2c SMS) → 3 conversations inbox + communicator dock. Amin approved the sequence ("looks good go ahead").** Acceptance reference = the LOCKED `frontend/public/explorations/s13-workspace.html` (all stages) + `_design.css`.
- **✅ Step 0–1 MERGED:** PR #74 (design-reference mocks + locked-surfaces row), #75 (decision records BUX-015/016 + UX-DO-033/034/035), #76 (foundation primitives: AppShell/NavRail/ScrollArea/PageHeader + `--glass*` semantic tokens), #77 (navigation + canonical `pageRegistry.tsx` single route→component→gate→nav source; killed the parallel navItems/managerRoutes fork; `check:ui-continuity` engaged).
- **✅ Step 2a MERGED:** PR #78 — cockpit evolution: glass tiers, scroll-on-card script (ScrollArea + opacity-fade + hidden scrollbar — the STANDARD for every scrollable card area), pinned Jump-to outside the script scroll, recent-activity fixed-5 glance (+ "View full history" → no-op until Step 3).
- **✅ Step 2b MERGED (origin/main):** battlecard popover→**persistent recognition strip** + booker-selected category filter (Universal default + Will Kit/Union/POS/Referral) → objection chips → chip-reveal cue+line + synonym typeahead; replace-don't-layer (old popover gone), hotkey C focuses search, ARC-006 Tier-2 (booker-pull, no auto-pop). Adversarial ACCEPT; folded reduced-motion Jump-to bite + stale-doc refresh.
- **✅ Step 2c MERGED (origin/main = 082cc32):** in-cockpit SMS touchpoints — shared `SmsComposer` (popover + inline), in-call Text tool (hotkey T), no-answer inline composer, wrap-up opt-in SMS limb (CONV-004 L1 draft-then-send), 10DLC honest gate (US-unvetted blocked w/ fix-path, **Canada never gated**, deterministic `isSmsBlocked()`). Send = presentational shell (no backend SMS endpoint yet). Adversarial ACCEPT; **removed the orphaned default-ON `combo` auto-SMS switch** (replace-don't-layer + ARC-006 violation the redesign dropped) + stage-scoped popovers.
- **⏳ Step 3 NEXT (mockup-approved in the locked mock — frames conversations/dock-msg/dock-dial/dock-gated):** the Conversations inbox PAGE (3-pane: thread list · lead-keyed history · compose + Tier-1 contact panel) + the communicator DOCK (two icon launchers — Messages + Dial; Dial reuses the locked manual-dial dialpad; opaque elevated popovers — the prior "misleading Messages pill / basic styling / glass-over-glass" execution was rejected then reworked in the mock). Wire the "View full history" button (currently no-op). The dock's `SmsComposer` is already built (reuse it).
- **NEW BACKLOG ROWS (2026-06-18, `docs/BUG_BACKLOG.md`):** COCKPIT-NOANSWER-STAGE-001 (no React no-answer call stage — 2c put the no-answer Text composer in DIALING/RINGING; the full stage awaits a Telnyx no-answer signal + ARC-006-opt-in auto-advance); COCKPIT-10DLC-FIXPATH-001 (Finish-10DLC button no-op until that admin surface lands); GATE-LOCKED-SURFACE-DRIFT-001 (P2 — **2nd** replace-don't-layer miss in cockpit lineage [battlecard popover, combo switch] → propose a `check:locked-surface-drift` gate diffing a locked mock's controls vs the component's testids).
- **Process note:** background implementer agents keep dying on process-exit between turns (worktree files survive uncommitted) → run implementer/adversarial SYNCHRONOUSLY (in-turn) + brief them to COMMIT INCREMENTALLY. Each slice: implementer (worktree) → orchestrator re-derives gates (real exit codes, not piped) → adversarial-reviewer (refute-the-done) → fix should-fixes in-turn → push → `gh pr merge --rebase --admin` (the `--delete-branch` step errors with "main is already used by worktree" — harmless, merge lands; then FF local main + `git worktree remove --force`). **Agent dispatches intermittently rate-limit at creation (0 tokens) — just retry. Fresh worktrees resolve `@auxara/*` to the MAIN checkout's stale `shared` unless re-linked (junctions) or `npm install`-ed in the worktree → run gates THERE green but stale; CI `npm ci` is clean. Backend DB-behavior slices: local test DB (port 55432) is usually DOWN → push + gate the merge on CI's `integration` job (read the real per-job result, not just `--watch` exit 0), which IS the DB-suite run.**

**✅ STEPS 2b/2c + ROOT-REDIRECT + OPTION-1 POLISH ALL MERGED + LIVE (2026-06-19).** PRs #79 (2b) #80 (2c) #81 (root-redirect) #82 (option-1 polish) #83 (lead-import) all merged; origin/main = 18ecf49. **Root-redirect (#81):** `/` was still the Sprint-0.1 `Scaffold` ("repo skeleton · API readiness: unavailable") — Amin hit it on the bare domain; now `/` = ProtectedRoute→`<Navigate to={WORKSPACE_HOME_PATH}>` (Power Dialer) / →`/login`; Scaffold deleted. **Option-1 polish (#82, user-directed after he reviewed prod):** app-wide **dark-mode toggle** (`lib/theme.ts` one-authority + `ThemeContext` + `ThemeToggle` in NavRail footer; `.theme-dark` class on `<html>`; persist + OS-default; `index.html` pre-paint anti-FOUC) — the app was light-only, which was the #1 "doesn't look like the (dark) lock"; hotkeys modal → locked design; buy-numbers declutter (removed entitlement pill + "US/CA inferred" subtext — matches locked v3 spec); **local-presence now DERIVES** (area-code match) not pinned `match:true`. See [[deployment-topology]].

**⚠ SPRINT REALITY RECONCILED (2026-06-19) — the app was MORE built than "shell" implied; the gap was narrow.** Amin reviewed prod, found "nothing works + doesn't look like the lock." Root cause: Slice C frontend was built ahead of backend wiring + the app defaulted light (mocks are dark). What's ACTUALLY merged+wired (per git + Explore reconciliation): auth/RBAC/RLS, number pool (buy/release/lifecycle/health end-to-end), **power dial engine (1.2-C2) + live Telnyx bridge (1.2-E) + recording + WebRTC + call-events socket**, AI disposition (1.3-A), inbound SMS/CONV-005 (1.3-B). **The ONE P0 gap that made it feel dead: NO lead-list import → empty queue → "Dial next" no-ops.** (Plus: compliance gates are `pass=true` placeholders; prod-config gaps = Telnyx number-SEARCH key [buy "inventory unavailable"], live WebRTC for the session ["Connection issue"].)

**CORRECTED ORDER (Amin: "do option 1 THEN look at the actual sprint + do them in the correct order" → he chose "P0 import + real compliance gates"):**
- **✅ Slice 1 — Lead-list import backend — MERGED + CI-VERIFIED (#83, origin/main 18ecf49).** `POST /api/lists/:id/import` (+ GET/POST /api/lists): JSON rows → central E.164 normalize → validate → within-list dedupe (`(list_id,e164)` unique) + cross-list flag (DLR-010) → Prospect insert (INT-007), tenant-scoped RLS; import-warnings digest; **DNC scrub-on-import = honest pending** (no fabricated pass); reuses migration 0007 (no schema change); row cap `THRESHOLDS.lists.maxImportRows`; tz IANA-only. **Adversarial caught a REAL BLOCKER** (manager role 403'd — routes required `tenant` scope but manager holds `lists.manage`/`lists.import` at `team`; `scopeCovers('team','tenant')`=false; tests only ran as Owner so it hid) → fixed scope `tenant`→`team` on all 3 guards + added a manager RBAC test that bites. CI `integration` job GREEN (imported→`next()`-eligible + RBAC + tenant-isolation + dedupe + re-import-idempotency). Decisions: JSON rows not CSV-multipart (CSV parse = the UI slice's job); bounded-sync import not a worker (CRM-scale → INT-006 later).
- **✅ Slice 2 — Real compliance gates (Set B) MERGED + CI-VERIFIED (#85, origin/main 320ef94, 2026-06-19).** `complianceGate.ts` is the REAL deterministic Set-B gate now: calling-hours (prospect-TZ 8a–9p + per-state stricter via `shared/src/config/complianceGeo.ts`; CMP-012 power=hard-block; fail-closed on unresolvable TZ) / DNC-freshness (≤31d; null-or-stale → BLOCK) / recording-disclosure (all-party-consent state-map + PIPEDA + fail-safe-disclose-when-uncertain) + per-tenant config (migration `0013`) + honest audit (`*_pass=null` + `{enforced:false,reason:'tenant_disabled'}`, never fabricated) + **CMP-1 STIR audit-row backfill** (migration `0014` = tenant UPDATE policy + column-restricting BEFORE-UPDATE trigger; permits ONLY a null→value `stir_attestation` stamp; `callProjection` stamps it post-connect). **3 review catches fixed before merge:** (a) adversarial — disclosure failed OPEN on a non-null-but-UNRECOGNIZED state (messy import `"California"` not `"CA"`) → skip-disclosure is now keyed on a positively-recognized US state code (`countryFromStateCode===US_COUNTRY_CODE`), not the overridable `prospectCountry`; (b) CI live-DB — the `0014` immutability test was the bug, NOT the trigger (it UPDATEd a column to its SAME value → `IS DISTINCT FROM` correctly permits the idempotent no-op the STIR backfill relies on; trigger is correct + its column list exhaustive) → test flips to a genuinely-distinct value so the rejection branch fires; (c) CI live-DB blast-radius — the now-real gate fail-closed 5 pre-existing Sprint-1.2 dialer-engine fixtures (dial-serialize ×3 / dialer-engine-events / pause-button-atomicity) → fixtures now feed compliance-passing inputs (the real `dial({now})` seam + fresh `dncLastScrubbedAt`), gate unchanged (not gamed). **Lesson: the live-DB `integration` job is the load-bearing proof — adversarial+compliance-auditor "verified by reading" both MISSED the trigger-test bug + the fixture blast-radius; only CI's real Postgres caught them. Always gate the merge on `integration` green, never on a read-only review's "clean."**
- **✅ Audit remediation MERGED (#84, 2026-06-19) + AUDITOR CADENCE CODIFIED.** First-ever full-app baseline audits run (compliance-auditor + cybersecurity-auditor, per Amin "run them everywhere + make sure all auditors run at their stages") — both essentially clean; findings remediated: exploitable `ws` DoS 8.20.1→8.21.0 + `gate:audit` CI guard (high/critical prod vuln fails the build); auth rate-limiter now fail-CLOSED + `REDIS_URL` prod-required (SF-2); HSTS; `gate:lifecycle-write` scanner (a raw `numbers.status` write outside the `numberLifecycle` authority fails the build — CMP-2); `redis://` telemetry redaction. Hardening backlogged (`docs/BUG_BACKLOG.md`): **H-1 SPA CSP** (careful Report-Only pass), **H-3 Sentry beforeSend** (must land WITH any DSN), **H-4 import-owned existence check** (live Telnyx sub-account). **Cadence wired everywhere going forward:** global `~/.claude/rules/orchestrator-mode.md` "Auditor cadence" + project `.claude/rules/sprint-rigor.md` §10 closure-checklist + `.codex` parity — per-slice domain/security lens by what's touched + a whole-app sweep at sprint/phase close (a green `gates:all` is necessary but NOT sufficient).
- **✅ Slice 3 — Lead-import UI + cockpit list-picker — BUILT + MERGED to main (verified 2026-07-03).** The surfaces exist on origin/main: `frontend/src/pages/lists/{LeadImportWizard,LeadListsPage,StartDialingDialog}.tsx` (+ `leadImportView.ts`, `lists.css`, tests), last touched in the responsive §7 commit `6dcb95e` (2026-06-29, full-screen import wizard). No longer owed — CSV upload + preview + warnings digest + list picker shipped.
- Then: VM-drop, inline calendar, teleprompter render, battlecard trigger logic, 10DLC vetting, billing. **Step 3 (Conversations inbox + dock) is PAUSED** — don't add more frontend breadth until the dialer actually dials.

**KEY PRODUCT DECISIONS (Amin, 2026-06-16) — grounded in 3 REAL agent scripts at `${HOME|backslash}\OneDrive\Desktop\Scripts\` (POS_Script.pdf, Live_Call_Objection_Playbook.pdf) + `Downloads\Telegram Desktop\MINA_Will Kit...docx`:**
- **Teleprompter (BUX-005): KEEP + redesign** as a calm OPTIONAL guided-mode toggle (Amin had flagged he didn't recall approving it). **Authoring = PASTE** the agent's existing script (browser-native paste preserves bold/headings/lists) — **NO server-side PDF/Word parser** (unreliable: scanned PDFs have no text, columns mangle; docx tables/images; untrusted-upload security surface). Real scripts are **linear + sectioned + inline-branch-as-prose** ("if yes/if no/if still no"), ~4.5k chars — NOT formal node-trees. Reframe: render as **sectioned doc + section jump-bar + inline if-yes/if-no highlight + muted rep-cues ("Tone:", "(pause)") + merge-field auto-fill (Tier-1 lead data ONLY — ARC-006) + manual agent-paced scroll**. BUX-005 node-tree stays the STORAGE format; authoring = paste sectioned text (deviation from the approved node-tree authoring — flagged, not silent).
- **Battlecards (AI-003): Phase-1 = tenant-AUTHORED objection→rebuttal library** (real playbook: Universal + Referral/Union/POS/Willkit groups), keyword/tap **reveal-on-demand, NO auto-pop, NO AI in v1**. (My earlier "AI-suggested battlecards" framing was WRONG — corrected; matches AI-003 Phase-1=manual.)
- **NO AI in v1 for live-call scripts + battlecards** (agent-authored). `lineProvenance` enum (`your_playbook|manager_approved|suggested_response|ai_generated`) ships the first two; AI-suggested lines slot in later as a provenance that augments/replaces — zero rework.
- **Post-call AI disposition draft (AI-001/002): KEPT in v1** (headline differentiator BENCH-PRO-AI-DRAFT-DISPOSITION; never touches the live call; booker always confirms — ARC-006).
- **NEW REQUIREMENT — entitlement = 3-state visibility (Amin, 2026-06-16):** teleprompter + battlecards (premium add-ons) resolve to one of **`active` | `hidden` | `locked`** per tenant/team entitlement. **`hidden` is the DEFAULT for un-entitled** — the feature isn't rendered at all (no nag, nothing to dismiss; "a permanent locked item in your face gets annoying fast"); buying the add-on flips it to `active`. **`locked`** = an OPTIONAL upsell-teaser mode (gated panel + "upgrade") the platform switches on per plan/feature only when deliberately driving an upgrade. Backend entitlement resolver returns the visibility mode; frontend renders all three. Same model likely extends to the AI disposition draft (Amin floated paywalling it under one premium tier). Record in decision-log + sprint doc at closeout; mocks show all 3 states.
- **Mock format = HTML explorations** in `frontend/public/explorations/` (NOT the visualize widget — Amin's explicit instruction). Make them self-contained (inline _design.css tokens) so they open standalone.

**FOUNDATION SCOPING (Slice 0 brief decisions):** NO `campaigns` table in 1.3 (ai_battlecard_triggers.campaign_id = nullable plain-uuid seam, 0007 precedent; taxonomies tenant-default via is_default); `triggers_callback` = TAG-ONLY flag (no scheduling — ARC-006; reconciles sprint-doc vs data-model §393 contradiction); defer `vm_scripts`/`sms_templates` to 1.4; CONV-005 inbound VM uses new `voicemail_configs` (not the outbound vm_scripts library).

**⚠ CODEX-DISPATCH GOTCHAS (learned 2026-06-16 — see [[codex-claude-division-of-labor]]):**
1. **Never run `codex exec` through the Bash tool with `| Tee-Object`** — Tee-Object is a PowerShell cmdlet; bash → "command not found" exit 127, codex never runs.
2. **A positional prompt arg to `codex exec` in PowerShell HANGS on stdin** ("Reading additional input from stdin..." forever = the background-agent stall footgun). FIX: **pipe the prompt via a here-string** — `$prompt = @'...'@; $prompt | codex exec -s workspace-write -C "<wt>" -o "<report>.md"` via the **PowerShell tool**, run_in_background. Healthy start = "Reading prompt from stdin..." then the codex banner (session id, gpt-5.5). Harmless noise: Neon/Railway MCP auth errors at boot (codex proceeds without them).
3. `codex-cli 0.130.0` present; flags `-s workspace-write` `-C <dir>` `-o <last-message-file>`. Fresh worktree needs `npm ci` (orchestrator runs it) — node_modules isn't shared.

**Tooling:** `pdftotext -layout` available for PDF extraction; docx text via `[System.IO.Compression.ZipFile]` → `word/document.xml` → strip tags.

**⚙ A-PATH (real-dial backend) CONFIG STATE + 2026-06-20 SESSION:**
- **DNC drift — the doctrine-drift-auditor REVERSED the diagnosis (2026-06-20, its FIRST run; verified against code).** My earlier "the CODE is a Tier-1a authority leak" call was **WRONG**: `complianceGate.ts:292` returns `tenantDisabled()` (no block + honest `tenant_disabled` audit) when `dncScrubEnabled=false` (migration 0013 default ON) — that **capability-level disable IS the Tier-1b override**; and the enabled-path fail-closed (null/stale→block) is **affirmatively endorsed** by ADR-CMP-001 + CMP-005 ("stale=block when enabled") + CMP-010 ("fail-closed"). DNC has **no per-call override BY DESIGN** (ADR-CMP-012 §3: confirm/off may NEVER override a DNC hit — no per-call legal basis to be on a federal list). **So the code is doctrine-correct — and the auditor PREVENTED a wrong "fix" that would've stripped the ADR-mandated fail-closed = opened a real compliance hole.** The REAL drift is **doctrine-vs-doctrine** (lower docs state the freshness rule WITHOUT the tenant-disable carve-out → read as no-override Tier-1a, which is what made it LOOK like a leak + would let a real bug pass audit): **D1** project-rules §1 (both mirrors), **D2** compliance-auditor brief #4, **D3** security-doc §137 (also conflates Tier-1a on-list w/ Tier-1b freshness); + **D4** `complianceReasons.DNC_HIT` mis-tiered "(Set B)" + dead (queue-excluded at dialerEngine.ts:206, never gate-emitted), **D6** CMP-005/006 citation slip (thresholds.ts:93 / complianceGate.ts:280). **Fix slice (a, decided "do a now") = reconcile docs UP to the ADR + registry comments — NO code-behavior change.** **Sprint-close gate (Amin: "no drift anywhere on this authority leak before sprint done; do b at end if it makes sense"): D5** (missing `CALLING_HOURS_MANUAL_OVERRIDE` reason, ADR-CMP-012) + the Set-B wording sweep (calling-hours/recording-disclosure — auditor confirms BOTH are NOT code-drift: power calling-hours hard-block is ADR-CMP-012-settled; recording-disclosure is non-blocking + correct) + a clean full doctrine-drift authority-leak sweep. **One product Q for Amin:** safe-default-ON (current, ADR-CMP-001, matches incumbents) vs default-OFF — flipping = a fresh ADR change, not assumed. **→ RESOLVED 2026-06-20:** Amin chose **keep ON**; **(a) MERGED PR #88 (main b1fd805)** — D1–D4+D6 reconciled docs-to-ADR, NO behavior change (only comments in complianceGate/thresholds/complianceReasons). Remaining = **CMP-SETB-SWEEP-001** (sprint-close GATE: D5 `CALLING_HOURS_MANUAL_OVERRIDE` reason + calling-hours/recording-disclosure wording sweep + a clean full authority-leak `doctrine-drift-auditor` sweep before 1.3 closes; do (b) at sprint-end if it makes sense). DNC-scrub vendor research → **INT-DNC-PROVIDER-001** (DNC.com / Blacklist Alliance top picks; FTC SAN model = tenant is the liable seller → validates Tier-1b).
- **`doctrine-drift-auditor` created (PR #87) + cadence-wired** (see [[orchestrator-mode-setup]]) — born from the 3× drift recurrence; its first finding = the DNC contradiction above.
- **SENTRY_DSN PULLED ✅ (Amin, 2026-06-20)** — removed from api+worker via the dashboard (the MCP had auto-blocked me). H-3 PII-leak risk CLOSED. The H-3 `beforeSend` scrubber MUST land BEFORE any DSN is re-added (backlog).
- **WebRTC connid fix MERGED (#86, origin/main ae07205):** added `TELNYX_WEBRTC_CONNECTION_ID` = credential connection `2909569031675577469`, distinct from `TELNYX_CONNECTION_ID` = Call Control app `2909455785626961663` (dial). Mint uses webrtc-one; dial keeps app-one. **NO username/password needed from Amin** (ephemeral telephony_credentials + JWT, not static SIP creds — the console Auth-tab user/pass is unused; set anything if forced).
- **Railway prod var GAPS (names-only checked 2026-06-20, Amin to set):** `api` missing TELNYX_CONNECTION_ID, TELNYX_WEBRTC_CONNECTION_ID, STORAGE_ENDPOINT; `worker` missing all 4 STORAGE_* + both TELNYX connids. Values: app=`2909455785626961663`, webrtc=`2909569031675577469`, STORAGE_ENDPOINT=`https://ef5a7073cf8f726bea6944a71b017c9c.r2.cloudflarestorage.com`, STORAGE_BUCKET=`auxadial-storage`. TELNYX_WEBHOOK_SIGNING_SECRET ✓ already set; R2 creds verified working. Simplest: set the 2 connids + 4 storage on BOTH services.
- **Telnyx Messaging profile (future SMS), banked:** "Auxadial Messaging" id `40019cc2-28f1-4ae7-83e0-9d72fe7472f9` (API V2), US+CA, sender +16047429198 (= the voice DID). **inbound webhook MISMATCH FIXED ✅ (Amin, 2026-06-20)** — profile URL now matches our route `/api/webhooks/telnyx/sms` (`API_ENDPOINTS.webhooksTelnyxSms`). (US SMS still needs 10DLC; Canada works now.)
- **⚠⚠ RECORDING-DISCLOSURE = REAL BLOCKING COMPLIANCE BUG (CMP-DISCLOSURE-001; found 2026-06-20 by the deep-dive auditor Amin asked me to run; VERIFIED by me in `bridgeOrchestrator.ts`).** `bridgeOrchestrator.ts:53-54` calls `recordStart(...,'dual')` **unconditionally** on every connected call — nothing reads the gate's `recordingDisclosure.required` (read only by `complianceAuditWriter`), **no disclosure AUDIO plays anywhere**, no fail-closed guard, and `recordingEnabled` is checked only at FINALIZE (`recordingFinalize.ts:138`), not before recordStart. → a real call in an **all-party-consent state (CA/IL/PA/WA/+10) or ANY Canadian call (PIPEDA) RECORDS WITHOUT DISCLOSURE = illegal.** The gate's disclosure COMPUTATION is doctrine-perfect (non-blocking, fail-safe-when-uncertain, PIPEDA, honest audit `played=null`) — the bug is that the ACTING layer never consumes the verdict. NOT live-violating yet (no real calls; Railway config incomplete) but bites the **first real all-party/Canada call — incl. the imminent test call if it records to/from the Canadian DID**. **Fix:** interim fail-closed = record ONLY when `recordingEnabled && !recordingDisclosureRequired` (don't record in all-party/Canada until disclosure-audio is wired) + honor REC-008 at recordStart; proper build = wire disclosure-audio playback BEFORE recordStart + persist `recording_disclosure_played` + emit `DISCLOSURE_PLAYED` (callProjection.ts:45 slot exists). **Product tradeoff (Amin's call, PENDING):** interim = no recording (→ no transcript/AI-draft/CoachAI) in 14 all-party states + Canada until disclosure-audio lands → ship-interim-now + build-disclosure-audio-next, OR build-disclosure-audio directly. **CALLING-HOURS deep-dive = CLEAN** (code doctrine-correct; only D5 backlogged). **Lesson (doctrine-loop): auditing a gate's COMPUTATION isn't enough — must trace whether the verdict is ACTED ON; the first note-only pass stopped at the gate.**
- **⚙ STATUS @ end of 2026-06-20 session (DNC + recording + Railway):** (1) **DNC pre-provider provider-gated allow MERGED (#89, main 1f2e602)** — DNC-default-ON no longer blocks every dial; with no provider it allows + records audit-only `dnc_provider_unavailable` (NO UI nag — Kixie add-on model; research: EBR/opt-in exemption + Genesys block-only-when-configured). `DNC_SCRUB_DRIVER='none'` default + `dncScrubProviderConfigured` gate input. Keep-default-ON (Amin). Block/warn tenant mode (`dnc_scrub_mode`, Kixie-style) DEFERRED → INT-DNC-PROVIDER-001 (inert until a provider exists). Built by sprint-implementer; doctrine-drift + compliance auditors BOTH clean; CI integration green. (2) **Recording disclosure (CMP-DISCLOSURE-001): Amin chose A (interim fail-closed: record ONLY when `recordingEnabled && !disclosureRequired`) NOW for the smoke test, then B (disclosure-audio playback, restores Canada recording) later — he needs Canada recording. A is NOT yet built → it is the NEXT build before the smoke test.** (3) **Calling-hours deep-dive auditor = CLEAN** (code ADR-correct; only D5 `CALLING_HOURS_MANUAL_OVERRIDE` reason backlogged in CMP-SETB-SWEEP-001). (4) **Railway prod = ALL vars set + verified** (api + worker have the Telnyx connids + STORAGE_*; SENTRY_DSN intentionally absent) — config DONE. (5) **NEXT: build recording-A → smoke test (dial→bridge→connect on real default config; A makes a Canadian test call connect without recording-without-disclosure) → then B + the DNC provider integration later.**
- **✅ DONE end-of-session 2026-06-20:** (1) **CI Actions-minutes dedupe MERGED (#90, main)** — `ci.yml` now triggers on `pull_request` + `push:main` only (was double-running on `push:['**']` + PR); ~halves Actions minutes, no coverage lost (Amin hit 90% of the plan). Further levers offered (path-filter docs-only PRs; Pro/public-repo) — not done, his call. (2) **Recording-disclosure fail-closed A MERGED (#91, main 9febaae)** — `bridgeOrchestrator` records ONLY when `recordingEnabled && !recordingDisclosureRequired` (reads tenant + the gate's persisted verdict via an injectable `defaultLoadRecordingDecision`/withTenant); bridge always connects; fail-safe = don't-record-when-unknown; REC-008 honored at capture. **B (disclosure-audio playback → restores all-party/Canada recording) is the named follow-up in CMP-DISCLOSURE-001.** doctrine-drift + compliance auditors BOTH clean; the §4.1 gap closed with `recording-decision-db.test.ts` (non-mocked resolver) — **CI integration caught a real FK-fixture bug (P2003: compliance_audit_log.call_id→calls) which I fixed (the resolver itself was correct) — proof the live-DB gate works.** Sibling `voicemailFallback.ts:101` (inbound-VM unconditional recordStart) flagged in CMP-DISCLOSURE-001 for B/counsel. **→ BACKEND IS NOW READY FOR THE LIVE SMOKE TEST** (DNC allows pre-provider, recording gated by A so a Canadian test call connects without record-without-disclosure, Railway vars all set). Remaining = Amin's Telnyx-console items — the outbound-profile **Record → "Do Not Record" is REQUIRED, not cosmetic**: A gates OUR explicit `recordStart`, but if Telnyx-side auto-record is ON it records regardless + bypasses A; verify it's OFF so our A-gated recordStart is the SOLE record authority. Then place the test call.
- **SMOKE-TEST SEED (2026-06-20, Neon proj `still-sky-26160948`, tenant `4d552390` = amin.sharif89; RLS owner-bypassed so MCP run_sql sees rows):** manually seeded list `a0000000-0000-4000-8000-000000000001` + prospect `+17787454278` (BC/Vancouver) + DID `+16047429198` (active/dialing/assigned-to-Amin/604 — eligible + a 604↔778 local-presence match) + `calling_hours_enforced=false` on his tenant (his standing ask: off until a config UI exists — I did NOT change the system/migration default, which stays safe-default-ON per ADR-CMP-001; flagged the default-flip as a tracked compliance change if he wants it). **Deleted the empty `amin@nuvoralink` tenant `aef3798c`** (cascade; he'll recreate as admin at admin-self-serve). **THE COCKPIT IS FIXTURE-DRIVEN for the queue rail / shift-stats / caller-ID dropdown** (`frontend/src/pages/softphone/cockpitFixtures.ts` — the 555-01xx PLACEHOLDER_*); the **DIAL IS REAL** (`api.calls.dial({listId})` → `dialerEngine.dial` picks the DID server-side via NUM-006; the dropdown is cosmetic). **BLOCKING the dial:** `VITE_DEV_LIST_ID` (build-time env) must be added to the `auxara-dialer-web` Vercel project (Production = the list id) + redeploy — the CLI env-add was AUTO-BLOCKED as a prod-config change, so Amin's doing it in-dashboard (CLI IS authed as `nuvoralink`, team `nuvoralinks-projects`, if he later approves). The env var is the dev list-id stopgap; the real flow = upload list → list-picker UI (Slice C / S1.3 campaigns-lists) passes the selected list id → env var goes away. **Filed `ONBOARD-PROVISION-001`** (BUG_BACKLOG): a new tenant has no number/list → unusable out-of-box (this is WHY the smoke test needed manual seeding); needs auto-first-DID-at-signup + assign-number-at-user-creation (Dialpad model) — embedded **billing decision** (auto-buy a first DID = a Telnyx cost per signup) is Amin's call before build.
- **LIVE SMOKE-TEST DIAL DEBUG (2026-06-20) — 3 bugs found in the chain, each PROVEN not guessed (verify-don't-assume saved me 3×: wrong on authority-leak, wrong on sip-pref, wrong on TELNYX_CONNECTION_ID — Amin caught that one):** (1) **Stuck pause flag** — `dial` 409'd; `dialsession:<tenant>:<booker>:paused` (Redis, NO TTL) was set + **the cockpit has a Pause button but NO Resume wiring** (`api.calls.resume` + backend exist; frontend never calls it) → permanent lockout. Cleared via a browser-console `POST /api/calls/resume` (Amin's session). **FOLLOW-UP: wire a Resume button** (real bug). (2) **`client_state` base64url → Telnyx 422 code 90017** ("must be valid base64") on EVERY live dial — `encodeClientState` used `.toString('base64url')`; Telnyx needs STANDARD base64. **FIXED + LIVE: PR #92 `c3fe7c8` (base64url→base64); redeploy `c265b282` SUCCESS; /api/ready 200.** Proven by direct Telnyx repro (base64url→422, base64→call placed). Surfaced now b/c the ADR-DLR-011 A2 "live proof" ran via Telnyx's OWN test page, never our dial code (journey L11). (3) **Neon cold-start broke the FIRST auto-deploy** — `migrate:deploy` (preDeployCommand) `P1001 can't reach DB` because Neon scale-to-zero was asleep; redeploy after a warm-up `SELECT 1` succeeded. **FOLLOW-UP: make `migrateDeploy.ts` retry on P1001** (cold-start tolerance) so deploys don't fail on a sleeping Neon — SAME root as Amin's "why so long to connect" (Neon cold-start adds ~3-5s to first queries; lever = Neon autosuspend setting, costs more, Amin's call). **Railway env (TELNYX_CONNECTION_ID=2909455785626961663, WEBRTC=2909569031675577469) confirmed CORRECT — do NOT change.** After #92 Amin re-dialed: dial succeeded (client_state fix CONFIRMED — `telnyx_call_control_metered dial ok count:1`), the AGENT leg reached his browser, but **his phone never rang**. (4) **BRIDGE — agent leg never auto-answered → prospect leg never dialed.** Webhook timeline: 2× `call.initiated` (200) → 36s silence (NO `call.answered`) → 40s `first_command_timeout` → ENDED. Cause: `useWebRTC.wireCall` only answered when `call.direction === 'inbound'` (+ only on the first notification) — too strict; Model A2 browser ONLY receives, so every call IS the agent leg. **FIXED + LIVE: PR #93 `dbfc1fe` — answer UNCONDITIONALLY + `console.info('[webrtc] agent leg received → answering',{direction,state})` diagnostic; Vercel prod Ready.** Also seen (FOLLOW-UPS, dual-leg cleanup, NOT yet fixed — secondary, don't block prospect dial): the on-net SIP agent leg trips the **inbound-VM fallback** (`INBOUND_NUMBER_UNKNOWN` on leg B, the credential's inbound side — fallback bailed) + Telnyx **hangup webhooks for leg B 400** (`call_binding`, no client_state → Telnyx retries; handler should ack-200 unbindable-but-valid-signature events). **NEXT: Amin HARD-refreshes dialer.auxara.io → re-dials → watch console for `[webrtc] agent leg received → answering` (confirms the fix fires + shows the SDK's real direction) → his phone (+17787454278) should ring → answer → bridge (two-way audio). If a NEW chain error appears (prospect-leg dial / bridge / recording) debug from there. Open code follow-ups: Resume button, migrate P1001 retry, dual-leg webhook cleanup (inbound-fallback + 400→200).**
- **✅ BRIDGE WORKS + LATENCY + COCKPIT REDESIGN SHIPPED (2026-06-20, end of session):** (a) after #93 Amin re-dialed → **CONNECTED, two-way audio both ends** — full chain PROVEN (dial→agent-leg→browser-auto-answer→prospect-leg→bridge). Minor follow-up: no ringback tone on laptop while dialing. (b) **Latency:** Amin moved the WHOLE Railway cell (api+worker+Redis) to **us-east4** (next to Neon us-east-1) → noticeably faster. **ADR-INF-012 recorded** (one co-located cell now → regional cells at scale; NOT multi-region API replicas — they'd share the one US DB → worsen the write-chatty dial path; Telnyx edge handles call audio region-independently). Remaining lever = Neon-warm (Amin said leave Neon for now). (c) **COCKPIT density redesign SHIPPED to prod (#95 `80360dc`):** the connected stage was a FIXED-HEIGHT box (`--height-cockpit-connected`, now REMOVED) nested-scrolling the script → now FLOWS into the AppShell scroll; + tighter top bar, one-line on-call header, smaller script chrome (green IF-YES box → small inline cue), smaller controls, **in-call quick-tags REMOVED** (disposition wrap-up only), mock `softphone.html` reconciled to match build; Jump-to → `scrollIntoView` (flows, reduced-motion); `cockpit-layout.test.ts` now guards width AND height drift. Built by the `implementer` agent which **DIED mid-run (process exit lost in-process state) — but its uncommitted working-tree edits SURVIVED**; I salvaged + fixed 2 stale Jump-to tests + verified 56/56 + gates green + merged. **LESSON: a dead background agent's working-tree edits survive — check `git status`/branch before assuming lost.** Amin reviews on **PROD (hard-refresh)** — **revertible via `git revert`**. ⚠ **PREVIEW-CORS GOTCHA:** can't review the logged-in cockpit on a Vercel PREVIEW URL — the api ALLOWED_ORIGINS only allows the prod origin (per-deploy preview URLs fail CORS — BUG_BACKLOG Sprint-0.1 note); so cockpit reviews are on prod. **NO-WIDGET preference saved → [[no-widget-mockups]].**
- **✅ AUTONOMOUS CONTINUATION (2026-06-20, Amin out — "build the next step; reach me on Telegram; write a webhook so you can receive my answers"):** (1) **Telegram 2-way ops channel LIVE + verified end-to-end (#96, main 5e90150).** The bot already SENT deploy alerts; I built the RECEIVE side → [[telegram-ops-channel]] for how it works + how to reach Amin async. (2) **Migrate cold-start retry SHIPPED (#97, main 77c4608)** — the [[migrate-on-deploy-live]] P1001 follow-up is DONE: `migrateDeploy.ts` retries a connection error only (5×, 3/6/9/12s; real migration errors fail fast); `lib/migrationRetry.ts` + `migrationRetry.test.ts` pin the classifier. Deploys now survive a sleeping Neon. (3) Cockpit redesign (#95) still awaiting Amin's prod review. **OPEN code follow-ups now: Resume button (real bug — Pause has no Resume wiring), dual-leg webhook cleanup (inbound-fallback + 400→200 ack), recording-disclosure-B (audio playback → restores all-party/Canada recording, CMP-DISCLOSURE-001), ringback tone.** ~~**NEXT VISIBLE PRODUCT STEP = the LEAD-IMPORT UI**~~ — **SUPERSEDED: this was built + merged** (`frontend/src/pages/lists/LeadImportWizard.tsx`, s14-lead-import `ae4eac3`, later the full-screen wizard `6dcb95e`); see the 2026-06-28 recalibration note above (line ~12) which first corrected this. This 2026-06-20 "next" note is historical.
- **✅ COCKPIT REDESIGN — mock iteration (2026-06-21, design review + sentiment audit).** Iterated the connected stage through `frontend/public/explorations/s14-cockpit-*.html`: swap → onescreen → **tuned** (FULL/guided: script + objections + left rail) → **streamlined** (seasoned-agent: script/objections OFF via a per-agent Settings toggle × tenant entitlement). **Streamlined is approved-for-now (Amin):** two content-sized glass cards (LEFT = the call — identity + Tier-2 lead-details [Lead type/DOB/Spouse/Email/Address, from tenant CSV] + control row with red hang-up IN the strip; RIGHT = Up-next + Recent-activity), cleaned copy (dropped "LIVE" [redundant w/ Connected], "Open booking link" not narrated, no "Booking with"/closer-subtitle, closer = avatar-only left of the Book/Notes tabs, no divider lines). All capabilities sequenced in `docs/BUG_BACKLOG.md` → COCKPIT-CAPABILITIES-SEQUENCE-001 + audit `docs/app-plan/product/32-power-dialer-sentiment-audit.md` (both on the `s14-cockpit-swap-mock` branch); lead-data boundary refinement in [[dialer-not-crm-design-boundary]]. **OPEN THREADS:** (1) **responsive / different-screen-sizes pass = a DEDICATED future session** (Amin's explicit defer — basic breakpoints exist in the streamlined mock [stack cards <980px, stack call-body <640px, hide ticker] but need real tuning across sizes); (2) apply the same cleanups to the FULL/guided (tuned) view for consistency; (3) the actual React `CallCard.tsx` build (PENDING Amin's greenlight — he hasn't said "build" yet); (4) the ringing-stage rich-lead pass (lead context shown DURING the dial — audit's #1 surface). **Mock workflow is now localhost-not-push** (see [[no-widget-mockups]]).
- **✅ COCKPIT v2 (Runs 1+2) MERGED → prod (PR #99, 2026-06-22; CI green incl. `integration`/backend-DB after Amin fixed a GitHub-Actions billing block that 2s-failed all jobs).** Both s14 mocks LOCKED (commit 1cb9dce). **R1 GUIDED**: existing `CallCard` connected stage → `s14-cockpit-tuned` (top-bar ticker Queued·Deferred·Dialed·Answered·Connected·Talk-time [fixture-backed til shift-stats agg]; rail This-shift·Up-next·Recent-activity; red rec-dot beside Connected; no 'On the call'; call-head [identity+lead-details tenant-CSV tall-left | avatar+Book/Notes beside name]; objections NO-search; pinned controls+red hang-up in-strip; script right). **DECISION: one-screen LOCKED-COCKPIT fit (block-size:100%+flex+ScrollArea) SUPERSEDES #95 'flows-into-AppShell'** (s14 IS one-screen; layout-and-ia §1; `cockpit-layout.test.ts` guards it). BookPane→single 'Open booking link' (ADR-ADM-006 visible override DROPPED per the simplified mock; resolution stays backend; a visible 'change link' is a future add if Amin wants). **R2 STREAMLINED+TOGGLE**: two-card `s14-cockpit-streamlined` + per-agent guided⇄streamlined toggle persisted `User.cockpitView` (migration 0015)+`/api/me/cockpit-view` (mirrors hotkeys, self-scoped)+`shared/taxonomy/cockpitView.ts` (REUSED a pre-existing untracked stub)+REQ-BUX-010/BUX-017; ONE shared component set (ConnectedStatusRow/ConnectedCallHead/ConnectedControls/RailUpNext/RailActivity) both views. Adversarial ACCEPT both runs; fixed BookPane-chrome, taxonomy-unexported(dead), wrong-Proves-ID, stale-filemap, + an R1 guided 'Up next' fidelity gap (old `<ol>` vs mock qnow/qpeek/qmore). **OPEN: (1) toggle home = cockpit topbar INTERIM → formal = future Settings page (mockup-first); (2) entitlement active/hidden/locked DEFERRED (needs billing infra); (3) responsive narrow-viewport collapse DEFERRED (no breakpoint tokens — same as guided); (4) Amin's PROD review pending — the one-screen FIT is the headless-unverified visual gate (revertible via git revert). Doctrine-loop: `gate:test-intent` can't catch a resolving-but-wrong Proves ID (REQ-BUX-009 mis-stamped across ~5 cockpit test files) → candidate: accept decision-log IDs or require minting a REQ. HEAD 9ac7170 on s14-cockpit-swap-mock (rebased onto main).**
- **✅ COCKPIT-FIX PR #100 MERGED → prod (2026-06-22, from Amin's prod review): guided scroll/height clip + dark too-black + Calendly slow.** (1) SCROLL [blocker]: `.cockpit__card-body` was padding-only → the connected `.cockpit__2col` flex:1/min-block-size:0 was inert → overflowed the card's overflow:hidden → inner ScrollAreas unbounded → controls+script clipped, NO scroll. Fix: `.cockpit__card-body:has(.cockpit__2col){display:flex;flex-direction:column;flex:1;min-block-size:0}` (scoped — non-connected untouched); mutation-proven test. (2) DARK too-black: token-USAGE swaps (NO value changes) — ticker/rail-card/up-next near-black `--surface-sunken`/`--surface-solid`→`--surface-raised`; tool/open-scheduler/pause flat `--shadow-soft`→tactile `--shadow-raised`; hangup `--btn-ring:--danger-ring`; shared `.btn--ghost` untouched; applies BOTH light+dark (source fix — Amin to glance at light). (3) Calendly: widget.js already async; added preconnect/dns-prefetch for calendly.com+assets.calendly.com (the iframe cold-connects on click). Amin re-checks on prod.
- **⏳ NEXT (Amin: "merge 100 then work on the rest") — DIAL/SHIFT-LIFECYCLE ROBUSTNESS cluster (branch `s14-dial-lifecycle` off main):** (A) **device-pending sticky + no-retry**: `SoftphonePage.startDial` (~L318) sets `DEVICE_CHECK_PENDING` ("Still setting up your devices") when `measurements==null`, but nothing clears it / auto-retries when measurements arrive → label sticks, must re-press Dial. FIX: clear on measurements-ready + auto-proceed a queued dial intent + reflect "setting up" on the Dial affordance. (B) **connection-error no-recovery**: `useWebRTC` `setStatus('error')` (~L235/311/348) is a dead-end (the connect effect only re-runs on an `enabled` change = page refresh); `actions.reset` resets the CALL not the connection. FIX: auto-retry the WebRTC connection on error (bounded) + make "Reset phone" re-establish (re-run start()). (C) **Pause has no Resume**: `savePause`→`api.calls.pause()`; no resume call (api.calls.resume + backend EXIST) → stuck after pause. FIX: wire a working Resume affordance. (D) **End shift UNWIRED**: `SoftphonePage:578` `<Button data-testid=softphone-end-shift>` has NO onClick (placeholder). "Pause dialing" (rail→api.calls.pause) IS the stop-dialing control; End shift is meant to end the SESSION. FIX: wire End shift to end-session (pause + return to pre-shift/not-on-shift) OR flag if it needs a model/product call (Amin didn't pick wire-vs-remove; default = wire per my recommendation). All FRONTEND → Claude implementer, NOT Codex.
  - **STATUS 2026-06-22: MERGED → prod (PR #101) — implemented + adversarial-hardened + a folded-in idle-void fix Amin flagged on prod (the non-connected cards — idle/dialing/ringing/wrap-up/inbound/paused/ended — rendered as a FULL-HEIGHT empty glass card because `.cockpit__card{flex:1}` filled the viewport in EVERY state; scoped `flex:1`→`.cockpit__card:has(.cockpit__2col)`, mirroring the #100 card-body fix, so only connected/reconnecting fills + non-connected are content-sized; mutation-verified both ways). ALL cockpit work now on prod: #99 (v2 guided+streamlined+toggle), #100 (scroll/dark/Calendly), #101 (dial-lifecycle + idle-void), #102 (rail scroll-fade top-card clearance via symmetric `padding-block` + `--width-cockpit-lead: 44rem` cap on non-connected cards, left-aligned — FIRST-PASS width, Amin to tune on prod). DOCTRINE-LOOP (Arm 1): the `format:check` gap bit the implementer TWICE (#100, #102 — it ran a hand-picked gate subset, not the full `npm run verify`, so an unformatted token hit a RED CI `verify` both times; the guarded merge — `gh pr checks` before the merge, NOT the watcher's exit code which only means "completed" — caught it both times) → tightened `~/.claude/agents/implementer.md` to REQUIRE the full aggregate `npm run verify` before reporting (minus the heavy DB suite the orchestrator owns), not a subset.**
- **✅ CARD-LAYOUT RESOLVED (#103, 2026-06-22) — the recurring "idle card looks wrong" — GROUNDED (Amin frustrated: "why do you keep fucking up this card, check the approved mockup").** Root: I kept GUESSING the non-connected card (content-size, then an invented 44rem cap) instead of reading the approved mock. A grounding workflow read the ACTUAL reference: **`softphone.html` is the authoritative idle/non-connected mock** (the s14 tuned/streamlined mocks are CONNECTED-only). GROUND TRUTH for idle/dialing/ringing/wrap-up/inbound/paused/ended: **FULL main-column width** (NO cap — `--width-cockpit-lead: 44rem` was INVENTED, now deleted) + **content-sized + top-aligned** (`softphone.html:41 .sp { grid-template-columns:280px 1fr; align-items:start }`). The "height mismatch" was `.cockpit__layout { align-items: stretch }` stretching BOTH columns (rail fills tall, short card sits in a stretched-tall column) → FIX: `.cockpit__layout { align-items: start }` (non-connected content-sized/top-aligned) + `.cockpit__layout:has(.cockpit__2col){ align-items: stretch }` (connected one-screen fit ONLY). **LESSON (hard): for a LOCKED surface, READ the mock FIRST — don't guess. softphone.html = idle/non-connected authority; s14 = connected only. I guessed 3× before grounding; Amin had to force "read the rules/docs, start fresh."**
- **✅ CANCEL-BRIDGE FIX — MERGED → prod (PR #104, 2026-06-22; all 3 auditors ACCEPT). Serious ARC-006; Amin found it: "i cancelled the dial but my phone still rang + connected, the bridge is not dropping".** Diagnosed by the grounding workflow: **Cancel is LOCAL-ONLY** — `SoftphonePage.tsx:414` cancel = `hangup()` (the agent WebRTC leg, often `null` mid-ring) + `dispatch({type:'CANCEL'})` (UI→idle); it NEVER calls the server (no `api.calls.cancel`). The bridge is fully webhook-driven server-side: agent leg answers (browser auto-answers UNCONDITIONALLY `useWebRTC.ts:307`) → `bridgeOrchestrator` dials the PROSPECT leg (`:131`) → bridges (`:142`) → uninvited connect after Cancel. **FIX (cancelBridge finding):** persist `agentCallControlId`/`prospectCallControlId` + a `canceling` flag on the `calls` row (migration; schema has NO `call_control_id` today); add `POST /api/calls/:id/cancel` wiring the ORPHANED `dialerEngine.cancel` (`:660`, unused) to `client.hangup` BOTH legs; **race-guard** in `bridgeOrchestrator.onLegAnswered` — check `canceling` BEFORE `dialProspectLeg` + BEFORE `bridge`, hang up the just-answered leg + return; frontend `cancel` calls the new endpoint + the browser auto-answer short-circuits when cancelled. Idempotent (set-flag + Telnyx hangup 404-tolerant). **BUILT (commit c32e465) + AUDITED via the `cancel-teardown-audit` workflow — compliance CLEAN (ARC-006 holds across all 3 race timings; tenant-isolated; Telnyx ENDED stays the call-state authority), cybersecurity CLEAN (RBAC+tenant-404+rate-limit; leg-ids don't leak), adversarial ACCEPT (race tests bite — mutation-verified) — ALL ACCEPT, NO blockers. The one should-fix (a comment over-claiming the TOCTOU "fully closed") fixed (731af5d): an irreducible sub-ms window resolves to ring-then-drop, never a sustained connect (no Telnyx atomic exists; documented honestly in the migration + orchestrator). Backlog `CANCEL-TEARDOWN-HARDENING-001` (Low: defaultIsCanceling DB-error consistency; hardening: cancel rate-limit bucket + console.info→logClientDiagnostic). MERGED PR #104 → prod (migration 0016 applies on deploy). Amin verifies: dial → cancel mid-ring → prospect must NOT ring/connect.** LESSON: critical telephony/ARC-006 slices get the full 3-auditor cadence (compliance+security+adversarial, parallel workflow) before merge — a green `gates:all` is necessary but not sufficient.** Built A/B/C/D (session/shift modeled as orthogonal local state, like the connection dimension). **Adversarial caught 2 REAL pre-merge BLOCKERS:** (1) ARC-006 — a QUEUED dial (pressed while devices setting-up) was NOT consumed by End-shift/Pause → after End-shift→restart (or Pause→Resume via the `useMemo([…paused])` startDial recreation) the auto-fire re-fired the CANCELLED dial = an uninvited call, masked only by the server pause (which fails-OPEN on Redis outage). FIX: endShift + savePause both clear `pendingDialRef`. (2) End-shift paused the server (no TTL) but restart never resumed → booker stranded (DIAL_PAUSED, no Resume card). FIX: onStartShift resumes; startDial's DIAL_PAUSED catch sets `paused=true` so the Resume card recovers ANY divergence. + should-fixes: WebRTC retry budget resets only after a HEALTHY register (min-uptime debounce `connectHealthyUptimeMs=30s`; was resetting every register → flap-storm); honest topbar (Shift-ended badge, End-shift hidden when ended). Each fix mutation-verified; gates green. Amin chose **merge-as-is** (the 3 new states — Setting-up Dial / paused card / Start-shift surface — ship; revertible; mockup-first waived for these recovery states). **DOCTRINE-LOOP (Arm 1) — 2nd instance of a class: a frontend lifecycle flag drifts from its server-authoritative twin when the transition isn't traced BOTH ways. Rule/checklist candidate (sprint-rigor / product-first-planning): for any session-lifecycle control (pause/resume/end/start) mirroring a no-TTL server claim, prove the round-trip both directions + test dial-guard reachability after each transition (Redis up AND down).**
- **✅ COCKPIT S15 REDESIGN MOCK — Claude-design handoff reconciled to our data (2026-06-24).** Amin had claude.ai/design redesign the cockpit+workspace; I imported the handoff, ground-truthed every field against `cockpitFixtures.ts` + `lists.ts`, adversarial-cut the unbacked extras, and rebuilt it as **`frontend/public/explorations/s15-cockpit/`** (one stage-switchable file: idle→ringing→connected·guided→connected·streamlined→wrap-up→didn't-connect, on the design-synced token bundle ≈ our real `frontend/src/design-system/tokens/*`). **STATUS = NEW LOCKED DIRECTION (Amin's call): supersedes the connected-only s14 mocks (`s14-cockpit-tuned/streamlined`); once approved the React `CallCard` build follows it.** Mockup-first → **PENDING Amin's review** (open `…/s15-cockpit/index.html` standalone or via dev server `/explorations/s15-cockpit/index.html`). **Adversarial CUTS:** external CRM/marketing timeline rows (web-form / email-opened — outside `ActivityKind`, ARC-006 Tier-3); appointment **"goal 12 · 50%"** target; **"Attempt 2 of 4"**→"Attempt 2" (cap=cadence/Tier-3); the **cockpit-energy FAB+slider** (live glow now the fixed `--shadow-live`). **KEPT per Amin:** the lead-details panel (tenant-CSV `displayFields` — NOT a CRM violation; the [[dialer-not-crm-design-boundary]] moved 2026-06-21), the weather cue (placeholder), and the **KPI deltas + sparkline trend** — Amin LIKES them. **→ 1.3 BUILD ITEM (Amin's ask): build the hourly-trend aggregation backend for the shift ticker** so the deltas/sparkline are real (only `dialed` has a real counter today; rest are `PLACEHOLDER_SHIFT_STATS`). **Mock-only scaffolding (NOT in React):** the stage switcher + KPI-trend/Script-size toggles (relocated into the stage bar). **BUILD-NOTE (real):** the cockpit lead-details must render the GENERIC `displayFields` `{label,value}[]` (cap 24, tenant-ordered), NOT the fixed `leadType|dob|spouse|email|address` enum the fixture hardcodes. Battlecard categories realigned to ours (Universal/Will-Kit/Union/POS/Referral). Full real/placeholder/cut table + build-notes in `…/s15-cockpit/README.md`. Both items filed in `docs/BUG_BACKLOG.md`.
- **✅ COCKPIT S15 OPERATIONAL EVOLUTION — APPROVED + LOCKED + BACKEND BUILD UNDERWAY (2026-06-25).** Amin sent a FULLER CD handoff (`Power Dialer Cockpit`, full bundle extracted to `${HOME|backslash}\Downloads\_handoff\`); re-audited (the first `…/s15-cockpit/index.html` was built from a TRUNCATED export → missed pieces) + fixed the translation per his feedback: **deferred = header chip + slide-out tray** (NOT a right-column card); **Notes panel** shown (prior notes + autosave; streamlined right-col / guided Notes tab); **dispositions FLAT — NO categories** ("completion is backend, don't verbalize"); **VM-detected cue + template pickers on BOTH Drop-voicemail & Send-text**; **distinct Auto-logged stage**; **both guided + streamlined kept** (BUX-017, his call — the handoff's calm connected = the streamlined view). Promoted into the locked `s15-cockpit/index.html` (live-verified DOM). **5 NEW DECISIONS logged + propagated:** DLR-012 (terminal-disposition completion), DLR-013 (auto-dispo `disposition_source=system`), DLR-014 (AMD passive signal — **ADR-DLR-014, scoped partial reversal of the DLR-005 AMD scrap**; predictive/parallel/AMD-to-bridge STAY scrapped), DLR-015 (in-session Skip/Defer + persistent deferred tray), BUX-018 (per-lead notes). **ARC-006 CLARIFIED (the over-correction Amin flagged): operational dial-state IS the dialer's OWN job (not Tier-3) — only strategic lead-lifecycle/system-of-record stays Tier-3** → propagated to both always-on `authority-boundary.md` mirrors + ADR-ARC-006 + the assurance companion + PRD (REQ-DLR-006/007/008/009 + REQ-BUX-011) + feature-scope + glossary + user-flows + 3 assurance maps. **BACKEND BUILD = SEQUENTIAL, each DB-verified + audited + committed on `s15-cockpit-redesign` (Amin: "do sequential, each done properly before the next"):** 63fda9d (lock+decisions), 1998394 (doc-propagation), **F1 ✅ 1fa8531** (migration **0018**: `dispositions.triggers_completion`, `prospects.completed_at/deferred_until/defer_reason`, `calls.note/amd_result` + contracts + `shared/src/taxonomy/amdResult.ts`), **F2 ✅ effd365** (DLR-012: dispositionSave NULL-guarded idempotent completion-write + dialerEngine `completedAt:null` queue filter; DB-green 547 tests + doctrine-drift CLEAN), **F3 ✅ 17aa45a** (DLR-015: new `routes/prospects.ts` + `services/prospectQueueState.ts` defer/skip/call-now/tray + queue defer-skip; DB-green 553 tests + cybersecurity CLEAN). **Migration 0018 is AUTHOR-ONLY** (applies on deploy/merge; the test-runner applies it to a fresh local test DB per slice). **NEXT (same per-slice loop — implementer build [fast-checks; DB tests written-not-run] → I review diff + test-runner DB suite + relevant auditor → commit):** **F4 = DLR-013 auto-disposition** (no-contact Telnyx result → `disposition_source=system` in callProjection/dispositionSave; booker>system; non-terminal→recycles) → F5 = DLR-014 AMD (premium AMD webhook → `call_events` signal + `calls.amd_result` + AI-008 metering; **READ THE FULL TELNYX AMD DOCS first**) → F6 = BUX-018 notes (`calls.note` autosave + LeadNotesDTO aggregation) → **FE1 = React cockpit** (`SoftphonePage`/`CallCard` → the locked s15 mock; Claude implementer, NOT Codex). **THEN MERGE GATE (human-gated, irreversible): apply 0018 + full `verify` + DB integration suite + COMPREHENSIVE compliance + cybersecurity + doctrine-drift + adversarial sweep over the assembled F1–FE1 diff → PR-merge to main (0018 applies to prod on deploy).** **Carried non-blocking hardening (do at the merge gate):** F3 GET-tray rate-limit symmetry; `deferReason` treat-as-untrusted in any future non-escaping consumer (CSV export / CoachAI / email digest). The CD handoff raw blob `frontend/public/explorations/Power Dialer Cockpit.html` is left UNTRACKED (261k, superseded by `Downloads/_handoff/`) — offered to remove. See [[dialer-not-crm-design-boundary]] (operational-dial-state axis), [[design-system-locked]].
- **CONTINUED same session (2026-06-25) — F4 + P1 DONE + committed; disposition system now works for real tenants.** **F4 ✅ 4c6e3fb** (DLR-013 auto-disposition: callProjection hook on endedAt&&!answeredAt → autoDisposeNoContact writes `disposition_source=system` from the Telnyx hangup cause; booker>system NULL-guard; non-terminal; honest no-op if code absent; DB-green 566 + doctrine-clean). **P1 ✅ 7388526** (BUX-004 default-taxonomy provisioning — **THE disposition unblock**: a real tenant had NO taxonomy so disposition-save + DLR-012 + DLR-013 all silently no-op'd; now `seedDefaultDispositionTaxonomy` seeds at signup [auth.ts] + dev fixture + **migration 0019 backfills existing tenants**; central `shared/src/taxonomy/defaultDispositionTaxonomy.ts` composes F4's system seed; **editable default set = Appointment set·completes / Callback·tag+completes / Refused·completes / Wrong number·completes / DNC·dnc + No answer/Busy/Failed·system-recycle**; `wrong_number` added per DLR-012 [auditor caught the set had omitted it]; DB-green 573 + doctrine-clean). Backend slices **F1/F2/F3/F4/P1 ALL committed** on `s15-cockpit-redesign` (63fda9d lock, 1998394 docs, 1fa8531 F1, effd365 F2, 17aa45a F3, 4c6e3fb F4, 7388526 P1). Migrations **0018 (F1 cols) + 0019 (P1 backfill) are AUTHOR-ONLY** — apply on deploy/merge; the test-runner applies them to a fresh test DB per slice. **NEXT (same per-slice loop — implementer build [fast-checks; DB tests written-not-run] → I review diff + test-runner DB suite + relevant auditor → commit): F5 = DLR-014 AMD** (premium AMD webhook → `call_events` signal + `calls.amd_result` + AI-008 metering — READ THE FULL TELNYX AMD DOCS first) → **F6 = BUX-018 notes** (`calls.note` autosave + LeadNotesDTO aggregation) → **FE1 = React cockpit** (`SoftphonePage`/`CallCard` → the locked s15 mock; Claude implementer NOT Codex) → **MERGE GATE** (Amin-decided: merge AFTER FE1 so prod shows the real cockpit; human-gated — full `verify` + DB suite + COMPREHENSIVE compliance+cyber+doctrine+adversarial sweep over the assembled diff → PR to main → 0018+0019 apply to prod → prod review). **Carried merge-gate items:** F3 GET-tray rate-limit symmetry; `deferReason` treat-as-untrusted in future export/CoachAI consumers; the `.claude`/`.codex` centralization-parity gap RECURRED on F4 (I mirrored it) → doctrine-loop candidate = a CI gate asserting the two rules dirs stay in sync. Prod review (2026-06-25) was BLOCKED at login (I can't enter credentials); Amin logged in but we pivoted to finishing the build first — current prod (`main`) = the merged cockpit redesign #99-#104, NOT the F1-P1 work (feature branch).
- **F5 ✅ acb92dd (DLR-014 AMD passive signal).** Premium Telnyx AMD on the PROSPECT leg post-bridge (zero dead-air; predictive/AMD-to-bridge STAY scrapped) → `call.machine.premium.*` webhooks reduce to ONE `AMD_SIGNAL`/`AMD_GREETING_SIGNAL` call_events row (mapped once via `amdResult.ts`) → `amdProjection.ts` writes `calls.amd_result` (detection>beep precedence) + **extends DLR-013 with a `voicemail` system auto-dispo** (`autoDispositionVoicemail.ts`; booker>system NULL-guard; only on a machine detection of an ANSWERED call; non-terminal/recycle; honest no-op if absent). NEW `voicemail` system code (systemDisposition seed → default taxonomy now **9 codes**; **migration 0020** backfill). Metered via the existing Telnyx adapter (no PII). Cockpit reducer maps AMD→null (false-positive never flips the card). Full Telnyx AMD docs read+cited. ARC-006 Tier-2 — NEVER auto-acts (hangup-spy test asserts `hangups===[]`). DB-green **590 tests** + compliance-auditor CLEAN + doctrine-drift-auditor CLEAN (zero drift). **Migrations 0018/0019/0020 all AUTHOR-ONLY** (apply on deploy/merge). Backend slices **F1/F2/F3/F4/P1/F5 ALL committed** (acb92dd = F5). **NEXT: F6 = BUX-018 per-lead notes** (`calls.note` autosave + `LeadNotesDTO` aggregation) → **FE1 = React cockpit** (`SoftphonePage`/`CallCard` → locked s15 mock; Claude implementer) → **MERGE GATE** (Amin-decided: merge after FE1; full verify + DB suite + comprehensive compliance+cyber+doctrine+adversarial sweep + apply 0018/0019/0020 → PR to main → prod review). **Carried merge-gate items:** F3 GET-tray rate-limit symmetry; `deferReason` untrusted-in-future-consumers; F5 explicit-tenantId on the VM `updateMany` (RLS already covers) + an `answeredAt`-guard negative test; the `.claude`/`.codex` centralization-parity gap (recurred F4) → candidate CI sync-gate.
- **F6 ✅ b87481b (BUX-018 per-lead notes) — BACKEND LAYER 100% DONE.** Reuse-first (NO new table): the lead's notes = aggregation of its calls' `calls.note` (from 0018). `PUT /api/calls/:id/note` autosaves the "This call" note (dedicated `noteAutosave` rate-limit, object-scoped 404 anti-enum); `GET /api/calls/:id/lead-notes` → `LeadNotesDTO {priorNotes[{date,label,body}],currentCallNote}` (one `leadNotesMapper`; labels from honest Call fields = Attempt N / Inbound). **Latent-bug fix:** `dispositionSave` was never persisting `input.notes` anywhere durable — now the wrap-up disposition note + the autosave are the SAME `calls.note` (guarded: omitted notes can't wipe an autosaved one; a correction lands on an unchanged disposition). DB-green **595 tests** + cybersecurity CLEAN (note PII/telemetry boundary closed — never in logs/events/CallDTO; RLS-FORCE'd; CSRF+rate-limit+4000-cap). Carries: lead-notes GET gated on `dispositions.write` (managers later); `calls.note` retention/CoachAI-export coverage (compliance); deferReason+note untrusted-free-text class. NO migration. **ALL BACKEND SLICES DONE: F1/F2/F3/F4/P1/F5/F6** (7 slices, every one DB-verified+audited+committed; HEAD b87481b on `s15-cockpit-redesign`). **NEXT = FE1 = the React cockpit** (`SoftphonePage`/`CallCard` → the LOCKED s15 mock `frontend/public/explorations/s15-cockpit/index.html`; Claude implementer NOT Codex; mock APPROVED so the build is pre-authorized; consumes ALL the F1-F6 backend) → then **MERGE GATE** (apply 0018/0019/0020 + full verify + DB suite + comprehensive 4-auditor whole-diff sweep → PR to main → prod review shows the real cockpit).
- **⚠ PARALLEL SESSION ACTIVE on this repo/branch (2026-06-26):** another Claude session is editing docs in the shared tree — `ADR-INF-013-telephony-account-architecture.md` (NEW), `sprint-1-6.md`/`sprint-2-5.md` (NEW), + `decision-log.md`/`07-adr-index.md`/`01-product-brief.md`/`sprints/README.md` (mod). I committed F5/F6 **path-scoped** (only my files) to NOT swallow their uncommitted work — KEEP doing path-scoped commits (`git commit -F - -- <explicit paths>`) while their work sits uncommitted; never `git add -A`. **UPDATE:** the parallel session has since COMMITTED its docs to the SHARED branch (`fcd6a23 docs(strategy): council strategic review…` sits below my FE1a) + is now editing `decision-log.md` + `engineering/25-cost-capacity-performance.md` — we share one checkout/branch, so keep path-scoping. **FOOTGUN HIT:** `git add frontend` swept the untracked 261k `Power Dialer Cockpit.html` handoff blob into FE1a (5b1e5f3); removed it in `e403885`. Lesson: `git add <dir>` stages untracked files too — add explicit paths or `git rm` strays.
- **FE1 = wire the 7 backend slices into the EXISTING cockpit shell (it's already sophisticated: ticker/guided+streamlined/rail/morphing card/hotkeys/live-telephony) + add the s15 operational UI.** Decomposed into sequential FE sub-slices. **FE1a ✅ 5b1e5f3 (the core loop — real disposition save).** The gap was severe: `saveNext` just dialed; the selected disposition + typed note were NEVER persisted, and the wrap-up used the `DISPOSITION_KEYS` FIXTURE. FE1a: added `api.getDispositionTaxonomy()` + `api.calls.saveDisposition()`; **filled a latent backend gap — `GET /api/disposition-taxonomies` was registry-advertised but never implemented** (added it, tenant-scoped/RLS/dispositions.write@self); flat wrap-up from the real seeded taxonomy (booker rows filtered from system codes via the registry); persist `{code,notes}` (source=agent) → calls.disposition+calls.note BEFORE advancing (save-failure does NOT advance); wired the previously-dead wrap-up notes Textarea; retired the fixture (Gate 10); honestly omitted the AI-draft strip (no real draft gen yet). DB-green **600 tests** (disposition-taxonomy-route 5/5 incl. tenant-isolation) + frontend **438** + cyber CLEAN (RLS-FORCE'd both tables; provenance-spoof guard intact) + adversarial ACCEPT (mutation-verified the tests bite). Backlogged **DISPO-SOURCE-WIRE-001** (HTTP can write source=system — pre-existing). Carried (merge gate): API-contracts doc rows for the disposition GET+POST; the `me-cockpit-view` test-server ephemeral-port flake (transient `bad port` on `app.listen(0)`). **REMAINING FE sub-slices:** **FE1b** = Skip/Defer + deferred header chip & tray (F3/DLR-015); **FE1c** = Notes PANEL (prior dated notes + during-call autosave, F6/BUX-018 — FE1a did only the wrap-up note); **FE1d** = VM-detected cue + VM/SMS template pickers + auto-logged stage (F5 AMD + F4). Then the MERGE GATE.
- **🚢 SHIPPED 2026-06-27 — the whole s15 cockpit is MERGED to main + LIVE on prod.** FE1b ✅ (b0ac1bf + d3fb4f4 teardown-gate fix), FE1c ✅ (0476c1a notes panel; one-note reconciliation + autosave-cancel-on-WRAP_UP), FE1d ✅ (cbd6afd AMD cue + auto-logged stage; + debe06a Gate-11 fix) — each adversarial-ACCEPT. **Merge gate GREEN:** `verify` exit 0, DB suite 102 files, whole-branch **compliance + doctrine-drift sweeps CLEAN** (1 doctrine nit fixed). **PR #108 MERGED** (--rebase --delete-branch) → Railway auto-deploy → **migrations 0018/0019/0020 applied to the dialer prod DB at 02:31:47Z, not rolled back** (verified in Neon `still-sky-26160948` "nuvora-dialer" — see [[deployment-topology]]; ⚠ NOT the "Auxara"/small-credit DB, which is CoachAI-lineage — I wrong-DB'd it first; the 401-route probe only proves new CODE, not migrations). Backend new routes 401 (not 404), readiness green, frontend 200. **PENDING: Amin's prod review** (open dialer.auxara.io → login → Power Dialer). **Caveat for the review (as of 2026-06-27, now RESOLVED):** dialing end-to-end needs a list WITH leads — at that time the lead-import UI was unbuilt (import BACKEND existed #83). **UPDATE 2026-07-03: the lead-import UI SHIPPED (commit `6dcb95e`, `frontend/src/pages/lists/*`) — a real tenant can now upload a CSV + pick a list; this caveat no longer applies.** **Carried follow-ups (non-blocking, backlog/closeout):** DISPO-SOURCE-WIRE-001 (HTTP can write source=system — in BUG_BACKLOG); F3 GET-tray rate-limit symmetry; F5 explicit-tenantId on VM updateMany + answeredAt-guard negative test; API-contracts doc rows for disposition GET+POST; the 0019/0020 SQL-mirror→TS CI drift-gate; the `me-cockpit-view` ephemeral-port test flake; the auto-logged pane's attempt-count + live countdown (no backend field yet); the pre-existing `.claude`/`.codex` Page-registry/Manager-routes mirror drift; the VM/SMS template pickers (deferred — no playback/send/templates backend). ~~**NEXT after the prod review: the lead-import UI (mockup-first)**~~ — DONE (shipped `6dcb95e`, see the update above).
