---
name: coachai-coaching-quality-improvement
description: "CoachAI coaching-QUALITY scope (post role-fix, 2026-07-14): surface ALL critical moments not just 1; rich structured [did]/[mistake]/[next-time] coaching not one-liners; complete multi-turn evidence (rep + prospect). Amin's verbatim ideal-coaching example inside."
metadata:
  node_type: memory
  type: project
  originSessionId: 89cf399d-21ae-44d4-bd69-82f2e6a72d27
---

Surfaced 2026-07-14 by Amin AFTER the role-attribution root-fix shipped (see [[coachai-mono-role-attribution-rootfix]] — DONE, merged to CoachAI main @ `05fbb7d0`, deploy-verified, and verified corrected on the real re-run of 66e18d42). The role bug is fixed; **this is a NEW, separate COACHING-QUALITY scope.** Amin's key insight: the 66e18d42 6:14 "I'm blocking the schedule for you right now" moment IS the right moment and the coaching is substantively CORRECT — it's just shown too shallow/short and with incomplete evidence. So the fix is NOT "pick a better moment" — it's coach the moment(s) properly.

**Three requirements (Amin's intent):**
1. **Multi-moment** — surface ALL 2–3 critical coaching moments, NOT a forced single pick. Verbatim: "i dont understand why we keep focusing on showing only 1 moment, if there are 2 or 3 moments that are critical they all need to be shown." **The CANONICAL second-moment-that-must-surface = the prospect-CONFUSION moment ([0:23]/[0:25] "What benefits?" — prospect confused, rep answered with individual benefits instead of RE-ANCHORING that Navpreet extended his union benefits). This is Amin's ORIGINAL complaint #3, REINFORCED 2026-07-14 ("i still want to make sure the confusion part is also being ranked and surfaced, dont forget about that"). The role-fix restored its evidence (spans usable=true) but the fresh run surfaced 0 distinct comprehension finding. LOAD-BEARING open question the investigation must answer (drives the fix design): is the confusion moment DETECTED-and-dropped (→ ranking/selection fix) or NEVER-detected (→ comprehension detector / taxonomy fix — does semanticJudgment even look for "prospect confused AND rep failed to re-anchor"?).**
2. **Rich structured depth** — coaching = [what the rep did] + [the specific mistake] + [what to do next time]. NOT "short ass one liners that dont make sense" / "too mellow and short." **Amin's model coaching for the 6:14 moment (verbatim, this is the quality bar):** "you did ask them if the time doesnt work and they confirmed they might have to do some work, but then you only just said the cementing line and moved on, you didnt wait for them to confirm or more importantly you should have rescheduled them. next time when they tell you the time might not be 100% try to emphasize the importance of the meeting, and either offer to reschedule them or get a yes confirmation from them."
3. **Complete evidence** — show BOTH the rep's turns (Jehan [5:54] "are you not sure about the 2 p.m. later?", [6:11–6:14] the cementing line) AND the prospect's ([6:01] "we might get some work… we are free but"). Currently only the prospect side shows → the moment reads confusing when it's actually clear. The real mistake being coached: rep asked → prospect uncertain about 2pm → rep said the cementing/pressure line but then MOVED ON — didn't wait for a confirmation or reschedule, just sent the email.

**Approach (product-first, NOT a patch — Amin: "this also needs to be investigated and fixed properly"):** investigation dispatched (agent `abe3f039403334a53`) to root-cause WHY the current deployed pipeline emits 1 short thinly-evidenced moment across four layers: coaching-moment SELECTION (`semanticJudgment.ts` / `primaryFixRanker.ts` — is there a hard cap of 1 / a single primary-fix collapse?), coaching GENERATION (Prompt A/B depth + the output schema fields — is there a corrective-guidance field + a [did]/[mistake]/[next-time] structure?), evidence ATTACHMENT (single quote vs a multi-turn exchange), and the UI coaching card (can it already render multiple cards / rich text / multi-turn evidence?). Then: product-first plan → **mockup the richer coaching card (user-facing → mockup-first + approval before code)** → implement as an AI-decision-contract change (prompt + schema + validation per the ai-decision-contract-builder discipline; the accepted judgment must reach the rendered card). Concrete example throughout = 66e18d42's fresh `analysis_json_main_v1` run (payloadJson jsonb on `SessionPipelineRun`). Related: [[coachai-no-outcome-analytics]] (don't add outcome KPIs), [[no-internal-narrative-in-ui]], [[mockup-quality-bar]], [[codex-claude-division-of-labor]].

**ROOT CAUSE (investigation confirmed + orchestrator-verified in the prompt, 2026-07-14):** (A′ confusion) the confusion moment is **NEVER-DETECTED, not dropped** — `semanticJudgment.ts` comprehension rules (~1086-1090) are outcome-binary (RESOLVED→emit NO miss / attempted-unresolved / pitched-through), and counterexample (a) at ~1088 ("What benefits?"→rep answers→"yes I am his friend" = RESOLVED, no miss) LITERALLY trains suppression of exactly this call. The taxonomy has NO slot for "resolved but via the WRONG path (rep led with product details instead of re-anchoring the Navpreet referral) / delayed multi-turn re-anchor (~26s)". (B shallow) the rich coaching EXISTS in the bundle (`problemStatement`+`whyItMatters`+`actionStatement`+`acknowledgments[]`) but the UI drops it — `actionStatement` ("next time") is coupled to `replacementLine` (quality-gated null here → whole corrective band vanishes), and `acknowledgments[]` ("what you did + gap") has ZERO UI consumers. (C evidence) the semantic moment's `evidenceQuote` is single-string; the bundle has 3 turns but `TheMomentSection.tsx` renders only 2 (drops the `acknowledgments[]` 5:54 rep setup turn). (A count) single-hero is a deliberate PD-CR-2 design; UI already supports multiple. **Deliberate-design flags — do NOT blindly reverse:** the ~30 miss-suppression guardrails (over-flagging was the ORIGINAL problem → add a NARROW new miss-shape, not loosen); the `replacementLine` invented-fact gate (correct — bug is the coupling); the 320-char coaching cap.

**DISPATCH (2026-07-14, parallel):** items 1+2 (backend) → **Codex** in worktree `${WORKSPACE:dev}/coachai-wt-coaching-quality` branch `feat/coaching-quality` (off main 05fbb7d0): item1 = new comprehension miss-shape (`resolutionQuality` resolved_wrong_path/delayed_re_anchor) + rewrite counterexample (a) + golden fixture from 66e18d42; item2 = surfacing policy so ≥2 co-equal moments flow through the DTO (backend only — UI is orchestrator's). item3+ (UI: un-couple actionStatement from replacementLine, render acknowledgments[] + full exchange, co-equal moments) → mock via **Claude designer** (`frontend/mock/coaching-card-richer.html`). **OWED (orchestrator review):** (1) verify Codex's backend + run the **LIVE-AI validation** for item1 (does the real model emit the confusion miss with the new prompt on 66e18d42 — Codex fixture-only can't prove this); (2) review + show the designer's mock for user approval BEFORE production UI code; (3) golden-fixture + role-battery stay green.

**STATUS 2026-07-14 (Codex + mock BOTH landed):** Codex DONE — both backend items committed @ **`b55c0844`** on `feat/coaching-quality`, `FINAL_VERIFY_EXIT=0`; golden fixtures + killer mutations proven: the confusion-miss golden at `backend/scripts/semanticJudgmentRegression.ts:1053` (exact 0:23→1:14 sequence), the co-equal-moments regression at `backend/scripts/callReviewMapperRegression.ts:5448` (2–5 co-equal rows for ≥2 qualifying misses; mutation `CO_EQUAL_MIN_KILLER_MUTATION_EXIT=1`). Codex DID NOT add `resolutionQuality` (decided existing kind/category/confidence/rationale/coaching_instruction suffice — defensible, but REVIEW). **STILL OWED (next session, orchestrator):** (a) review Codex diff `b55c0844` (the semanticJudgment prompt change + the callReviewMapper co-equal policy) + **run the LIVE-AI validation** — does the REAL production model emit the confusion miss with the new prompt on 66e18d42 (Codex fixture-only can't prove it). (b) **The mock is delivered** (`${WORKSPACE:dev}/coachai-wt-coaching-quality/frontend/mock/coaching-card-richer.html`) BUT Amin pushed back — wants a **DESIGN RETHINK, not a tweak**: "consolidate more, our current design elements are not conducive, rethink how we're showing things." The mock fragments each moment into 3 boxes (did/slipped/next-time) + a separate transcript × 2 = reads as a form not coaching. DIRECTION PENDING Amin's pick among three explored framings: **unified-note-per-moment** (one flowing coaching sentence + inline proof, lean sequence) / **timeline-anchored** (coaching lives inline ON the call; evidence+coaching are one thing) / **compact-list→depth-on-expand**. Per the new [[orchestrator-mode]] "delegate design/mockup to a Claude designer agent" doctrine (codified this session), the consolidation exploration runs via a designer once Amin picks — NOT hand-built by the orchestrator. Nothing merged to CoachAI main from `feat/coaching-quality` yet.
