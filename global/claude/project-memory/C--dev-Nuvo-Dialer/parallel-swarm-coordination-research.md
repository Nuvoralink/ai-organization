---
name: parallel-swarm-coordination-research
description: 2026-07-24 research + 3-round Codex-Sol consensus (reached CONSENSUS:COMPLETE at r3) on scaling parallel agent swarms; design = ONE resource-admission authority; 2 pre-existing live bugs found; SETTLED but NOT yet implemented (impl plan is next)
metadata: 
  node_type: memory
  type: project
  originSessionId: 6a2a9c3b-6681-4c19-9401-2b742de47710
  modified: 2026-07-27T05:33:08.314Z
---

2026-07-24. Goal: scale parallel agent work without merge conflicts / duplicated work / missing wirings. Method: 3-track research (real-team coordination + agent-swarm SOTA + repo audit) → design v1 → 2 independent Codex Sol (gpt-5.6-sol, xhigh) reviews (completeness + adversarial red-team) → orchestrator verified 9/9 load-bearing claims → reconciled to **v2**. Both round-1 passes returned NOT-YET and CONVERGED on the same architecture. Loop reached **CONSENSUS: COMPLETE at round 3** (cap 4) — architecture + impl-spec both COMPLETE; residuals are all IMPLEMENTATION-SPEC (SQLite schema/versioning, Windows path fixtures, epoch/ABA mutations, process-containment standard [Job Object — boundedProcess tree-kill can't prove daemonized-descendant containment], manifest-closure derivation, RB-002 prisma reader-lease-vs-content-addressed fork, closed-world authority-registry classifier, executable landing cmd w/ pre-CAS rereads, observe→enforce denominators). NOT yet implemented — impl plan + user go is the next step. Codex resequenced: identity+fencing→SQLite ledger→observe both launcher paths→prisma-hardening(early, live bug)→enforce→digests+v3 edges→candidate-CAS(human-gated). Scratchpad docs: parallel-swarm-design-v2.md + codex-review-r1/redteam-r1/review-r2 outs + my-independent/adversarial-analysis.md.

**Settled research:** conflict ≈ divergence×overlap; partition by ownership (path/module) NEVER line-ranges; contract-first = the parallelization enabler; parallelize READS, serialize WRITES unless disjoint write-sets behind frozen contract; measured 27.67% uncoordinated agent-PR conflict, 41.7% cross-fleet (Claude+Codex same surface) vs 19.8% same-fleet → the Codex=backend/Claude=frontend split is a CONFLICT control; industry converged on isolate-per-task→branch/PR→gated-merge; "semantic merge" (textually-clean but seam unwired = missing-wirings) is the field's #1 UNSOLVED problem and this repo is already ahead (gate:endpoint-wiring + functionality-parity-auditor).

**v2 = ONE resource-admission-and-integration authority** (NOT the v1 "6 gates"; reuse lifecycle controller + gate:agent-control-plane, don't add 6 gates):
- Claim exclusive RESOURCES (typed vocab: canonicalized write-set [sep/case/realpath/glob/dir-containment/rename/generated-output aware] + singletons root-lockfile/prisma-schema/migration-namespace/gate-registry + authority-domain locks from centralization §1 + integration-tip) atomically, pre-launch, under ONE short global admission mutex.
- **Claims are durable RECORDS, not held mutexes** → lock-ordering rule: no coordination mutex held while acquiring/under/releasing the test-DB lease; DB lease releases before any task/queue state update. Dissolves the deadlock class.
- **Fleet chokepoint = the bounded dispatcher (agent:run), NOT the Claude-only TaskCreated hook** (verified: Codex codex-task-status.mjs is read-only, can't mint attempts — lifecycle/README.md:26). Codex-touching parallel work = single-writer until a trusted Codex reservation adapter exists.
- Freeze a contract-BUNDLE DIGEST (not base SHA) → survives unrelated main commits, invalidates on member change; states proposed→active→retired/recovered + thaw; require real provider→consumer liveness (functionality-parity-auditor).
- Merge = build EXACT candidate merge commit + risk-appropriate proof (verify alone is NOT enough/NOT authority) + expected-tip compare-and-swap + every action-authority criterion; deploy/schema/etc still human-gated; read verify's REAL exit.
- DAG edges bind to landed-commit/artifact-digest, never "task complete"; needs task-assurance **v3** (v2 schema is additionalProperties:false — task-assurance.v2.schema.json:6).
- Prisma = per-output atomic gen (sibling temp → atomic replace → completed-marker), route verify+postinstall+direct through it, separate install lock domain, NO test-DB-lease reuse. (v1's shared-node_modules premise was WRONG — output is worktree-local src/generated/prisma; owned by RB-002.)
- Rollout: off|observe|enforce (observe emits conflicts, NEVER "safe"); enforce only for parallel multi-writer; single-agent path untouched; kill switch preserves evidence. 7-step sequence: harden state store→observe reservation adapter (all fleets)→audit declared-vs-actual→enforce parallel-only→digest-freeze+typed edges→merge queue low-risk→per-output prisma.

**BUILD IN PROGRESS (2026-07-24, this session).** Branch `codex/swarm-slice1-state-store` in worktree `${PROJECT:auxara-dialer}-swarm-slice1` (cut off origin/main; NOT pushed/PR'd — merge human-gated). Codex implements each slice, Claude adversarially reviews, Codex fixes. **ACCEPTED (each Codex-built → Claude-reviewed/probed → verified):** s1 `8256f094` (2 live-bug fixes + atomic writes) · s2 `4985339f`+`9f2e17c4`+`5e42e4ab` (SQLite admission ledger `coordination/{resourceKey,ledger,resourceConfig}.mjs`, `gate:resource-coordination`; review fixed 3 overlap false-negatives + release state-guard + reconcile fail-open; testing-guardrails §23 kind-pair-matrix rule) · s3 `79c9329a`+`2e6339a1` (mode-gated `off|observe|enforce` default-OFF observe instrumentation: `coordination/{mode,register,coverage}.mjs` + `coordination-mode.v1.json`; agent:run `--claim-file` + TaskCreated hook, fail-safe/never-block; dispatch-claude-cli DEFERRED-recorded; ACCEPT review) · s4 `c8ffa3b7` (authority-domain registry `authority-domains.v1.json` + `expandResourceClaims` cross-file semantic-collision folding; probe-verified) · s5 `014d0d03` (task-assurance **v3** schema + upgradeV2ToV3 via existing validator + `coordination/dependencyGraph.mjs` cycle/orphan/validateRequires; probe-verified). · s6 `67dced11` (contract-bundle digest freeze: `coordination/freeze.mjs` manifest+digest sha256-parity with v3 edges, freeze store proposed/active/retired, isFreezeValid drift detection, `gate:contract-freeze` 7/7; Codex timed out pre-commit, orchestrator verified+committed staged work; verify arbitrating). **CONTROL-PLANE CONSOLIDATION (parallel stream, user directive 2026-07-26):** `${PROJECT:control-plane|backslash}` = THE one universal repo; Codex reconciling its 46 control:check problems (branch `reconcile/live-drift-2026-07`, live→canonical); Explore sweep found: ${DEPENDENCY:skills-root|backslash} NO git remote (~115 skills: Specforge29/VisualForge32/MarketForge45/Hormozi25, 21 dirty), plaintext Google-AI key in ~/.claude/settings.json (rotate + template), ~/.codex/memories+automations unmapped, SpecForge 29 loose copies unmanaged; absorption brief ready (marketplaces→dependencies/ mirrors, memories/automations/plugins/skill-lock captures, settings template, scheduled control:check alarm); THEN Phase B = swarm engine → control-plane canonical + overlay propagation to dialer/CoachAI (user: "no point if nothing uses it"); THEN rename repo (GitHub+local). MEMORY POLICY (user 2026-07-26): transcripts stay machine-local; curated memory/*.md files captured into repo; memory self-bootstrap rule → canonical global CLAUDE.md; FINAL PHASE = unified memory MCP server on Neon Postgres (ONE memory, all agents Claude+Codex, every machine; server+registration canonical in control plane, only DB cred local) — user confirmed "one memory for all agents everywhere". s6 ACCEPTED. s7 COMMITTED in 5 increments (`4412b827..dce11a7a`: counters→SQLite ledger, shared countDispatch seam both dispatchers, mode-epoch, promotionReadiness, sqlite-lazy-load) — verify green-save-baseline; Codex DID write the cross-process barrier test (`scripts/coordination-coverage.test.mjs`, READY/stdin barrier, in gate:coordination-wiring 15/15) — my grep missed it (lesson: check the slice's own diffstat before declaring a control missing); adversarial review of s7 IN FLIGHT; s8 brief READY (enforce+kill-switch: block only on PROVEN conflict, FAIL-OPEN-loudly on coordination error, readiness demotion w/ acceptNotReady override, exit 125, kill-switch preserves evidence); s9 = human-executable candidate-landing package (separate). CONTROL-PLANE PHASE A COMPLETE: reconcile (45/46, e17e0a6) + absorption (6 commits thru d25dad8: test-intent union, 5 marketplaces+skills-root→dependencies/, codex memories/automations/_retired, plugins/plans/skill-lock, SANITIZED settings.template.json, curated project-memory capture, memory-bootstrap rule in canonical CLAUDE.md, unified-memory roadmap note, recommend-only drift alarm) → **control:check PASSED (0 problems)** on branch `reconcile/live-drift-2026-07` (unpushed, human-gated). PHASE B design: dialer's `.ai-organization/runtime/core/*` is ALREADY overlay-managed by the control plane (ownership.json SHA rows) → after swarm branch merges (human), capture coordination engine into control-plane canonical + both overlays → overlay:install delivers engine+s1-fixes to CoachAI (backflow+propagation in one). Then rename repo. **Prepped/queued:** s7 promotion-grade coverage denominator (SQLite-serialized + symmetric countDispatch + mode-epoch + promotionReadiness — resolves COORDINATION-COVERAGE-LOST-UPDATE-001) → s8 enforce-mode activation + kill switch (RISKY, full review; fail-OPEN on ledger error not fail-closed, block only on proven conflict) → s9 candidate-CAS landing (human-executable package). **DEFERRED:** prisma atomic-gen (s1 fixed the live bug; residual intra-worktree race backlogged). **Known baseline:** verify RED only on PRE-EXISTING `agent-context-budget-gate` (~10,818>10,000, origin/main). **Owed:** CoachAI backflow (runtime DIVERGED — has writeJson flag-'w' bug [clean apply] but NO listTaskAttempts [corrupt-swallow needs per-structure analysis], dedicated pass); bootstrap-template backflow (template doesn't store runtime directly). Goal set: "finish the full implementation properly" (Stop hook active). ENDGAME STATE 2026-07-26: s8 (enforce+kill-switch, 5 commits + my conflict-set test fix eff426b9) and s9 (candidate landing, 6 commits + my re-home fix 60295585 — Codex had put candidate.mjs in a NEW root coordination/ dir which the overlay would never propagate; moved to canonical home) DONE. FINAL adversarial review (s8+9): **CORRECTED 9 findings — ACCEPT push / REJECT enforce-flip until F1**: F1 BLOCKING-on-activation = hook computes refusal AFTER acceptLifecycleTask+recordReplacementDispatch → refused task's retry permanently stall-blocks (fix: pre-acceptance phase + retry-after-refusal killer test); F2 baseline matcher rejects the real two-line gates:all cascade (default profile can never be ready); F3 no-mutation test reads LOCAL not remote refs; F4 project gate-name literal in propagated engine (Gate-11); F5 candidateSha GC-able (retain refs/candidates/<sha>); F6 stale eslint glob; F7 child-125 remap collides with exit 2; F8 hook admission exception counts nothing; F9 criteria-from-source-tree (document only). What HELD: fail-open, no-claim-on-refusal, lease CAS, proof-binds-candidate, kill-switch preservation, peek-epoch, standing DISPATCHER_NOT_INSTRUMENTED. **PUSHED: dialer branch → PR #287 (findings disclosed, do-not-enforce warning); control plane RENAMED → github.com/Nuvoralink/ai-organization, branch pushed → PR #4 (Phase A complete, control:check 0)**. Bootstrap prompt = docs/new-machine-bootstrap.md (in PR #4) + sent to Amin. Slice 10 (F1-F8 remediation) COMPLETE: 8 commits `2ba301ac..25ea2057` (F1 refuse-before-acceptance + retry-after-refusal killer test PROVEN; F2 aggregate-line attribution; F3 ls-remote assertions; F4 engine-empty/project-supplied baseline; F5 refs/candidates/<sha> GC-proofing; F6-F8), gates 19/20/8 green, final verify green-save-baseline, PUSHED to PR #287 + READY comment posted. **ENFORCE ROADMAP (user 2026-07-27: "no point if its not enforced").** VERIFIED BLOCKER — the engine is merged+live but INERT AT THE POINT OF USE: repo-wide grep proves NOTHING passes `--claim-file` (only run-bounded-agent.mjs + its tests reference it), so no claims register and enforce would have nothing to enforce on (false-green forever). Queued briefs in scratchpad: **STEP 1** `enforce-step1-claims-from-briefs.md` (DIALER: add `--brief` to run-bounded-agent.mjs deriving the claim descriptor from the `CLAUDE_DISPATCH_BOUNDARY_JSON.edit_paths` the dispatch brief ALREADY carries — reuse check-dispatch-boundary-consistency.mjs's parser, don't write a 2nd; mode-gated+fail-safe; make `--brief` the documented default dispatch form). **STEP 2** `enforce-step2-instrument-claude-dispatcher.md` (CONTROL PLANE: instrument canonical `skills/bootstrap-orchestrator/scripts/dispatch-claude-cli.mjs`; the real design problem = a user-GLOBAL script reaching a PER-PROJECT ledger → resolve the target repo from the child's --cwd, load THAT repo's installed coordination runtime, no-op safely if absent; then and ONLY then remove its entry from KNOWN_UNINSTRUMENTED_DISPATCHERS so `promotionReadiness` can honestly return ready). Step 2 depends on Step 1 merged + Phase B canonical. THEN the human flip: mode→observe, watch real parallel work, confirm conflicts are genuine, then →enforce (needing `acceptNotReady` at that point means readiness is still telling the truth about a gap). SEQUENCING RULE LEARNED: don't run a dialer slice while a control-plane Phase-B capture reads FROM the dialer — half-finished work gets captured.

**SHIPPED 2026-07-26: BOTH MERGED.** Dialer PR #287 squash-merged → `origin/main` `ff7288fe` carries all 12 coordination modules (merge-commits are FORBIDDEN in the dialer repo → use `--squash`; rebase re-conflicted on the generated REPO_FILEMAP across 35 commits, so I merged main INTO the branch instead, resolving REPO_FILEMAP by regeneration + decision-log "Last updated" by union). Post-merge integration surfaced 2 main-introduced gates: `gate:doc-graph` (STALE generated manifest → regenerated via `writeDocGraphArtifacts()`, now 668 nodes incl. ARC-010) and `gate:project-ledger-drift` (**PRE-EXISTING** — byte-identical 4 findings on clean origin/main via throwaway worktree probe; AUTH-007/INF-011 need projection exceptions). So main now has TWO pre-existing failing gates: agent-context-budget + project-ledger-drift. Control-plane PR #4 rebase-merged → renamed repo `Nuvoralink/ai-organization` main; post-merge `control:check` showed 11 problems (1 unclassified-tracked-path = my bootstrap doc, 9 drift, 1 local-only) → fixed via `refresh-tracked-scope.mjs --approve <path>=documentation` + per-mapping `control:capture --mapping <id> --update-existing` (a BLANKET capture is correctly REFUSED: live CLAUDE.md holds literal paths where canonical holds ${HOME} tokens) → **control:check PASSES**, pushed `9ad6016`. **PR #3 CLOSED as superseded — merging it would have DELETED 73,660 lines** (its content was already on main via the rebase-merge SHA rewrite; SHA-ancestry checks LIE under squash/rebase merges — always compare trees: `git diff --stat origin/main <branch>`). PRE-EXISTING SPRAWL surfaced, NOT touched: 10 stray dialer worktrees + 13 branches showing "commits not on main" — but that count is meaningless here because the repo squash-merges; needs per-branch CONTENT audit before any deletion. Prior state was: both PRs open + READY — dialer #287 (all 10 slices, inert on merge, enforce-flip blocker RESOLVED) + control-plane ai-organization #4 (Phase A). All autonomous work DONE; remaining = human merges → Phase B runbook → cosmetic local folder rename. LOCAL folder rename deferred (handle busy — likely Codex desktop; `Rename-Item '${PROJECT:control-plane|backslash}' 'AI Organization'` after closing it). Remaining after s10: push fixes to PR #287 → Amin merges both PRs → Phase B runbook (scratchpad phase-b-propagation-runbook.md) → done.

**2 PRE-EXISTING LIVE BUGS found (fix regardless):** (1) corrupt attempt JSON → orchestration-state.mjs:204 catch{return[]} → EVERY SessionStart reads "0 active tasks" (false confidence); evidence-runtime.mjs writeJson flag 'w' not atomic. (2) 6× 21MB orphaned query_engine-windows.dll.node.tmp* in backend/src/generated/prisma (~127MB) = non-atomic generate. NOT yet fixed/flagged to backlog — surfaced to Amin.

**Through-line risk:** a false-green gate is WORSE than none (kills manual vigilance) → every control fails LOUD+CLOSED on its own uncertainty + ships a meta-test naming its killer mutation. Do NOT build: live agent mailboxes/blackboard, file-lock daemon, swarm frameworks, full DAG scheduler/graph-UI. Real ceiling = verification throughput (single test-DB lease); watch, don't pre-solve.

Related: [[push-rebase-before-dispatching-into-worktree]] [[commit-before-adversarial-review]] [[codex-claude-division-of-labor]] [[codex-dispatch-windows-form]].
