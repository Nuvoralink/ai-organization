---
name: sprint-1-2-paused
description: Sprint 1.2 (power dialer) — A/B/C/D merged (C2 = PR#53 859c2e0, 2026-06-14); only E (softphone) + F (closeout) remain. New test-runner agent pipeline (implementer fast-checks → test-runner → orchestrator) + how to dispatch E/F.
metadata: 
  node_type: memory
  type: project
  originSessionId: c736ed6a-56e0-4496-b3fa-e4b8599be152
---

**Sprint 1.2 (Power Dialer + Call Lifecycle) is PAUSED** as of 2026-06-12 — user's call, to stop conflicts with concurrent **Sprint 1.1** work (a separate orchestrator/session owns the numbers lane: PR #34 numbers-api, num-lifecycle-service, num-007-clean-numbers, ci/db-integration, num-mocks). Resume 1.2 only after 1.1 finishes. **✅ 1.1 FINISHED 2026-06-13** — foundations PR #46 (`9a147d1`) + migration `0009` prod-live (verified) + DoD reconciled #47 (32/35; the only 3 open are post-merge live-provider validations: FCR test keys / e2e buy→release / live spam-flag — NOT 1.2 blockers). **1.2 is now resumable.**

**DONE + on main (permanent — do NOT unwind):** Slice A = PR #33 merged (`d7ff43c`). Migration `0007_call_lifecycle_foundation` (calls + ARC-007 operator seam + REC fields; `call_events` partitioned + `telnyx_event_dedupe` idempotency guard; immutable `compliance_audit_log`; ai_disposition_drafts/ai_usage_events skeletons; telnyx_webrtc_credentials; lists/prospects with **INT-007** identity + **DLR-010** queue cols; users **CMP-016** e911 seam; RLS ENABLE+FORCE on all tenant tables). Shared: `operatorType`, `callEventTypes`, `dispositionSource`, `errorCodes`, `contracts/calls.ts`, `endpoints.ts` migrated to **ARC-008** `{path,external}` (API_ENDPOINTS still derives plain strings — non-breaking), `thresholds.dialer`. **Plus the CI `integration` job** (Docker Postgres + `dialer_app` NOBYPASSRLS) that gates the RLS/append-only/idempotency suites server-side — closed a doctrine-loop Arm-1 gap the adversarial reviewer caught; the 1.1 lane then added a fail-fast timeout to it (#35). Adversarial review verdict: ACCEPT (170/170 live).

**SLICE PROGRESS:** ✅ **A merged** (#33, foundation) · ✅ **D merged** (#49, recording authority, migration 0010) · ✅ **B merged** (#50, `4b97681` 2026-06-13 — `lib/providers/telnyx-call-control.ts` adapter [sets `client_state={tenantId,callId}`] + `webhooks/telnyx.ts` ed25519 webhook [`telnyx_event_dedupe` unique-insert IN the withTenant tx] + `services/callProjection.ts` [pure reducer + idempotent rebuild] + `lib/pubsubChannels.ts`; imports D's `recording.finalize` queue; new event types `RECORDING_SAVED`/`PROVIDER_EVENT_UNMAPPED`; **`WEBHOOK_SIGNATURE_INVALID` 403→401**). ✅ **C ALL MERGED** — C1 (#51) compliance-gate skeleton + C3 (#52) AI-metering/disposition-queue + **C2 (#53 `859c2e0`, 2026-06-14)** the power dialer engine: DLR-010 `next()` queue (FIFO+recycle+min-gap+cap+DNC-suppress+exhaustion) + audit-first commit-before-dial + pause-wins-the-race (BENCH-CON-PAUSE-BUTTON-BUG) + NUM-006 select + dials-today + disposition-draft enqueue + dial_* telemetry + thin `routes/calls.ts` + `COMPLIANCE_BLOCKED`; 2 reviewers ACCEPT, 56/56 green. REMAINING: **E** softphone (webrtc-token broker + live call card per LOCKED cockpit mock; Claude lane) · **F** closeout (docs + reviews + **first live PSTN call** exit criterion; codify the test:integration-before-push rule into sprint-rigor; NIT-1 source-of-truth-map AMD line already fixed in B). Telnyx + R2 creds in env (0.0).

**LESSONS FROM D (apply to B/C — both add DB suites):**
1. **Codex sandbox CANNOT run `test:integration`** (no Docker/network) and `npm run verify` SKIPS DB suites (dbGate needs loopback Postgres). So a sandboxed implementer's "verify green" is NOT proof for DB-backed behavior — it was green while the playback route 404'd its own owner. **Orchestrator MUST run `npm run test:integration` locally (Docker) BEFORE pushing any DB-backed slice's PR** — catch it pre-CI, not at the CI integration gate. (The CI `integration` job is the backstop and DID catch D's, but pre-PR is cheaper.)
2. **Never edit an applied migration.** Codex edited 0007 (already deployed) to add `recording_enabled` because my re-baseline note wrongly said the column was in 0007 — doc-08 listed it but the migration never added it. Orchestrator fix: relocated to new `0010`. **Verify schema claims against actual migration SQL, not the data-model doc.** New columns → new migration always.
3. **Verify the critic.** The adversarial reviewer's REJECT was right (route 404'd owner, 4 tests red) but its root-cause *hypothesis* (RLS context-loss across transactions) was WRONG — the real cause was a malformed `UUID_RE` (8-4-4-12, missing a group) rejecting valid callIds before the lookup. Reading the code beat trusting the critic's diagnosis. (Backlog REC-UUID-DEDUP-001: extract one shared UUID validator — duplication caused it.)

**LESSONS FROM B (2026-06-13, Claude-lane — confirmed the doctrine HELD, no new gap):** The D-lesson #1 paid off hard — the orchestrator's full `verify` + `test:integration` caught **6** issues in Codex's "done" work its sandbox never surfaced: (1) a TS2542 compile error (`compactJson` assigned to readonly `InputJsonObject`); (2) 4 unformatted files (sandbox skips `format:check`); (3) a **sync-throw Promise bug** in the unconfigured adapter (`unavailable():never` threw before `Promise.reject` ran — unit test caught it); (4) an **invented `REQ-TEL-001`** ID (gate:test-intent caught — real ID is `REQ-DLR-001`); (5+6) two **dead fixtures** (`hangup`/`unknown` → alias-map + UNMAPPED fallback uncovered — adversarial reviewer caught, now wired). Every existing control fired (gate:test-intent, tx-seam gate, adversarial reviewer, the verify-the-critic + read-the-real-exit habits) — **no new gate needed.** Two process notes: **(a) the "exit 0" trap is real on EVERY run** — a backgrounded `npm run X` task-notification "exit 0" is the trailing `echo`'s code, not X's; always read the `VERIFY_EXIT`/`INTEGRATION_EXIT` sentinel written INTO the log, never the wrapper. **(b) Killing Codex mid-run** (user switched to Claude lane at ~20% Codex quota): checkpoint-commit its work first (`git add -A` + `wip`/feat commit, gitleaks-clean), then the orchestrator finishes verify+review+fix on top — Codex's substantial work is preserved, not wasted. `gh pr merge --delete-branch` FAILS in a worktree setup (can't check out `main`, held by primary) — the server merge still lands; delete the remote branch manually with `git push origin --delete`.

**⚠ ORCHESTRATOR CONCURRENCY RULE (2026-06-14, learned the hard way — TWO collisions):** Parallel agents isolate *worktrees* but **NOT** (a) git's shared `.git` or (b) the **single shared Docker `auxara-testdb` (port 55432)**. Two collisions in one session: (1) C1's worktree HEAD got reset mid-session (a git op raced); (2) running the adversarial reviewer's mutation test **concurrently** with my own `test:integration` → the reviewer's test-db setup/teardown killed my run's DB connection → **23 files failed with `Can't reach database server at localhost:55432`** (a pure environment artifact, zero real defects). **RULES:** **(i) SERIALIZE every test-DB user — only ONE `test:integration` (orchestrator OR any sub-agent) at a time.** Never tell a sub-agent to run `test:integration`/the DB tests while my own is running (or vice versa). If a reviewer must run a DB mutation test, run it AFTER my check finishes, not in parallel. (ii) A surprising mass-failure (N unrelated files, all `Can't reach database server`) = the shared DB went down mid-run → re-run ALONE before believing any defect. (iii) Codex can't run `test:integration` anyway (no Docker), so Codex-parallel is DB-safe; the risk is **Claude agents/checks that DO use the DB**. ✅ **CODIFIED in #53 → `sprint-rigor.md §12 L1`** (the "one test-DB runner at a time" rule).

**✅ AGENT PIPELINE — the answer to "agents die at the test phase" (CODIFIED 2026-06-14 in #53; use for E/F + all future slices):** Root cause = the heavy DB suite floods a long implementer's context + the shared `auxara-testdb` gives phantom failures under concurrency, both concentrated at the LATE test phase. **New 3-role pipeline:** (1) **implementer** (sprint-implementer) runs ONLY fast bounded checks (`typecheck`/`lint`/`format:check`/`gates:all`) + WRITES the DB tests but does NOT run them or full `verify`; (2) a NEW **`test-runner` agent** (`.claude/agents/test-runner.md`; read-only Bash/Read/Grep/Glob, NO edit) runs the heavy `verify`+`test:integration` in a DISPOSABLE context as the SOLE DB user → returns a COMPACT verdict (pass/fail + 1 root-cause + suggested-fix per failure); (3) **orchestrator** reads the ~15-line verdict, decides+applies fixes, owns git+merge. Keeps BOTH the implementer AND my context clean (the user's explicit concern). Adversarial-reviewer + compliance-auditor run AFTER test-runner, **sequential** (DB-free; tell them NOT to re-run the suite) — both return compact verdicts (they burned ~470k tokens reviewing C2 in THEIR contexts; I ingested ~40 lines). Codified: `sprint-implementer.md` + `test-runner.md` + `sprint-rigor.md §12 L1` + handoff §2. NOTE: a just-created agent file isn't in the live registry until a session reload — so C2's own gate I ran myself; **E is the first slice to use the test-runner agent end-to-end.** **New backlog from C2 review:** `DIAL-SERIALIZE-001` (per-booker Redis dispatch claim — closes the pause GET→commit window + the concurrent-dispatch TOCTOU double-dial; **do in E**) + `DIAL-GATE-ORDER-001` (re-check pause before `evaluateGate` once the 1.4 gate does I/O).

**C2/E GUARDRAILS (drift-review 2026-06-14, verdict ON-PLAN — apply when briefing these):**
- **E EXTENDS the existing softphone — do NOT rebuild.** main already has `frontend/src/pages/softphone/{SoftphonePage,CallCard,HotkeysModal,PreShiftGate}.tsx` + a tested `callMachine.ts` (real reducer on shared `CALL_STATES`/`canTransition`) + **`simCallDriver.ts`** (Sprint 1.0-D labeled-simulation driver whose header says *"1.2 replaces this file with the Telnyx Call Control driver — same dispatch contract"*). E = **swap `simCallDriver` → a real driver wired to `useWebRTC` + WebSocket `call_events` + `/api/calls/*`, keeping the same dispatch contract**; add `useWebRTC.ts`/`useCallState.ts`/`CallCard.test.tsx`/webrtc-reconnect. Rebuilding the shell/machine = parallel-system drift (Gate 6). Make this an explicit E acceptance criterion.
- **C2 must BRANCH on the gate verdict** (DoD line 61 "gate runs → if pass, dial fires"): C1's skeleton returns the real `{ pass, reason?, audit_basis }` + writes the `compliance_audit_log` row; C2's `dialerEngine` blocks the dial when `!pass` (audit row records the block) and dials when pass. 1.4 then swaps ONLY the gate computation, not the wiring. A void gate or a non-branching engine = a wiring gap 1.4 inherits.
- **Connect-cue is RESOLVED = CLIENT-SIDE (slice E, NOT C2).** Pre-C2 drift review (2026-06-14) found the authority already decides it: PRD `02-prd.md:79-80` ("local connect-cue UX, not a persisted `call_events.event`"; "agent-side only — the prospect never hears it") + `THRESHOLDS.dialer.connectCue` (`file:'connect-cue.mp3', volume:0.4, durationMs:200`, frontend asset, fired on BRIDGED). Server-side Telnyx injection into a bridged leg CAN'T be "prospect never hears it" → it's a browser `Audio()` cue. **C2 does NOT build `services/audioMixer.ts`** (the `sprint-1-2.md:103` blast-radius line naming it is STALE DRIFT — reconcile it). The beep belongs to slice E's softphone (off the projection/BRIDGED event).
- **Telnyx `connectionId` source = `TELNYX_CONNECTION_ID` env (DECIDED 2026-06-14, Phase 1).** `TelnyxDialParams.connectionId` is required but had NO source on main (no `PhoneNumber` column, no env — only `'conn_123'` in tests). Decision: a single Call Control App for the shared Telnyx account → one `TELNYX_CONNECTION_ID` env (add to `env.ts`, optional like `TELNYX_API_KEY`); C2's dial passes it. Per-tenant connections only matter with per-tenant Telnyx sub-accounts (post-Phase-1, ~50 tenants OPS-002). Record in decision-log when C2 lands.
- **C2 audit-first write-ordering (migration 0007 invariant):** `calls_outbound_requires_compliance_audit_check` CHECK + DEFERRABLE-INITIALLY-DEFERRED 1:1 FK (`calls.compliance_audit_id` ↔ `compliance_audit_log.call_id`) → an allowed dial is ONE `withTenant` tx, **audit-first**: `writeComplianceAuditRow(tx,{gateInput,decision})` → `tx.call.create({direction:'outbound', complianceAuditId, startedAt})` → commit → THEN `client.dial()`. The calls row must exist BEFORE the INITIATED webhook (the webhook drops events whose call row isn't found). Blocked dial = audit-only row (`callId:null`), no calls row, no dial.
- **C2 consume-signatures (verified on main acd370b):** `evaluateComplianceGate(input):ComplianceGateDecision` (SYNC); `writeComplianceAuditRow(tx,{gateInput,decision,stirAttestation?,consentProofId?}):Promise<string>` (OBJECT arg, returns audit-id); `getTelnyxCallControlClient().dial({tenantId,callId,connectionId,from,to,webhookUrl?})/hangup(id)`; `selectOutboundNumber(input)` is PURE/sync — C2 fetches candidate rows + `readDialsTodayBatch(tenantId,numberIds)` itself, passes `{candidates,strategy,destinationE164,bookerId}`, gets `{numberId|null,reason}`; `incrementDialsToday(tenantId,numberId)` (null=Redis-down fail-open) on dial; enqueue `AiDispositionDraftJob{tenantId,callId}` to `AI_DISPOSITION_DRAFT_QUEUE` on hangup (consumer=1.3). `applyEvent` is already called by the webhook — C2 doesn't call it on the normal path. `calls.operatorType` C2 writes `'human'` (ARC-007, no ai_agent branch).
- **Local checkout 3 behind:** primary `${PROJECT:auxara-dialer|backslash}` HEAD was 10b8d4f + had stale untracked complianceGate.ts/etc. (worktree-race cruft) — `git fetch` + cut C2 worktree off `origin/main` (acd370b), don't branch off the stale primary tree.
- **Committed briefs:** A/B/D have committed briefs in `docs/agent-prompts/sprint-1-2/`; C1/C3 ran on thorough INLINE briefs (fine, in flight). Write committed C2/E/F briefs (forbidden-file list + re-baseline against current main) before dispatching them.
- **Queue contract pattern:** backend-local per-worker constant (`recordingFinalize.ts` exports `RECORDING_FINALIZE_QUEUE/_JOB`), documented in doc 09 — NOT `shared/taxonomy/queues.ts` (frontend never references queue names). C3's `aiDispositionDraft` queue mirrors this (already in C3's brief). If queues proliferate, consider a `backend/src/lib/queues.ts` registry.
- **Future (1.5, not now):** `sprint-1-5.md:134` Stripe webhook still says "403" — align to 401 when 1.5 is scoped (different surface: Stripe HMAC vs Telnyx Ed25519).

**LANE BOUNDARY — verified vs git + docs (do not re-litigate):** `numberRotation` / **NUM-006** caller-ID-by-area-code picker is **Sprint 1.1's** deliverable (sprint-1-1 DoD §67 + test ladder §155; decision-log line 89), NOT 1.2 — even though a 1.2 power dial *consumes* it. It is unbuilt as of the pause. 1.2's `dialerEngine` CONSUMES `numberRotation` at dial time (producer 1.1 / consumer 1.2); never double-build it. (Stale nit to clean: `sprint-2-1.md:16` still lists NUM-006 as a Phase-2 ID.)

**ON RESUME:** `git fetch origin` first; **re-baseline every brief against post-1.1 main** (design-baseline verification — 1.1 will have changed numbers API, contracts, `numberRotation`; the briefs' "existing code" + forbidden-file lists will have drifted). **1.1 foundations to EXTEND, not rebuild (PR #46 — design-baseline-verify against these):** slice **D** extends `backend/src/lib/recordingStorageAuthority.ts` (verify/health exists → add store/finalize/playback, fail-closed, never `STORAGE_DRIVER`); slice **C**'s dial path increments the existing `dialsTodayCounter.ts` (ADR-NUM-002 — Redis counter, `null` not `0` on miss, ranking input NEVER a cap) + its `dialsTodayReconcile` nightly worker; `inboundRouter.ts` ships an **injectable conversation-resolver stub** → wire the real Prisma resolver when `conversations` lands (1.3, not 1.2). 10DLC Brand vetting poll + `/api/tendlc` route exist (campaign-level 10DLC is 1.3). New build gate **`gate:tx-seam`** (in `gates:all`) fails on a `create(args: unknown)` tx seam — type DB-write seams against `Prisma.TransactionClient`. Division-of-labor model is confirmed working: **sandboxed Codex (`codex exec -s workspace-write -C <worktree>`) runs non-interactively, classifier-allowed, NO unsandbox approval needed** — Codex codes + self-verifies (needs `npm ci` run by orchestrator first since fresh worktrees have no node_modules), orchestrator does ALL git/rebase/merge. The Claude `sprint-implementer` agent lane also works but crashed once at `gh pr create` (work was committed+pushed first, so salvageable). See [[orchestrator-mode-setup]].
