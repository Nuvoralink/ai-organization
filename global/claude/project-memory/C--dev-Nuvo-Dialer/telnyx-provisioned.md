---
name: telnyx-provisioned
description: "Telnyx is ALREADY set up (funded account, key in backend/.env, Canada DID, Call Control app, WebRTC cred connection) — use it; never ask Amin to create/fund/key it"
metadata: 
  node_type: memory
  type: project
  originSessionId: c736ed6a-56e0-4496-b3fa-e4b8599be152
---

Telnyx is ALREADY provisioned for the dialer (verified live via the API 2026-06-15). **Do NOT ask Amin to create an account, fund it, or generate a key** — read `backend/.env` + query the account first (Gate 1). I made this exact mistake once (told him to create+fund+key an account that was fully set up).

- **API key:** in `backend/.env` → `TELNYX_API_KEY` (len 58, works) + `TELNYX_WEBHOOK_SIGNING_SECRET` + `TELNYX_WEBRTC_SIGNALING_URL`. `backend/.env` is gitignored — load the key into the shell (`grep '^TELNYX_API_KEY=' .env | cut -d= -f2- | tr -d '\r"'`), pass via process env, **never print or commit it**. ⚠ `TELNYX_CONNECTION_ID` is NOT in `.env` yet → wire it (= the Call Control app id below).
- **Balance:** ~$10.47 USD (funded; outbound is fractions of a cent/min — fine for testing).
- **Number:** ONE DID — **+16047429198** (Canada / Vancouver BC, active). Use this **Canada** DID for the first real call (the wedge is US/CA; STIR/SHAKEN covers CA). Buy a US number only if a test genuinely needs US (Amin pre-authorized that).
- **Call Control Application:** "Nuvora Link Dialer", `2909455785626961663` — ⚠ **a LEFTOVER from Amin's earlier "nuvora link app" test; NOT wired to this dialer.** Its webhook (`https://api.nuvoralink.com/api/v1/webhooks/telnyx`) points at that other app and some settings are stale. For the dialer: either RECONFIGURE it (webhook → the dialer's ACTUAL deployed backend, matched signing secret, voice profile) or stand up a dedicated "Auxara Dialer" Call Control app + move the Canada DID onto it — decide for durability when building the live slice (inspect its full settings first via the API). The chosen app's id becomes `TELNYX_CONNECTION_ID`.
- **WebRTC credential connection:** "Nuvora WebRTC", `2909569031675577469` (agent-leg SIP creds the E1 broker mints against) — may ALSO be from that earlier test; verify/reconfigure for the dialer when building. Two clearly-unrelated connections ("3cx Main line", "Forward Only") are NOT ours.
- **The dialer's deployed backend is `dialer-api.auxara.io`** (Railway; confirmed by Amin 2026-06-15) — NOT `api.nuvoralink.com` (that's the earlier app). The Telnyx Call Control app's webhook must point at **`https://dialer-api.auxara.io/api/webhooks/telnyx`** (the flat internal path per `shared/contracts/endpoints.ts`; the app currently has the wrong host AND the wrong `/api/v1/` path). Frontend host is **`dialer.auxara.io`**, cookies on `.auxara.io` (BUG-002 same-parent-domain auth, per `backend/.env.example`).
- Brand/domain: **Nuvora Link / nuvoralink.com** (GitHub org `Nuvoralink`; deployed API host `api.nuvoralink.com`).

**The live-PSTN slice's remaining work is CODE + wiring, not Telnyx setup:** the agent↔prospect audio **bridge** (backend Call Control bridge), the **real WebRTC driver** in the cockpit (frontend, replacing the sim — useWebRTC/useCallSocket + the E1 broker + the E2 `/call-events` socket), env-wire `TELNYX_CONNECTION_ID`, and confirm the webhook path + Railway backend are live. Telnyx MCP runtime (`uvx`/`uv`) is installed; the MCP server itself isn't added (not needed — direct API works with the key). See [[sprint-1-2-paused]] + [[user-profile-and-operating-mode]] (do ALL technical work myself; bring Amin only product/decision calls).

**WebRTC two-way audio PROVEN 2026-06-15** via the hosted demo (webrtc.telnyx.com, Token login with a disposable minted telephony-credential JWT → registered → called the cell → two-way audio confirmed). Also proven 2026-06-15: a direct Call Control API dial from the Canada DID rang the cell (showed "Vancouver, BC"). Learnings for the real build:
- **Outbound WebRTC calls REQUIRE `callerNumber` = an owned DID** (`+16047429198`) — with it blank the leg drops. The cockpit's number selection (NUM-006) supplies it.
- The JS client anchored to **EU / fr5-prod** — pin the region to a NA datacenter for the real client (TelnyxRTC region option / cred-connection anchorsite) for latency.
- The on-demand telephony-credential + `/token` mint flow works (= our E1 broker).
- **Architecture CONFIRMED = Model A** (server places the prospect call via the Call Control app + bridges the booker's WebRTC agent leg) — chosen over client-originated because the prospect leg must be a server-controlled Call Control call for **recording (REC-006) + disclosure playback (CMP) + future barge/whisper**; the spike proved the WebRTC media leg that Model A bridges in. Decision-log/ADR row owed.

**Live-slice progress (2026-06-15), branch `s12e-live-pstn`:**
- ✅ **Frontend WebRTC driver DONE + committed** (`d67baad`): `useWebRTC` (auto-answers the agent leg, NA region pinned, token refresh, owns ONLY the connection dimension) + `useCallSocket` (drives `callMachine` off the authoritative `/call-events` socket) + `api.calls.dial` wiring (cockpit shows the REAL dialed prospect) + `simCallDriver` retired. All gates green (typecheck/lint/format/check:ui/gates:all/build/212 FE tests, incl. an 8-test `useWebRTC` suite). Recovered LOSSLESSLY after the implementer agent stalled/died at the verify phase (watcher detected it → orchestrator checkpoint-committed `4f2185e` → finished in foreground). Lesson reinforced: intricate/crown-jewel work → do it myself or foreground; always arm a stall-watcher on background agents.
- 🔧 **BACKEND bridge (Model A2) IN PROGRESS** on `s12e-live-pstn`. Decided **per-call A2**: server dials the agent leg → the browser auto-answers it (built) → on its answer the server dials the prospect → on the prospect's answer it bridges + record_start(dual). Chose per-call A2 over a persistent parked leg (A3) for Phase 1 (reuses the built auto-answer, no shift-start timing signal; A3 = future latency optimization). **DONE + committed:** `bridge()`+`recordStart()`+leg-tagged `client_state` (`4b561d0`); `getActiveSipUsername` (read-only, never provisions) + `dialProspectLeg` + `bridgeOrchestrator.onLegAnswered` (`8688c8a`). **2c PROGRESS:** webhook orchestration wiring DONE (`72f033f`, dormant). Adversarial review run + fully REMEDIATED (`dee4f47` docs + `b199304`): #4 cross-booker bleed — `useCallSocket` now scopes dispatch to the active `callId` (the real bug the review caught: the /call-events gateway broadcasts tenant-wide; the cockpit must ignore other bookers' events) + a biting negative test; #1 `bridge-orchestration.test.ts` (client_state round-trip + `onLegAnswered` branches, `decodeClientState` exported as the seam); #2/#3 filemap + FRONTEND_BLAST_RADIUS refreshed. Branch GREEN (typecheck/lint/format/check:ui/gates:all + both unit suites all 0). **ONLY REMAINING CODE STEP = 2c-DIAL (atomic, must land together for green):** flip `dialerEngine.dial()` to dial the AGENT leg (`getActiveSipUsername` → `sip:<u>@sip.telnyx.com`, leg:'agent') instead of the prospect (re-add the `getActiveSipUsername` import then) + add a `no_agent_session` DialResult variant + `NO_AGENT_SESSION` in `shared/taxonomy/errorCodes.ts` + the `routes/calls.ts` switch case + seed an active `telnyxWebrtcCredential` in the 4 DB-backed engine tests (compliance-gate-branch / dial-serialize / pause-button-atomicity / dialer-engine-events — each calls dial() expecting a placed dial, which now needs the credential). Then `test:integration` (serialized, sole DB user) → wire `TELNYX_CONNECTION_ID` on Railway + the Call Control app webhook → `dialer-api.auxara.io/api/webhooks/telnyx` → live call to +17787454278. ⚠ Sub-agents keep RATE-LIMITING at end-of-run (reviewer + 2 implementers) — do 2c-dial + the rest MYSELF, no delegation.

**Live-PSTN CODE COMPLETE 2026-06-15** — 2c-dial committed (`066c9e9`); the whole slice (frontend driver + backend bridge + dial-flip) is built, verified (full gauntlet + the 338-test DB `test:integration`, all 4 engine tests green), adversarially reviewed + remediated (cross-booker `callId`-scoping of `useCallSocket` was the real bug it caught), **8 commits on `s12e-live-pstn`, branch GREEN, NOT merged + NOT live yet.** To FINISH the sprint: (1) the LIVE end-to-end call through the cockpit — wire `TELNYX_CONNECTION_ID` on Railway + finalize the Call Control app (webhook→dialer-api) + deploy the branch + place the call to +17787454278; the **A2 linchpin (server-dialing the agent sip rings the registered browser) is UNVERIFIED live**; (2) PR → merge; (3) close-out F (Model-A2 ADR + decision-log + sprint doc). **Local-env lessons:** the main checkout's `node_modules` was ~660-pkgs incomplete (tsx missing → the dev backend couldn't start — NOT OOM, NOT my code) → `npm install` fixed it (dialer runs via `dev:backend`+`dev:frontend`, there is **no root `dev`**); and I mis-killed CoachAI's `npm run dev` by trusting the process name — VERIFY the script exists in THAT project before killing. Heavy local suites on a loaded multi-project machine add real pressure — run sparingly + `test:db:down` after.

**🎯 A2 architecture PROVEN LIVE end-to-end 2026-06-15** (spike via webrtc.telnyx.com demo + direct Call Control API — NO deploy needed): the Call Control app (`2909455785626961663`) dials `sip:<gencred>@sip.telnyx.com` (the agent leg) → the registered WebRTC browser **RINGS** → answer → dial the prospect (+17787454278) → `bridge` the two legs → **two-way PSTN audio confirmed**. Every link the real cockpit depends on, validated against real Telnyx.

**⚠ CRITICAL CONFIG FINDING the spike caught (would have silently broken the deployed cockpit):** the WebRTC credential connection (`2909569031675577469`) MUST have **`sip_uri_calling_preference = "internal"`** (it was `null` = OFF). With it off, the Call Control app's on-net dial to the agent's `gencred` sip address is **REJECTED and the browser never rings** — the agent leg silently fails, so the whole A2 chain dies. Set via `PATCH /v2/credential_connections/{id} {sip_uri_calling_preference:"internal"}` (now SET on 2909569031675577469). `"internal"` = only our own account connections can ring agent legs (secure — blocks external abuse of `sip:gencred@`; `"unrestricted"` would let anyone ring an agent). **This requirement must land in: (a) the Model-A2 ADR, (b) a deploy/onboarding setup step + the webrtcCredentialService connection config, (c) ideally a `/api/ready` or setup-script assertion** so a fresh environment can't ship the broker connection with SIP-URI-calling off.

**Spike login/routing lessons (for the deployed-cockpit test):** the demo receives INBOUND only when logged in via **Credentials login with the telephony credential's `sip_username` (gencred…) + its `sip_password`** (telephony credentials DO expose `sip_password` via GET) — NOT the connection's static user/pass (`nuvorawebrtc`), and NOT by calling the DID from a phone (a number→connection routed call is a DIFFERENT inbound path that does not ring a WebRTC client). Token login (the ~700-char JWT) got "rejected" in the demo (almost certainly paste corruption of the long blob) — the real frontend passes the token programmatically via `useWebRTC`, so it's unaffected, but **verify the token-login registration path in the deployed cockpit test**. Demo anchored EU/fr5-prod again (cosmetic for the spike; real client pins NA region in `useWebRTC`).

**✅ MERGED 2026-06-15 — Sprint 1.2-E + F close-out → main** (PR #62, rebase-merge `4ba6acc`, branch deleted; CI green: verify/integration/docker-build/Vercel). Shipped: ADR-DLR-011 (Model A2) + decision-log DLR-011 (count→120) + ADR index row + sprint-1-2 note. **Repo = `Nuvoralink/auxara-dialer`** (the Railway *project* is named `nuvora-dialer` — DIFFERENT names; `gh` needs `--repo Nuvoralink/auxara-dialer`). Spike credentials revoked + the exposed `nuvorawebrtc` connection password rotated (app doesn't use it); `sip_uri_calling_preference=internal` confirmed surviving the rotation.

**⚠ Deployed-smoke gap (found 2026-06-15):** the Railway `api` service (prod backend; project `acff21c9-1a8b-4b26-abb4-544c9238b1fc`, env production `85b4dea0-b784-461a-b9bd-45ce765267cc`, service `45bb250f-6c8b-4746-80fd-d9549b266b49`, deploys from `main`, healthcheck `/api/ready`, region asia-southeast1) has **ZERO Telnyx env vars** — no `TELNYX_API_KEY` / `TELNYX_CONNECTION_ID` / `TELNYX_WEBHOOK_SIGNING_SECRET` / WebRTC-connection var (only 13 base: DB/Redis/JWT/CSRF/cookies/Sentry/Telegram/STACK/ALLOWED_ORIGINS). **The deployed backend has NEVER been able to dial.** (Telnyx webhook IS correct — verified: app 'Auxara Dialer' `2909455785626961663` → `https://dialer-api.auxara.io/api/webhooks/telnyx`, v2, active.) **The deployed our-code smoke (1.2-F exit line 221) needs:** (1) wire prod Telnyx env on Railway [`TELNYX_API_KEY`=the live money-spending key, `TELNYX_CONNECTION_ID`=2909455785626961663, `TELNYX_WEBHOOK_SIGNING_SECRET`, + the broker's WebRTC-connection id 2909569031675577469] — NEEDED for prod to EVER dial, not throwaway; (2) seed a test tenant/booker(`calls.dial`)/WebRTC-cred-mapping/list/+17787454278-prospect/assigned-number/compliance — **overlaps Sprint 1.3 lists/onboarding**; (3) trigger via cockpit or authed `/api/calls/dial`. **1.2's core is DONE** (A2 live-proven + merged). **DECIDED (Amin, 2026-06-15): DEFER the full seeded our-code smoke to Sprint 1.3** — where list import gives a real prospect to dial (no throwaway DB seed; the live key enters prod only when prod actually dials). Sprint 1.3 inherits: (a) wire prod Telnyx env on Railway, (b) the deployed softphone→our-code→recording→`call_events` smoke (1.2-F exit line 221), (c) the ADR-DLR-011 named follow-up = wire `sip_uri_calling_preference="internal"` as a setup/`/api/ready` assertion (doctrine-loop "wire the gate"). All three pair naturally with 1.3's first real cockpit dial.

**✅ Prod Telnyx env NOW WIRED (verified 2026-06-29 via railway-agent — CORRECTS the 2026-06-15 "ZERO Telnyx env vars / has NEVER been able to dial" claim above; the env got wired during the 1.3 live-PSTN work).** Railway prod `api` service (project `acff21c9-…`, env `85b4dea0-…`, service `45bb250f-…`) now has SET: `TELNYX_API_KEY`, `TELNYX_CONNECTION_ID`, `TELNYX_WEBHOOK_SIGNING_SECRET`, `TELNYX_WEBRTC_CONNECTION_ID`, + R2 storage (`STORAGE_BUCKET/KEY_ID/SECRET/ENDPOINT`) → the deployed backend CAN dial + record, and the number search/buy code can run. Still **UNSET (the real prod-config gaps):**
- `TELNYX_MESSAGING_PROFILE_ID` → **SMS-send fails closed** until a Telnyx Messaging Profile is created + this var set (+ 10DLC brand/campaign for US; Canada SMS works without 10DLC). This is THE SMS-send prod blocker.
- recording-disclosure audio-clip ref (no `DISCLOSURE*` var) → **all-party/Canada recording stays skipped** until the clip is configured (the tenant-config UI will cover the upload; an env-level default clip is the other option).
- `DNC_SCRUB_DRIVER` unset = the **intended safe default** ('none' → honest `dnc_provider_unavailable`, never a false pass) — no action.
- `STORAGE_DRIVER` unset but R2 bucket/keys set — recording authority uses the durable backend, not this env, so likely fine.

**⚠ Open Telnyx-ACCOUNT risk (NOT env, NOT code, NOT the key):** can the account actually ORDER/BUY a DID? The "inventory unavailable" Amin hit before is **account-level** (Telnyx identity/address verification or number-ordering funding) — it gates the onboarding guided-buy (ONBOARD-PROVISION-001 part A) completing a real purchase. Verify in the Telnyx console + observe live at the onboarding preview (the guided-buy search either returns real DIDs or hits its honest "couldn't load numbers" state).

**✅ RESOLVED 2026-06-29 — DID API purchase PROVEN + Messaging Profile WIRED.** A live API order via `POST /v2/number_orders` SUCCEEDED (test-ordered `+15083165755`, order `5f4de4a0-a367-4278-927c-5284c5f2af1c`, `status:pending`, `requirements_met:true`/`approved`) → **the account CAN order DIDs via API; no manual buy, no account/regulatory blocker.** The prior "inventory unavailable" was the **search→order race** (the searched number got taken before the order) — mitigated by the onboarding buy page's Retry (re-search → fresh number). Our `buy()` (`backend/src/lib/providers/telnyx-numbers.ts:137`) posts the correct minimal order; `search()` returns reservable US inventory (HTTP 200). **Spare DID `+15083165755`** (phone-number id `7373ab6f-cc50-4f85-9f1b-b33d52a2de40`) now sits UNASSOCIATED on the account ($0.35/mo) — import into the pool or release via `DELETE /v2/phone_numbers/{id}` (Amin to decide). **`TELNYX_MESSAGING_PROFILE_ID = 40019cc2-28f1-4ae7-83e0-9d72fe7472f9`** ("Auxadial Messaging", API V2) committed to prod `api` + `worker` via railway accept-deploy (deploy `b087703d-…`, 2026-06-29) → **SMS-send unblocks once deployed** (`smsSend.ts:405` gate `TELNYX_API_KEY && TELNYX_MESSAGING_PROFILE_ID` now passes); the cockpit SMS composer still needs frontend wiring (task #13).

