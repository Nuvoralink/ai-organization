---
name: coachai-mono-role-attribution-rootfix
description: "CoachAI mono-upload speaker-role mis-attribution root cause + 4-slice fix plan (opener anchor + correction, one authority, reliability-gates-score). Pre-existing bug, wave-innocent. Proof fixture = session 66e18d42."
metadata: 
  node_type: memory
  type: project
  originSessionId: 89cf399d-21ae-44d4-bd69-82f2e6a72d27
---

Surfaced 2026-07-13 by Amin on a real coached call (CoachAI Coach-Internal, Neon project `snowy-glade-91475703`): manual MONO mp3 "Jehan recording 1.mp3", session `66e18d42-1fd1-4fe8-8953-dd08455662f2`. The AI forced a random line-level "you blocked the schedule" fix, missed the real problem (prospect confused, rep never re-anchored the Navpreet-referral credibility), and attributed client lines to the rep — because the coaching runs on scrambled speaker roles.

**Root cause (all 3 confirmed in source at origin/main `ad1b8a62`; keystone verified by orchestrator directly):**
1. **Reliability signals computed then DROPPED.** `buildScoreAvailability` (`backend/src/lib/coaching/callReviewAuthority.ts:700-770`) gates role reliability ONLY on `roleUnsafe` (structural B1 diarization health, `b1TranscriptEvidenceIntegrity.ts:204-210`) — blind to a clean-but-mislabeled mono call. The semantic `speaker_role_inversion_suspected` / `analysis_limited` / per-span `usable_for_primary_coaching` (set in `semanticExtract.ts`) never reach the score authority (`semanticLayer` not passed at `analysisPipelineCore.ts:2317-2337`). So 136/136-unusable + inversion-detected still scored `authoritative` 64/100 with a confident hard-fix. **Pre-existing in ALL 6 reruns incl. the 07-06 pre-wave original → the sales-signals wave is WAVE-INNOCENT for the mono path** (CA-2b F1/F2 gated to `channel_ground_truth`; mono byte-identical).
2. **Inversion DETECTED, never CORRECTED** — flag only; on mono `ambiguous`→canonical `unknown` (100% unusable), no re-map/re-run.
3. **No single role authority** — 4 independent role layers (`b1SpeakerRoleMap` → role-adjudication → CQ-014 `normalizeSpeakerRolesViaAI` gpt-5-mini → `b1ConversationParticipation` `strong_semantic_override`); no deterministic OPENER anchor. Also NON-DETERMINISTIC (same audio scored 60/73/73/73/67/64 across reruns — CQ-014/extract run with no temperature/seed; gpt-5-mini can't be pinned).

**REFINED 2026-07-13 (Slice-1 spike, empirically confirmed — design premise was subtly wrong):** it's a PARTIAL scramble, NOT a whole-call inversion. MOST prospect turns are correctly CLIENT — incl. the coaching-critical confusion `[0:23]/[0:25] What benefits?/for Naprit?`. Only a few CUE-LESS interjections (`[0:49]/[1:00]/[6:01]`, booker=prospect=0 cue score) got acoustically MERGED onto the booker cluster (same-room mono) → unseparable residual, NO role logic can isolate them (they share cluster 0 with real booker turns). The 07-13 catastrophe (136/136 unusable, inversionSuspected true) was a FALSE whole-call-inversion flag from the semantic detector that NUKED all usable evidence on a call whose orientation is actually CORRECT (opening heuristic fit_default=2) → comprehension_gap went blind. So the real fix for the user's complaint = suppress the false inversion so comprehension_gap fires on the correctly-labeled confusion lines; relabeling `[0:49]` is infeasible + not coaching-critical.

**Fix plan (design doc committed at `docs/app-plan/implementation/role-attribution-fix/design.md`; 4 slices, all gated BEHIND the channel-ground-truth short-circuit so zero dual-channel risk) — RE-SCOPED 2026-07-13 per the spike:** S1 (re-scoped Prong A) = new `b1OpenerRoleAnchor.ts` deterministic opener ORIENTATION authority: high-conf conflict→whole-call remap (fixes the whole-inversion class + determinism); high-conf AGREE→confirm + SUPPRESS the semantic false whole-call-inversion (restore usable spans so comprehension_gap can fire) — this is the user-visible fix for THIS call; low-conf→abstain. S2 Prong B = mono analog of CA-2b F2 flip-guard. S3 Prong C = thread `semanticRoleReliability` into `buildScoreAvailability`+`canCreateRepCoaching` so abstain+high-unusable → NO score + honest reason (never fabricate a pass). S4 = comprehension_gap-fires-AND-wins verification + determinism(seed) harness. **Honest Test 1 (DROP the `0:49→CLIENT` assertion — infeasible): whole-cluster-inversion corrected + mixed-cluster NOT over-corrected + false-inversion-suppressed-restores-spans + comprehension_gap fires on 0:23/0:25.** Proof = replay session 66e18d42's stored `Session.transcript`; mono 18/136 baseline stays authoritative; dual-channel byte-identical. DO NOT re-run/overwrite 66e18d42 — it's the before/after fixture. Slice-1 impl worktree `${WORKSPACE:dev|backslash}\coachai-wt-rolefix-s1` branch `fix/role-attribution-s1-opener-anchor`.

**Amin product decisions (settled 2026-07-13):** unrecoverable-mono (no clean opener AND voices unseparable) → **NO score + honest reason** ("couldn't reliably tell who was speaking — upload with separate channels / check audio"), never a fabricated number (Slice 3 uses this). Reliability gate downgrades AUTHORITY only, never fabricates a pass.

**PROGRESS 2026-07-14:** S1 (opener anchor + false-inversion veto) + S2 (per-line flip-guard) DONE, committed on `fix/role-attribution-s1-opener-anchor` (S2 head `7c9d2744`, S1 `f79e59b0`), each verified GREEN independently by the orchestrator (line-by-line read of the anchor/veto/gating + full `npm run verify` = 461 frontend tests + `gate:ai-output-authority` + all gates). S3 (reliability gate) IN FLIGHT (agent). **Verified from the STORED transcript (read-only Neon SELECT, project snowy-glade-91475703, `Session.transcript` = plain-text `[ts] [SPEAKER] text`):** 66e18d42 is a REP-DOMINATED appointment-set call with a CLEAN opener ("This is Jen ... calling from Kumas' office with AIL"); only ~4 real mislabels ([0:49]/[1:00]/[1:26]/[6:01] = cue-less CLIENT interjections merged onto the booker voice) out of ~90 lines — so **complaint-3 "most end lines are clients attributed to Jehan" is an OVER-GENERALIZATION** (the end is genuinely the rep's scheduling/email monologue, correctly BOOKER). Real coaching problem CONFIRMED = [0:23]/[0:25] CLIENT "What benefits?/for Naprit?" → [0:28] rep jumps to explaining benefits instead of re-anchoring the Navpreet referral credibility (the comprehension gap). The nonsensical "schedule was blocked" verdict traces to [6:14] "I'm blocking the schedule for you right now" (the rep's SCARCITY persuasion line, misread by the AI as a literal problem). **66e18d42 MUST stay SCOREABLE** (clean opener → reliable/limited, NOT unavailable) so its comprehension coaching shows — S3 calibration is pinned on this + a synthetic unrecoverable→unavailable fixture. Env gotcha: CoachAI worktree `npm run verify` needs a dummy `DATABASE_URL='postgresql://user:pass@localhost:5432/coachai?schema=public'` (prisma:generate loads it though it never connects). Op-lesson: never run 2 agents in ONE worktree — S2 was dispatched into S1's worktree + died on a ConnectionRefused, contaminating S1's committed tree (isolated via `git stash`, now strictly sequential single-writer per slice).

**FOLD-IN (Amin directive 2026-07-14): also merge 2 CoachAI PRs with the role-fix endgame** — **#216** `fix(telemetry): interpolate printf directives in the console guard` (files: `telemetry.ts` + `observabilityRedactionRegression.ts` + `.claude/agents/release-verifier.md` — DISJOINT from role-fix, zero conflict) and **#217** `refactor(analysis): consolidate console.time stage timings onto logAnalysisStageTiming` (files: `analysisPipelineCore.ts` + `runAnalysis.ts` + `runSandboxAnalysis.ts` + `reprocessCoachingSession.ts` — #217 edits analysisPipelineCore at ~1113/~2416, role-fix S3 edits ~1266/~2317, NON-overlapping → minor mechanical rebase conflict only). Both MERGEABLE + disjoint from each other; both fail ONLY the 2-3s zero-step `dialer-db-regressions`+`gates` remote-CI checks (flaky infra, not code — Vercel deploys green) → verify LOCALLY before merge, don't trust/distrust remote CI. Endgame order: finish S3/S4 + whole-branch review → local-verify #216+#217 → merge #216+#217 to main → rebase role-fix onto updated main (resolve #217↔analysisPipelineCore) → re-verify → merge role-fix → final live before/after proof on 66e18d42.

**ADVERSARIAL REVIEW (2026-07-14) — DEFECTS-FOUND, NOT merge-ready (blocker VERIFIED by orchestrator):** **BLOCKER** — S2's per-line flip-guard is an INVALID generalization (it uses S1's WHOLE-CALL orientation anchor as a PER-LINE principal lock; the CA-2b F2 guard it copied works only because a CHANNEL is per-line truth — a mono whole-call orientation is NOT). Under any branded opener it disabled the per-line prospect-recovery repairs for the whole call → `test:regression:b1-speaker` RED (a genuine strong-cue prospect deflection stayed BOOKER) AND it was the very thing keeping 66e18d42's cue-less interjections ([0:49]/[1:00]/[6:01]) mislabeled BOOKER (the context-recovery branch correctly flips them to CLIENT when NOT anchor-locked). **MAJOR** — D6 breach: `degradedSemanticFactsPhase1` (semanticExtract.ts:~721) calls `buildEvidenceSpansWithNormalization(gemini)` with NO channelGroundTruth → a dual-channel call whose extract throws → degraded path runs the opener veto on the non-GT path → S3 gate can no-score a GT call. **2 MINORS** — `limited` caveat promised in docs but never rendered (frontend keeps confidence internal — reconcile DOCS, not add UI chrome); loose `Proves: CQ-014` headers. **CONTROL GAP** — `b1-speaker` + the role-regression battery are NOT in `verify`/`gates:all`, so a RED guard shipped GREEN (my verify was necessary-not-sufficient). **FIX IN FLIGHT (agent a5013ce):** neutralize S2's per-line anchor lock (keep S1 orientation+veto & S3 gate intact + the pre-existing channel-GT guard) → b1-speaker green + interjections correctly → CLIENT (fixes complaint-3 as a bonus); thread GT into the degraded path; reconcile docs/headers; WIRE the role battery into a gate. Claims that SURVIVED review: S3 downgrade-only/no-fabricated-pass, 66e18d42-never-no-scored, no missed score-authority consumers, new-test bite — all HOLD. **After-proof:** 66e18d42 audio is `local` storage ("Jehan recording 1.mp3", finalized 2026-07-06) — may not be retained; produce the "after" from the RETAINED transcript via a scratch replay (user has no original audio, can't re-upload).

**CHECKPOINT 2026-07-14 (PAUSED — user switching internet):** the ENTIRE role-fix is COMPLETE + verified on branch `fix/role-attribution-s1-opener-anchor` @ **`f6b6a1c0`** (worktree `${WORKSPACE:dev}/coachai-wt-rolefix-s1`), commits: 571469d8 design, f79e59b0 S1, 24f2f06b filemap, 7c9d2744 S2(neutralized later), 6e8af31f S3, a88952d1 review-blocker-fix (S2 per-line lock removed + D6 GT + minors + `gate:role-battery` wired into gates:all/verify), f6b6a1c0 openerAnchor dead-exposure cleanup. Full `verify` last GREEN on a88952d1; f6b6a1c0 targeted-verified (backend tsc + gate:role-battery + gate:dead-exports all 0 — cosmetic backend-only removal). NOT pushed, no PR. REMAINING ENDGAME (network-heavy, do when user reconnects): (1) local-verify + `gh pr merge` #216 + #217 to main; (2) `git fetch` → rebase `f6b6a1c0` onto origin/main (resolve #217↔analysisPipelineCore.ts, non-overlapping regions) → full `verify` → push → PR → merge; (3) release-verify the deploy; (4) produce the before/after (re-analyze the retained audio if present, else the transcript, into a NEW scratch session — never overwrite 66e18d42). Lesson captured: a dead-code-removal "no consumer" claim must grep the TEST/SCRIPTS trees (`**/*Regression*`, `**/scripts/**`, `**/*.test.*`), not just `src` — my openerAnchor brief missed 2 regression consumers; the cleanup agent's escalation caught it (owed: wire this into the global dispatch-brief claim-verification rule). Related: [[coachai-no-outcome-analytics]], [[doctrine-upgrade-2026-07-02]].

**PROGRESS 2026-07-13 (orchestrator, this session):** worktree `${WORKSPACE:dev|backslash}\coachai-wt-rolefix-s1` branch `fix/role-attribution-s1-opener-anchor` (unmerged; whole mono-fix stacks here, one PR at the end, human-gated merge). **Slice 1 DONE + orchestrator-verified** (commits `f79e59b0` main + `24f2f06b` filemap): `b1OpenerRoleAnchor.ts` (`detectOpenerRoleAnchor` cluster-level + `confirmOpenerOrientation` role-level, reuses `quoteHasOutboundCallerCue`, `SEMANTIC_DETERMINISM_ALLOW: opener-role-orientation-structural-authority`) + veto in `inferSpeakerRoleNormalization(…, {orientationConfirmed})` + `openerAnchor` seam exposed on participation result. Regression `openerRoleAnchorRegression.ts` (`test:regression:opener-role-anchor`) BITES: (a) whole-cluster inversion corrected, (b) 66e18d42 mixed-cluster AGREE-no-over-correct, (c) veto restores "What benefits?" as usable PROSPECT comprehension evidence w/ inverted-opener divergence contrast, (d) clean-mono not over-suppressed + GT never runs anchor. `npm run verify` EXIT 0 verified by orchestrator on the committed tree (worktree has NO `backend/.env` → set `DATABASE_URL="postgresql://u:p@localhost:5432/db?schema=public"` inline for prisma generate). NOTE: two agents raced ONE worktree (ghost-resume `a83999eb` wrote uncommitted → fresh `a4b92ec8` verified+committed) — no corruption, but never run 2 agents in 1 worktree again. **Slice 2 IN FLIGHT** (agent `adc47e8e`, Prong B anchor-scoped flip-guard in `b1ConversationParticipation.ts` overrides ~697/703 + CA-2b F2 ~758 + `b1SpeakerRoleMap.ts` repair sites). **Slice 3 targets grep-confirmed** (`callReviewAuthority.ts` buildScoreAvailability :700 / roleUnsafe gate :727 / canCreateRepCoaching :245 / roleUnsafe conflict block :168; thread `semanticRoleReliability` from `analysisPipelineCore.ts` buildCallReviewAuthority call). **Slice 4** = live-AI capstone (comprehension_gap fires+wins + off-script flag returns + determinism seed) — needs a real OpenAI key + a REPLAY/COPY, never 66e18d42. Classifier (claude-opus-4-8) flapping badly this session → blocks Bash/Agent/Edit intermittently; read-only ops unaffected; retry through bursts.
