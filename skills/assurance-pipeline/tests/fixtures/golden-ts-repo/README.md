# Golden TypeScript security fixture
This source-only mini-repository is the oracle for assurance-pipeline precision and recall tests.
Five isolated vulnerabilities are paired with close clean lookalikes to expose false negatives and false positives.
All credential-like values are FAKE, planted, non-real test values and must never be used as credentials.
`labels.json` is the machine-readable ground truth for every labeled source line.
