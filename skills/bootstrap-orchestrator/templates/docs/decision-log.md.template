<!-- TEMPLATE: the decision authority. Save to {{DOCS_DIR}}/.../decision-log.md (the repo's decision-log location). Seed with the bootstrap row + the scrapped-scope section. -->
# Decision Log — {{PROJECT}}

The decision authority (below only ADRs in precedence: ADR > decision-log row > always-on rule > doc prose). Every non-trivial decision actually made lands here; architectural ones ALSO get an ADR. A scrapped decision goes to the "Scrapped & Dropped" section and is do-not-resurrect — re-expanding killed scope is a fresh explicit user decision, never a latent assumption.

_Last reviewed: {{BOOTSTRAP_DATE}}_

## Settled decisions
<!-- The exact seven-column schema is consumed by gate:decision-sprint-linkage. Sprint uses exact plan targets or `n/a`. -->
| ID | Title | Area | Phase | Sprint | Status | Notes |
|---|---|---|---|---|---|---|
| {{DEC_ID_1}} | Bootstrap orchestrator/fleet/gates/living-docs model | orchestration | foundation | {{INITIAL_SPRINT_TARGET_OR_NA}} | approved | Basis: proven cross-repo operating model. Reversal: operating model is explicitly superseded. |
| {{DEC_ID_2}} | Single-source rules under `{{RULES_DIR}}` | context/rules | foundation | {{INITIAL_SPRINT_TARGET_OR_NA}} | approved | Basis: {{RULES_DIR_BASIS}}. Reversal: {{RULES_DIR_REASON}} changes. |
| {{DEC_ID_3}} | Domain auditor `{{DOMAIN_AUDITOR_NAME}}` owns {{CROWN_JEWELS}} | assurance | foundation | {{INITIAL_SPRINT_TARGET_OR_NA}} | approved | Basis: product must-not-break invariants. Reversal: crown jewels change. |
<!-- FILL/add rows for each real decision made during the bootstrap + as work proceeds. -->

## ⛔ Scrapped & Dropped (do-not-resurrect)
| ID | Scrapped scope | Why | Reintroduction rule |
|---|---|---|---|
| — | _(none yet)_ | — | Re-expanding is a fresh explicit user decision, never a latent assumption |

## Structured pending/deferral register

<!-- Keep machine-readable pending decisions in the project path configured by gate:decision-sprint-linkage.
     Every row needs an owning decision ID, reason, owner, and review trigger/date. Empty is valid. -->
