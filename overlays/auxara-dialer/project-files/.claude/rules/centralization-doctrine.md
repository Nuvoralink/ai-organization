---
description: No hardcoded strings, IDs, copy, fonts, colors, permission strings, role names, error codes, event names, or contract shapes inside individual files. Every domain has a single central registry; everything reads from it.
paths:
  - "backend/src/**/*"
  - "frontend/src/**/*"
  - "shared/src/**/*"
  - "scripts/**/*"
---

# Centralization Doctrine — One Source of Truth Per Domain

Purpose: prevent the most-expensive class of architecture drift in a multi-tenant, multi-surface SaaS — the kind where the same concept gets restated in 30 different files, each version slightly different, and nobody notices until prod data is inconsistent or a tenant sees mismatched copy.

The rule: **no individual file holds domain truth.** Every domain has ONE central registry. Every consumer reads from that registry. Changing the truth means changing one row, not 30 files.

This rule applies to every task that touches code, prompts, docs, or content. The CI gate `npm run gate:doc-code-drift` enforces structural pieces of it.

---

## 1. Central registries by domain

| Domain | Central registry | Consumers must source from |
|---|---|---|
| **Roles** | `shared/taxonomy/roles.ts` (`Role` enum + display labels) | every RBAC check, UI role-mirror, glossary doc |
| **Permissions** | `shared/taxonomy/permissions.ts` (`PermissionKey` enum + descriptions + categories) | `requirePermission()` middleware, UI permission-mirror, custom-roles-builder |
| **Permission × Scope mapping** | `shared/taxonomy/rolePermissions.ts` (default `role_permissions` rows for system roles) | seed migrations, RBAC matrix tests, glossary |
| **Tenant kinds** | `shared/taxonomy/tenantKind.ts` (`CUSTOMER` / `INTERNAL`) | stack-bound JWT, signup flow, internal-admin guard |
| **Dial/run and dispatch modes** | `shared/src/taxonomy/dialRuns.ts` (`OUTBOUND_DISPATCH_KIND` + run/participant/dispatch lifecycles; team power is the sole automated dial lane, ADR-DLR-001/DLR-016) | `services/dialRuns.ts`, `services/dialerEngine.ts`, dial-run routes/contracts, billing attribution |
| **Number lifecycle states** | `shared/taxonomy/numberLifecycle.ts` (`ACTIVE` / `COOLING` / `INBOUND_ONLY` / `RELEASED`; DB enum `warming` is dormant legacy only) | state-machine guard, lifecycle UI badge, rotation picker |
| **Call states** | `shared/taxonomy/callStates.ts` (`IDLE` / `DIALING` / `RINGING` / `CONNECTED` / `WRAP_UP` / `INBOUND`) + the legal-transition map (`callMachine.ts` reducer) | `frontend/src/context/CallProvider.tsx` — the ONE shared owner of the live call: the single `TelnyxRTC` client, the single `/call-events` socket, and the single `callMachineReducer` instance (E3-2a). The cockpit (`SoftphonePage`) and Companion consume it via `useCallContext()` — neither owns its own WebRTC/socket/reducer instance. Hotkey scoping, wallboard, analytics read the provider's state |
| **Cockpit view** | `shared/src/taxonomy/cockpitView.ts` (`GUIDED` / `STREAMLINED` + `DEFAULT_COCKPIT_VIEW` + `isCockpitView`) — the per-agent connected-stage layout (ARC-006 Tier-2; BUX-017) | the `/api/me/cockpit-view` GET+PUT validator, the `MeCockpitView` DTO, the cockpit connected-stage render (`SoftphonePage` → guided/streamlined), the interim topbar toggle |
| **Hotkey actions** | `shared/taxonomy/hotkeyActions.ts` (BUX-008 bindable-action registry: defaults, unbound actions, reserved keys, `validateBindings`) | `useHotkeys` engine, the remap modal, the backend `/api/me/hotkeys` validator (one validator, both halves) |
| **Pre-shift gate** | `shared/contracts/preShift.ts` (measurement + verdict DTOs) + `THRESHOLDS.preShift` defaults | `usePreShiftGate` (measures), `services/preShiftGate.ts` (verdict authority), `tenants.pre_shift_config` overrides |
| **Disposition source** | `shared/src/taxonomy/dispositionSource.ts` (`AGENT` / `AI_DRAFT_ACCEPTED` / `AI_DRAFT_EDITED` / `SYSTEM`) | calls writer, AI-edit-rate dashboard |
| **System disposition (DLR-013 + B04 VM completion)** | `shared/src/taxonomy/systemDisposition.ts` (`SYSTEM_DISPOSITION_CODES` no_answer/busy/failed/voicemail + `CAUSE_TO_SYSTEM_CODE` Telnyx-cause→code map (no-contact only; voicemail is NOT in this map) + `resolveSystemDispositionCode` + `SYSTEM_DEFAULT_DISPOSITION_SEED`) — the deterministic factual outcome → `disposition_source = system` vocabulary | `services/autoDisposition.ts` is the one taxonomy/booker-precedence/event writer; `services/callProjection.ts` selects no_answer/busy/failed from a terminal never-answered call, while `domains/media/voicemailDropDelivery.ts` selects voicemail only after the human-requested Drop receives signed completed-playback evidence. AMD alone never calls the writer. Never inline a Telnyx hangup cause string or system code at a leaf (Gate 11) |
| **Default disposition taxonomy (BUX-004)** | `shared/src/taxonomy/defaultDispositionTaxonomy.ts` (`DEFAULT_DISPOSITION_TAXONOMY` — the 9 built-in default rows + flags = 5 booker + composing F4+F5's `SYSTEM_DEFAULT_DISPOSITION_SEED` for the 4 system codes no_answer/busy/failed/voicemail; `DEFAULT_DISPOSITION_TAXONOMY_NAME`) — the ONE built-in default set a tenant's `is_default` taxonomy is provisioned with | `services/seedDefaultDispositionTaxonomy.ts` (signup-tx seeder, idempotent), `routes/auth.ts` (signup), `prisma/migrations/0019_…` (8-row backfill) + `0020_…` (voicemail delta backfill); never inline a default code/label/flag at a leaf (Gate 11) |
| **AMD mode, result + Telnyx mapping** | `shared/src/taxonomy/amdResult.ts` (`AMD_MODES`: `off` / `standard` / `premium`; `ACTIVE_PROSPECT_AMD_MODE`; `TELNYX_AMD_PARAM_BY_MODE`; the passive result vocabulary and Telnyx→internal mapping) | `dialerEngine.ts` selects the one active prospect-leg mode; `telnyx-call-control.ts` maps that mode to the provider parameter and emits usage only for a paid mode; `webhooks/telnyx.ts` + `amdProjection.ts` reduce provider facts to `call_events` / `calls.amd_result`; DTO, wallboard, and VM-drop consumers may read the passive fact but never act autonomously. Never add a second AMD boolean, inline a provider parameter, or create a separate metering seam (DLR-014; Gate 11) |
| **AI validation status** | `shared/src/taxonomy/validationStatus.ts` (`PASSED` / `REPAIRED` / `REJECTED`) | bounded-repair flow, trace-truth test |
| **Objection labels** | `shared/src/taxonomy/objections.ts` (`too_expensive` / `already_covered` / `spouse_decides` / `...`) | battlecards, AI disposition evidence, CoachAI handoff |
| **Line provenance** | `shared/src/taxonomy/lineProvenance.ts` (`your_playbook` / `suggested_response` / `manager_approved` / `ai_generated`) | battlecard content, manager-approved lines, AI-suggested lines |
| **Subscription access status** | `backend/src/services/tenantSubscription.ts` (the live subscribed/inactive verdict seam; no shared subscription-status enum exists. BIL-001 EXTENDED it with the card-verification verdict `getTenantBillingState`/`isTenantBillingVerified`, reading `billing_customers.card_verified_at` written only by the signed Stripe `setup_intent.succeeded` webhook) | telephony credential broker and `SUBSCRIPTION_INACTIVE` response path; the onboarding "activate calling"/number-provisioning gate reads `isTenantBillingVerified`; future dunning + subscription-lifecycle vocabulary must extend this authority rather than inventing a shared leaf enum |
| **Appointment status** | `shared/src/taxonomy/sprint14Foundation.ts` (`APPOINTMENT_STATES` + the SMS-eligible subset + `APPOINTMENT_BOOKED_STATES` — the "counts as a booking the booker made" subset, `cancelled` excluded, enumerated so a NEW state must be classified rather than silently counted) | booking authority, provider webhook reducers, message-draft eligibility, and the booker shift-stats appointments count (`services/shiftStats.ts`); never inline a state literal at a query leaf (Gate 11) |
| **Analytics event names** | No implemented global event-name registry yet; a future emitter MUST introduce one here before emitting a new cross-surface event | every future emitter, aggregator, and dashboard; existing domain-event vocabulary is separately owned by `shared/src/taxonomy/sprint14Foundation.ts` |
| **Error codes** | `shared/taxonomy/errorCodes.ts` (stable machine-readable codes + safe user-visible messages) | every error response, frontend error consumer |
| **Compliance gate reasons** | `shared/taxonomy/complianceReasons.ts` (`CALLING_HOURS_VIOLATION` / `DNC_HIT` / `DNC_FRESHNESS_EXCEEDED` / `CONSENT_MISSING` / `RECORDING_DISCLOSURE_MISSING_AUDIO` / `…`) | compliance gate, audit log, UI reason display |
| **Prospect DNC status** | `shared/src/taxonomy/dncStatus.ts` (`DNC_STATUSES` — the 5-value Prisma `DncStatus` mirror; `SUPPRESSED_DNC_STATUSES` (Set) + `SUPPRESSED_DNC_STATUS_VALUES` (array for a Prisma `in`/`notIn`) — the Tier-1a TERMINAL never-dial/never-text triple `opt_out`/`internal_dnc`/`scrubbed_dnc`, ADR-CMP-001) | the voice-dial queue resolver + the raw/manual first-contact DNC block (`services/dialerEngine.ts`) and the SMS-send recipient suppression (`services/smsSend.ts`) all read the suppressed set from here; never inline the terminal triple at a leaf (Gate 11; `dnc-status-centralization.test.ts` fails on a re-inline). NOT the `dispositionSave.ts` `{opt_out, internal_dnc}` write-precedence pair — a different, narrower set (scrubbed_dnc deliberately excluded: a booker DNC may upgrade a federal-scrub match) |
| **Emergency short codes (CMP-016 / REQ-CMP-014)** | `shared/src/config/emergencyShortCodes.ts` (`EMERGENCY_SHORT_CODES` — the provider-defined `911`/`933` set + kinds; `classifyEmergencyShortCode` for raw human formatting; `resolveVoiceDialDestination` — the ONE voice-dial destination resolution, composing the classifier with `normalizeNanpE164`). E911 is **fail-closed** because Telnyx's current contract explicitly excludes emergency calls from Call Control (`provider-proof/B08-TELNYX-E911-TRANSPORT-PROOF.md` = `current_call_control_transport_unsupported`, grounded by Telnyx SIP response code D37); this registry recognizes short codes, it never claims readiness | `dialerEngine.dialRaw` (raw operator input), `callsRouter` + `dialerEngine.listEligibleFromNumbers` (caller-ID recommendation reads), and `dialerEngine.dispatchOutboundDial` (the one choke point every voice producer passes — fail-closed backstop before the compliance gate/audit/dispatch/call/provider). Never inline a `911`/`933` literal or a second short-code regex at a dial leaf (Gate 11), never resolve a raw voice destination with a bare `normalizeNanpE164` (that demotes a short code to a generic VALIDATION), and never read `users.e911_status`/`emergency_address`/`telnyx_e911_address_id` as readiness — that seam is dormant with zero dial consumers. Distinct from `supportedDestination.ts`: per decision-log OPS-005, "911/933 handling is CMP-016, not this row". Enforced behaviorally by `backend/src/__tests__/e911-dispatch-backstop.test.ts`, `backend/src/__tests__/e911-from-eligible-parity.test.ts`, and the DB-backed `backend/src/__tests__/e911-route-fail-closed.test.ts`; `backend/src/__tests__/e911-short-code-fail-closed.test.ts` is the source-ordering backstop |
| **Jurisdiction identity (CMP-004/CMP-013)** | `shared/src/config/jurisdiction.ts` (the total US/CA subdivision registry, canonical code/name/country, evidenced subdivision NPAs, normalizer, recognized-token and unambiguous/ambiguous NPA indexes). It owns identity only, never legal policy and never the independent country-per-NPA axis | calling-hours, recording-disclosure, and contact-frequency authorities consume it; `complianceGeo.ts` may expose derived compatibility adapters until Phase B retires them. A consumer MUST resolve raw code/name input to the canonical subdivision code before any code-keyed legal-policy lookup; recognizing a full name only for country while comparing its raw text to a code set is a split normalization and can invert the verdict. Never add a second state/province alias, subdivision inventory, or hand-maintained NPA-country classifier; `gate:jurisdiction-inventory` fails on a parallel authority |
| **Compliance geo / windows** | `shared/src/config/complianceGeo.ts` (state/area-code → IANA tz, federal + per-state calling-hours windows, all-party-consent state set, Canada/PIPEDA derivation — CMP-004/006/007) | the deterministic compliance gate (`backend/src/services/complianceGate.ts`); never inline a state code / window hour / tz literal at a leaf (Gate 11) |
| **Weather area-code location authority (BUX-010)** | `shared/src/config/complianceGeo.ts` (`WEATHER_AREA_CODE_LOCATIONS` + `weatherLocationForAreaCode`: curated NANP NPA → coarse representative city query; not prospect location truth) | `backend/src/services/weather.ts` only; never send a phone, tenant/lead id, address, exact coordinates, or an independently inlined area-code map to the provider |
| **Human-action compliance mode (CMP-012/013/014)** | `shared/src/taxonomy/humanActionComplianceMode.ts` (`block` / `confirm` / `off` + `HUMAN_ACTION_COMPLIANCE_MODES` + type-guard) — the shared human-triggered calling-hours, contact-frequency, and SMS quiet-hours posture | `complianceGate.ts`, `complianceConfig.ts`, `domains/messaging/smsQuietHours.ts`, shared calls/compliance contracts, and the three tenant mode columns; never inline a mode string at a leaf (Gate 11) |
| **Recording-disclosure policy (CMP-006/007)** | `shared/src/taxonomy/recordingDisclosurePolicy.ts` (`all_party` / `always` / `off` + `RECORDING_DISCLOSURE_POLICIES` + type-guard) — the recording-consent-notice posture. **Moved here from `backend/src/services/complianceGate.ts` (2026-07-01) so the Prisma enum, the gate, AND the /settings Compliance section share one source** | the compliance gate disclosure branch (`complianceGate.ts` re-exports it for its own consumers), the Prisma `RecordingDisclosurePolicy` enum, `ComplianceConfigDTO`, the Compliance section; never inline a policy string at a leaf (Gate 11) |
| **Compliance capabilities (Set-B; ADM-004 / CMP-020)** | `shared/src/taxonomy/complianceCapability.ts` (`COMPLIANCE_CAPABILITIES` — the 5 tenant-owned Tier-1b capabilities; each value IS the `tenants` column name) | the `compliance_config_change.field` audit vocabulary, the PATCH diff + risk classifier (`isRiskIncreasingTransition`), `complianceConfig.ts`; never inline a capability/column string at a leaf (Gate 11) |
| **Compliance-config contract (ADM-004 / CMP-020)** | `shared/src/contracts/compliance.ts` (`ComplianceConfigDTO` + the PATCH zod schema + `isRiskIncreasingTransition` (the ONE risk classifier both halves reason from) + `canonicalComplianceValue` + `ComplianceConfigChangeDTO`) | the tenant compliance-config route + service (`routes/tenant.ts` / `services/complianceConfig.ts`), the frontend Compliance section, `api.settings.*` |
| **API contracts (DTOs)** | `shared/contracts/*.ts` (one file per resource: `auth.ts`, `calls.ts`, `recordings.ts`, `conversations.ts`, `appointments.ts`, etc.) | both backend producer + frontend consumer + CoachAI handoff |
| **BullMQ custom job IDs** | `backend/src/lib/bullmqJobId.ts` (bounded SHA-256 of byte-length-prefixed arbitrary parts) | every production `Queue.add` / scheduler `jobId`; enforced by `gate:bullmq-job-ids` |
| **Disposition contracts** | `shared/src/contracts/dispositions.ts` | disposition taxonomy config, AI draft save, call wrap-up producer/consumer |
| **Conversation/SMS contracts** | `shared/src/contracts/conversations.ts` | conversations inbox, SMS webhook stitching, SMS send/read producers |
| **Battlecard contracts** | `shared/src/contracts/battlecards.ts` | battlecard config, keyword trigger consumers, AI-009 objection vocabulary |
| **Teleprompter tree contract** | `shared/src/contracts/teleprompter.ts` | teleprompter config writes, BUX-005 node-tree validation |
| **Lead-list + import contracts** | `shared/src/contracts/lists.ts` (List/Prospect DTOs, `normalizeLeadRow`, import digest, DLR-010/CMP-005/INT-007) | lead-import route + service, the Power Dialer list-picker, future CRM lead-pull (INT-006) |
| **API endpoint registry** | `shared/contracts/endpoints.ts` (canonical URL paths for every route) | frontend `lib/api.ts`, backend route table verifier |
| **Coaching/booker copy** | `shared/copy/*.ts` (one file per surface: `softphone.ts`, `wallboard.ts`, `admin.ts`, etc.) | every component; never inline string literals for user-facing copy |
| **Design tokens — primitives (Tier 1)** | `frontend/src/design-system/tokens/primitives.css` (OKLCH ramps + space/size/radius/duration/easing/z/opacity/blur/type scales) | ONLY Tier-2 semantic + Tier-3 component tokens reference these — never app/component code directly |
| **Design tokens — semantic (Tier 2)** | `frontend/src/design-system/tokens/semantic.css` (surface/text/border/accent/state/elevation/ambient · light + dark parity) | every component/page via `foundations.*` or `var(--…)`; no inline hex/oklch/px/font/shadow |
| **Design tokens — domain** | `frontend/src/design-system/tokens/domain.css` (call-state, number-lifecycle, compliance) | lifecycle/status badges + glows; never a one-off color |
| **Design tokens — typed accessor** | `frontend/src/design-system/foundations.ts` (`foundations.color/font/space/radius/shadow/width/motion`) | inline styles / JS consumers — the type-safe handle to the semantic tokens |
| **Component primitives** | `frontend/src/components/ui/*.tsx` (`Button`, `Badge`, `Card`, `PageHeader`, etc.) | every page; no handcrafted Tailwind repeated across pages |
| **Page registry** | `frontend/src/app/pageRegistry.tsx` (`PAGE_REGISTRY`: canonical route → component → role-gate → nav metadata; `NAV_ITEMS` is the nav projection, `HOME_PATH` the root) | the router (`App.tsx` derives every authenticated `<Route>`/`<ProtectedRoute>`) **and** the nav rail (`WorkspaceLayout`/`NavRail` derive `NAV_ITEMS`). One source — a route + its gate + its rail item can't drift; `check:ui-continuity` enforces it |
| **Manager routes (deep links)** | `frontend/src/lib/managerRoutes.ts` (canonical destination → URL **builder**, param-taking) — *reserved name, not yet created; lands with the manager deep-link surfaces (2.0/2.1)* | every link that navigates between manager surfaces. **NOT a flat route-paths constant** — canonical route paths live in the Page registry above |
| **Stable test selectors** | each component owns its `data-testid="kebab-case-action"` per `auxara-dialer-frontend-rules.md` | E2E + integration tests |
| **Thresholds / numerics** | `shared/config/thresholds.ts` (callback tail, calling hours, ASR confidence cutoff, AI-edit-rate alert) | every consumer; tenant overrides via `tenants` columns |
| **Provider routing + prompt versions (AI policy)** | `backend/src/lib/aiTierPolicy.ts` + `aiModelPolicy.ts` + `aiPromptPolicy.ts` | every paid provider call and prompt producer; no direct SDK imports in routes, no inline prompt-version literals |
| **Media ingestion policy + canonical playback + object-key roots (AI-010 / ARC-009)** | `shared/src/config/mediaArtifacts.ts` (`MEDIA_PURPOSE_INGESTION_POLICY` — per-purpose upload/TTS admissibility, owner-scoping, container set, byte/duration bounds; `MEDIA_AUDIO_CONTAINER_DEFS` — accepted MP3/M4A/WAV container/codec/MIME/probe identities; `MEDIA_NORMALIZED_AUDIO_PROFILE` — canonical_mp3_v1 derivative parameters, deterministic object-key suffix, and explicit provider audio type; `MEDIA_OBJECT_KEY_ROOTS` + `mediaObjectKeyPrefix`/`mediaOwnerObjectKeyPrefix`) | every media upload/TTS/preview producer, derivative persistence/verification, provider playback call, and playback tenant-scope guard. `disclosureAudioObjectKeyPrefix` (`services/disclosureAudio.ts`) and `voicemailGreetingObjectKeyPrefix` (`services/voicemailFallback.ts`) DERIVE from this row — never re-type `disclosure/<tenantId>/` or `greetings/<tenantId>/` at a leaf, which is how a producer and its guard drift apart and the clip goes dead-on-arrival at the provider (INT-D1 B-1). `ttsAllowed:false` on `outbound_voicemail_drop` is the live refusal authority consumed by `routes/mediaArtifacts.ts` before any metered adapter call (S14-PF-E1/E2) — never replace it with a route-local purpose literal. Uploaded MP3/M4A/WAV bytes remain immutable provenance; personal preview/delivery resolve only the exact verified canonical child and Telnyx receives `audio_type=mp3`. **TWO INDEPENDENT AXES — do not re-conflate:** client-ingestion eligibility (`uploadAllowed`/`ttsAllowed`) answers "may a client hand us bytes"; object-key AUTHORITY (`MEDIA_OBJECT_KEY_ROOTS[purpose].authority` = `enforced` \| `pending_adoption` + a named `adoptionOwner`) answers "is this root's authorship gate-enforced". Deriving the second from the first made the former `recordings` exemption permanent-but-labelled-temporary, because a carrier recording must stay `uploadAllowed:false` forever. Enforced by `gate:media-object-key-roots` (source-text re-inline plus reverse-doc drift) + `media-artifact-ingestion-policy.test.ts` (runtime producer ⊑ guard); B07 resolved `MEDIA-KEY-ROOTS-B07-ADOPTION` on 2026-07-18, so `recordings` is enforced with no adoption owner while remaining un-ingestible. Threshold values are live provisional Auxara application-safety policy; Telnyx publishes no direct playback ceilings for those dimensions, so calibration requires measured corpus/deploy benchmarks and the values must never be cited as provider limits |
| **Advisory voicemail guides (P0-E1-B)** | `shared/src/contracts/voicemailGuides.ts` + `shared/src/taxonomy/sprint14Foundation.ts` review/audit vocabularies | `domains/media/voicemailGuides.ts` + `routes/voicemailGuides.ts`; text/version/review DTOs never carry voice playback, selection, or approval authority |
| **Disclosure audio policy** | `shared/src/config/disclosureAudio.ts` | disclosure audio service, media ingestion/finalization, compliance disclosure checks |
| **Contact-frequency policy (CMP-013)** | `shared/src/config/contactFrequency.ts` | live `domains/contact-frequency` evaluation/claim/challenge authority, manual preflight/dial, solo/shared queue and wake, provider barrier, webhook/recovery, audit/config, and migration/schema CHECK evidence; no route-local count/limit/subject resolver |
| **Provider-event inbox bounds (ARC-009)** | `shared/src/config/providerEventInbox.ts` | migration/schema CHECK evidence is live; runtime receipt normalization has not adopted these bounds yet and must not invent a parallel limit |
| **Recording retention (REC-002)** | `shared/src/config/recordingRetention.ts` | recording access/finalize/lifecycle, deletion queue, retention contracts and workers |
| **SMS quiet hours (CMP-014)** | `shared/src/config/smsQuietHours.ts` | `domains/messaging/smsQuietHours.ts`, SMS send final authorization, immutable attempt evidence |
| **Sprint 1.4 operational bounds** | `shared/src/config/sprint14Foundation.ts` | internal-DNC, inbound routing, call-control, media generation, booking, and frontend WebRTC/API timeout consumers |
| **Supported destination (OPS-005)** | `shared/src/config/supportedDestination.ts` (`resolveSupportedDestination` + `supportedCountryForAreaCode`: maintained numbering-plan-backed positive US/CA classification; unsupported/non-US/CA/unassigned NANP is unresolved, never inferred as US by exclusion) | dialer final destination authorization, call-control service, SMS quiet-hours destination resolution, canonical jurisdiction country fallback, and retained `complianceGeo.countryForAreaCode` adapter |
| **OAuth configuration** | `shared/src/config/oauth.ts` | OAuth protocol routes, authorization/token services, first-party app-connection flows |
| **Terms & Conditions acceptance (ADM-001 / ADR-CMP-001)** | `shared/src/config/terms.ts` (`TERMS_URL` — the public Terms link target; `TERMS_VERSION` — the accepted version stamped into the signup audit evidence) | the signup Terms link (`SignupPage` frontend) + the `auth.signup` `audit_log.payload` version stamp (backend `provisionOwnerWorkspace` saga); never inline a terms URL/version at a leaf |
| **Connection state** | `shared/src/taxonomy/connectionState.ts` | integration/app-connection contracts and connection status projections |
| **Call event types** | `shared/src/taxonomy/callEventTypes.ts` | Telnyx webhook normalization, call projection, realtime call-event consumers |
| **Call end reasons** | `shared/src/taxonomy/callEndReason.ts` | call projection, terminal reconciliation, call DTOs |
| **Integration scopes** | `shared/src/taxonomy/integrationScopes.ts` | OAuth protocol, middleware authorization, integration access audit |
| **Operator type** | `shared/src/taxonomy/operatorType.ts` | call persistence/contracts; Phase 1 remains human-only |
| **Authorization scopes** | `shared/src/taxonomy/scopes.ts` | permission registry, RBAC grants, authorization middleware |
| **SMS keywords** | `shared/src/taxonomy/smsKeywords.ts` | inbound SMS STOP/HELP normalization and suppression handling |
| **10DLC vetting status** | `shared/src/taxonomy/tcrVettingStatus.ts` | tenant TCR state, vetting poller, SMS final authorization |
| **10DLC per-number texting status (ADM-007)** | `shared/src/taxonomy/tenDlcTextingStatus.ts` | the Number pool + number-detail Texting column, the number↔campaign attach service, the 10DLC registration dashboard; it is the tenant-facing "can this number text" vocabulary and never a second vetting vocabulary |
| **10DLC registration contract (ADM-002/ADM-007/ADM-008)** | `shared/src/contracts/tendlc.ts` (brand-status/campaign DTOs, the company vs sole-proprietor `TenDlcBrandSubmitRequest` union, business-ID kinds, company entity types, mobile-verification and number-attachment DTOs) | the 10DLC routes and Telnyx brand/campaign adapters, the guided registration surfaces, the Number pool Texting column and buy-dialog campaign picker; carrier/registry limits live in `THRESHOLDS.tenDlc`, never at a leaf |
| **10DLC guided-start derivation (ADM-002 / ADM-008)** | `shared/src/config/tenDlcRegistration.ts` (`TEN_DLC_REGISTRATION_COUNTRIES` — the selectable countries of registration + the `primary` US/CA display grouping; `tenDlcBusinessIdKindForCountry` — country → the ONE adaptive business-ID kind, an unlisted country resolving to `CORP_REGISTRATION` (the local corporation-registration identifier) rather than a US-shaped default; `TEN_DLC_SOLE_PROPRIETOR_COUNTRIES` + `tenDlcSoleProprietorAvailable` — TCR accepts an INDIVIDUAL from the US/CA only (ADM-008); `tenDlcCountryName`; `TEN_DLC_USE_CASES` — the TCR use-cases the campaign step offers) | the guided registration steps under `frontend/src/pages/settings/sections/tenDlc/`: `GuidedStart.tsx` (country options + the previewed ID kind), `BusinessStep.tsx` (which ID the business form asks for + the country label), `registrationState.ts` (`guidedOutcome` — the company / sole_proprietor / unavailable branch), `CampaignStep.tsx` + `RegistrationDashboard.tsx` (use-case options + value→label). The guided start asks exactly two questions and EVERYTHING downstream adapts from the answer, so never write `country === 'US' ? 'EIN' : …`, a second country or ID-kind list, or a bare use-case code at a leaf (Gate 11). It owns WHICH country resolves to which identifier only — `TEN_DLC_BUSINESS_ID_KINDS` in `shared/src/contracts/tendlc.ts` owns the ID VOCABULARY and is imported, never restated — and `TEN_DLC_USE_CASES` carries only the use-cases our own authorities name first-hand, growing when a grounded TCR source is read, never from memory |
| **Message-template placeholders (CONV-004)** | `shared/src/taxonomy/messagePlaceholders.ts` | message-template contracts, server-side placeholder renderer, draft/send unresolved-key guard |
| **Sprint 1.4 domain vocabulary/ownership (ARC-009)** | `shared/src/taxonomy/sprint14Foundation.ts` | Sprint 1.4 contracts and domain services plus `gate:s14-authority-boundaries`; Prisma is a persistence mirror, not a second vocabulary |
| **Recording authority** | `backend/src/lib/recordingStorageAuthority.ts` | every recording existence check; never `STORAGE_DRIVER` env or local state |
| **Recording availability (current generation)** | `backend/src/domains/media/recordingAvailability.ts` (`RECORDING_AVAILABILITY_MANIFEST_SELECT` + `recordingCurrentSources` + `recordingAvailabilityState`) | every surface answering "does this call have a playable recording right now?" — recording access/playback (`domains/media/recordingAccess.ts`), the call DTO mapper (`lib/callMapper.ts` — `RECORDING_PROJECTION_SELECT` IS this select). Those two are the ONLY direct readers; every other surface is an indirect FEEDER that fetches with the re-exported select and hands the row to the mapper — `routes/calls.ts:551`, `routes/integrationApi.ts:600`, `services/dialerEngine.ts:301`, `services/webhookOutbox.ts:361` (plus `services/integrationCallResource.ts` + `routes/dialRuns.ts`, which consume the mapper's `RecordingProjectionSource`/`CallDtoSource` shape). The feeder side is TYPE-ENFORCED: `CallDtoSource.s14RecordingManifest` is `RecordingAvailabilityManifest | null`, so hand-picking a narrower select fails `tsc` at the `toCallDTO` call. A consumer may NOT hand-pick a narrower manifest select or re-decide availability locally: the scalar `masterArtifactId` is a single-occurrence compatibility pointer (a valid multi-occurrence/transfer manifest deliberately has it `null`), so a local scalar-master predicate strands every transferred recording at `pending` forever — the exact drift this registry replaced (2026-07-17, B07) |
| **Deletion reconciliation routing** | `backend/src/domains/media/recordingLifecycle.ts` (`RECORDING_DELETION_RECONCILIATION_REASONS`) | the expired-claim recovery writer + BOTH deletion lanes. The ordinary lane EXCLUDES exactly what the reconciliation lane SELECTS, so a re-inlined literal silently strands (or double-schedules) a post-irreversible quarantined operation |
| **Telemetry redaction** | `backend/src/lib/telemetry.ts` (one secret/PII value-pattern registry) + sink-specific shape policies such as `backend/src/lib/safeAiUsageMetadata.ts` | every log/Sentry/audit/usage writer; never duplicate value-pattern registries or add route-local redaction |
| **Layout · scroll · IA** | `docs/design-system/layout-and-ia.md` (the standard) + `AppShell` / `ScrollArea` / `PageHeader` primitives (built) + `Section` (to build) | every page — scroll ownership (the shell scrolls, not pages) + the tiered IA model + the card/sections/tabs/disclosure rubric; pages compose, never reinvent |
| **Page-header surface** | `frontend/src/design-system/app-header.css` (`.app-header` — a FROST PANE: transparent background + backdrop-blur + sticky; the shell's ambient flows through — 2026-07-06, user-approved; never an opaque fill) | every page header — the `PageHeader` primitive composes it; a rich page header (cockpit topbar, comms header) co-applies `app-header` in JSX. ONE surface source; an ad-hoc hand-rolled header treatment is a bug. `check:header-surface` enforces it |
| **Responsive · layout modes** | `docs/design-system/responsive-design.md` (the standard, BUX-021 — sibling of `layout-and-ia.md`) + `frontend/src/design-system/tokens/breakpoints.css` (the ONE breakpoint scale + fluid-type / touch / safe-area tokens) | every visible surface — the per-band layout MODE contract, the adaptation-pattern catalog, the dimension-fiddling ban + §4 repair ladder, the intrinsic-first hard rules; `check:responsive` (WARN) enforces the mechanical un-adapted-layout shapes |

(This table grows as new domains emerge. Adding a new central registry requires a row here + a row in `assurance/source-of-truth-map.md`.)

## 2. Forbidden patterns

These are bugs even if they compile and tests pass:

- ❌ Inline string literal for user-facing copy: `<Button>Save</Button>`. **Use** `<Button>{copy.softphone.actions.save}</Button>`.
- ❌ Inline hex color: `className="bg-[#3B82F6]"`. **Use** the token: `className="bg-brand"` (mapped through tailwind config).
- ❌ Inline font: `style={{ fontFamily: 'Inter' }}`. **Use** the token: typography.body.
- ❌ Magic numeric: `if (daily_count > 75)`. **Use** `if (daily_count > thresholds.numbers.dailyCapDefault)`.
- ❌ Hardcoded permission string: `if (user.role === 'manager')`. **Use** `if (hasPermission(user, PermissionKey.MONITORING_BARGE, scope))`.
- ❌ Hardcoded API URL: `axios.get('/api/calls/' + id)`. **Use** `apiClient.calls.get(id)` (which routes through `shared/contracts/endpoints.ts`).
- ❌ Hardcoded event name: `track('dial_initiated')`. **Use** `track(Events.dialInitiated, payload)`.
- ❌ Hardcoded compliance reason string: `audit.write({ reason: 'calling hours violation' })`. **Use** `audit.write({ reason: ComplianceReason.CALLING_HOURS_VIOLATION })`.
- ❌ Hardcoded role name in test selector: `data-testid="manager-only-button"`. **Use** stable role-agnostic selector + RBAC test asserts visibility.
- ❌ Inline regex for normalization. **Use** the central normalizer (`shared/normalize/phone.ts`, `tz.ts`, `email.ts`).
- ❌ Pre-normalizing raw input before its domain resolver sees it. **Use** the registered composite
  resolver at the route/leaf: a bare normalizer can erase domain-significant intent before the
  classifier runs (`911`/`933` became generic validation when `calls.ts` normalized first).
- ❌ DTO shape redefined in two places. **Use** the single shared contract.
- ❌ Tailwind config diverging from `foundations.ts`. **Generate** Tailwind from foundations; never hand-edit Tailwind tokens.
- ❌ **Any raw design literal at the leaf** — `oklch(…)` / `rgb(…)` / a hex, a raw `16px` / `1.5rem`, a hand-written `box-shadow` / `cubic-bezier(…)`, a raw `linear-gradient(…)`, a font-name, a z-index number — anywhere under `frontend/src` **except** the token source (`design-system/tokens/*.css`). **Use** the semantic token (`var(--…)` or `foundations.*`). **If no token fits, add it at the source first** — the right primitive ramp / scale step → a semantic token — then reference it. Never inline a one-off "just this once." The `check:ui-guardrails` gate **fails the build** on this (mirrors the user-level rule "Tokens & scales — extend the source, never inline a raw value at the leaf").
- ❌ **A page that owns scroll/height** — `height: 100vh/svh/dvh`, `overflow: auto/scroll`, or `position: fixed` for layout in page/feature code. **Use** `AppShell` + `ScrollArea` — the shell owns the one scroll container (`docs/design-system/layout-and-ia.md` §1). The `check:layout` gate flags this.
- ❌ **An ad-hoc hand-rolled page header** — a `*__header` / `*__topbar` rule inventing its own surface treatment (a fill, a border recipe, its own blur) instead of referencing the one source. **Use** the ONE shared `.app-header` surface (`frontend/src/design-system/app-header.css` — a FROST PANE: transparent + backdrop-blur + sticky; the ambient flows through): the `PageHeader` primitive composes it, a rich page header co-applies `app-header` in JSX. Never re-declare a header surface per page, and never re-add an opaque fill to `.app-header` itself (the "solid bar" slab covered the ambient on every page; removed 2026-07-06 per user approval). The `check:header-surface` gate flags the ad-hoc class.
- ❌ **Info-dumping** — a wall of equal-weight fields in one card. **Use** the tiered model (Tier 1 visible / Tier 2 demoted / Tier 3 disclosed) + the card-vs-sections-vs-tabs-vs-disclosure rubric (`docs/design-system/layout-and-ia.md` §2).
- ❌ **Per-page IA reinvention** — the same object (number / lead / call) laid out differently page-to-page, or ad-hoc tabs/sections. **Use** one shared composite (`<NumberDetail>`, `<LeadCard>`): same object → same structure everywhere.

## 3. How to add a new central registry

When a new domain emerges (e.g. we add "battlecard categories" as a concept):

1. Decide: where does the truth live? (`shared/taxonomy/`, `shared/contracts/`, `shared/copy/`, `frontend/src/design-system/`, etc.)
2. Add the registry file in that location with the canonical shape (enum / DTO / map / etc.).
3. Add a row to §1 above (this rule's table).
4. Add a row to `docs/app-plan/assurance/source-of-truth-map.md` §"Authority inventory".
5. Add a row to `docs/app-plan/product/27-glossary-taxonomy.md` if it's a user/agent-facing taxonomy.
6. Update the relevant Cursor / Codex / Claude rule files if the registry has additional discipline.
7. Update existing consumers to source from the new registry (do not leave duplicate truth in two places).
8. Add a regression test that catches a future "inline literal" attempt for this domain.

## 4. How to change a central registry value

Changing a row in a central registry is a high-blast-radius change. Procedure:

1. Read every consumer per `docs/ARCHITECTURE_BLAST_RADIUS.md`.
2. Update the registry row.
3. Update consumers that derive UX/behavior from the value (e.g. a default daily cap change requires updating tenant onboarding seed + tests).
4. Run `npm run gates:all` — must pass.
5. Update the journey doc if the change taught a lesson about why the registry mattered.

## 5. CI gates (when scaffolds exist)

- `npm run gate:doc-code-drift` checks: every authority surface in `assurance/source-of-truth-map.md` resolves to a real file; every claimed central registry path exists; every glossary ID resolves; living-doc links + the Companion/rail/CMP-012 stale-authority echoes fail closed (`validateLivingDocumentation`); AND (added 2026-07-21, `validateDeferredEndpointDocLiveness`) **deferred-endpoint doc liveness** — for every endpoint key marked `deferred` in `check-backend-endpoint-parity.mjs`'s `BACKEND_DELIVERY_CLASSIFICATION` (backend code retained as a seam, runtime unmounted, cited `deferredBy`), a scanned describing-doc (`docs/app-plan/data-and-api/**`, `docs/app-plan/assurance/**`, `docs/design-system/handoffs/**`, `docs/app-plan/auditability/decision-log.md`) that NAMES that registry key must carry, in the same table-row/list-item/paragraph scope, the `DEFERRED` keyword + the row's OWN `deferredBy` decision-ID — or make no `Built`/`live`/`current`/`PENDING_WIRING`/`Slice-N` liveness claim. The key list + accepted qualifier IDs are DERIVED from the classification (never retyped); negation/history-tolerant; fenced code skipped; escape hatch `endpoint-doc-liveness-ok: <reason>`. This is the reverse-drift sibling of the parity gate (which only proves the ROUTE is unmounted): it stops the stale-premise-after-reversal class (the B06 embed-only unwind, INT-004/#256, left `09-api-and-integration-contracts.md`, the assurance cluster, and `HANDOFF-M07-Booking.md` describing native booking as "Built"/"PENDING_WIRING Slice-9" until an auditor found them). **Residual (owned by the doctrine-drift + functionality-parity LENSES):** KEY-anchored only — a doc naming the endpoint solely by its contract-dotted name (`booking.availability`), its URL path, or in key-less prose is not reachable by it (the registry key is the only mechanically-derivable identifier). Meta-test: `backend/src/__tests__/endpoint-doc-liveness-gate.test.ts`.
- `npm run gate:doc-registry-refs` (`scripts/check-doc-registry-refs.mjs`, in `gates:all`; added 2026-07-07) checks the doc-prose SIBLING of the above: a LIVING authority doc (`docs/**`, `frontend/docs/**`, `.claude/rules/**` — the dated `docs/agent-prompts/` brief archive is excluded) must not name a `COPY.<surface>.<path>` key that no longer resolves. It AST-builds the real `COPY` key tree from `shared/src/copy/*` (index aggregation → per-surface consts, incl. shorthand; no build needed) and fails on any unresolved `COPY.*` reference — the class `gate:doc-code-drift` misses (it resolves registry PATHS, not the KEYS a doc cites). Origin: PR #192 shipped `FRONTEND_BLAST_RADIUS.md` still citing the deleted `COPY.workspace.theme`, caught only by the adversarial reviewer. History-tolerant, scoped to the ref's own CLAUSE (a removal verb in a different table-cell/sentence/parenthetical can't shadow a live ref sharing a dense line): a ref whose clause carries a removal/past-tense word (removed/deleted/renamed/…) or a line carrying a `doc-registry-ok` marker is skipped; a spread-contributed key is never false-flagged (OPEN guard). Scope is deliberately `COPY.*` only (deterministic; fuzzy taxonomy-const-name prose is NOT scanned — it false-positives). Meta-test: `backend/src/__tests__/doc-registry-refs-gate.test.ts`.
- `npm run gate:endpoint-wiring` (`scripts/check-endpoint-wiring.mjs`, in `gates:all`; added 2026-07-08, NFR-012) checks the ARC-008 endpoint registry's DELIVERY side: every internal (non-`external`, non-`/api/webhooks/`) key in `shared/src/contracts/endpoints.ts` — registry entries AND `PATH_SUFFIXES` fragments — has a symbol-resolved shared `API_ENDPOINTS.<key>` reference under `frontend/src`, or an explicit `PENDING_WIRING` row with `{ reason, owner, addedAt, decision }` (`addedAt` is a real ISO `YYYY-MM-DD` date). Fails on silently-unwired endpoints, stale/dead/invalid deferrals, unsupported registry AST members that could hide keys, and backend route mounts that statically reconstruct a full canonical path instead of mounting its exact key. Comments, strings, tests, backend-only refs, and locally shadowed `API_ENDPOINTS` objects cannot certify wiring. Deeper parity — an api.ts method no page calls, DTO-field production↔consumption, nav/role reachability, or dynamic helper-built mounts — is the `functionality-parity-auditor`'s lens; `PENDING_WIRING` rows are dated deferrals that auditor re-triages, never exemptions. Meta-test: `backend/src/__tests__/endpoint-wiring-gate.test.ts`.
- `npm run gate:page-reachability` (`scripts/check-page-reachability.mjs`, in `gates:all` + the PostToolUse hook for `frontend/src/pages/**` + the two route authorities; added 2026-07-29, PAGE-UNREACHABLE-SURFACE-001) is the **PAGE-level twin of `gate:endpoint-wiring`'s endpoint-level floor**: a page component can exist, typecheck, pass its own unit test, and have no URL, no nav entry, and no way for any user to open it — forever. Every other control is blind by construction (`gate:endpoint-wiring` stops at the api layer; `check:ui-continuity` runs the OPPOSITE direction, registry→router→nav, so a file in NEITHER is outside its question; a page's unit test renders the component directly; `tsc` does not consider an unimported module an error). **Reachability has TWO authorities and both count:** `PAGE_REGISTRY` (`frontend/src/app/pageRegistry.tsx` — workspace surfaces) **and** a direct `<Route>` in `frontend/src/App.tsx` (the off-shell focused surfaces — auth, `/change-password`, `/ui`, `/companion`, `/onboarding/buy`, `/oauth/consent`, each DELIBERATELY absent from the registry so it gets no rail item). Treating registry-absence alone as unreachable would flag 8 correct surfaces on day one — a gate that cries wolf gets disabled. **Two rules:** every `*Page.tsx` under `frontend/src/pages/**` must be mounted (the repo's own naming contract — 17/17 routed surfaces carry the suffix, 0 composed sub-components do, so `CallCard`/`MembersTab`/`ContextPane`/`*Section` are structurally outside the rule rather than on a list that rots), and every IMMEDIATE child directory of `pages/` holding a non-test `.tsx` must contribute ≥1 mounted file (the semantic backstop, since the naming rule is defeated by omitting "Page"; scoped to immediate children so `settings/sections/` is never independently required to route). Reachability is deliberately NOT transitive through imports — nearly everything under `pages/` is transitively imported by some routed page, so a transitive rule is green even for a genuinely orphaned surface. **Why AST + the type checker, not regex:** reachability is a cross-file symbol question and the imports are not uniform — `../pages/lists/LeadListsPage` is direct while `./pages/auth` is a barrel re-exporting five files, which no regex can expand; the gate resolves each JSX tag → symbol → through every import/export alias (`getAliasedSymbol`) → the declaring file, with compiler options read from `frontend/tsconfig.json` itself rather than a second hardcoded copy. **Fail-closed everywhere:** a missing route authority, a missing/empty page inventory, zero resolved mounts, or ANY unresolvable capitalized JSX tag exits 1 — the last for HONESTY, since off a broken resolver "PageX is unreachable" is a lie that sends a reader to fix an already-mounted page, so the gate reports the broken resolver and emits no reachability verdict at all (that arm caught the gate's own first bug: a `.d.ts` filter turned successful third-party resolution of `<Route>`/`<Navigate>` into 14 false RESOLVE failures). Deferrals are dated `PENDING_ROUTING` rows (`{ reason, owner, addedAt, decision }`) that fail STALE/DEAD/INVALID; one row defers both the page and its directory finding. Meta-test: `scripts/check-page-reachability.test.mjs` (14 per-suite `mkdtemp` + `--root` scaffolds — the durable pattern FRONTEND-GATE-FIXTURE-COLLISION-001 prescribes, so it cannot join that flake class).
- `npm run gate:media-object-key-roots` (`scripts/check-media-object-key-roots.mjs`, in `gates:all`; added 2026-07-17, Sprint 1.4 B04) checks that a media object-key ROOT literal is born only in `shared/src/config/mediaArtifacts.ts`. It **AST-parses** that registry plus `shared/src/taxonomy/sprint14Foundation.ts` (the `typescript` compiler API — the same house pattern as `gate:bullmq-job-ids` / `gate:s14-authority-boundaries` / `gate:doc-code-drift`), reads the roots FROM the registry (never re-typing them), and enforces every root whose `authority` is `enforced` — currently `disclosure`/`greetings`/`voicemail-drops`/`recordings`; B07 resolved the recordings adoption on 2026-07-18. Any future unenforced set + its named owner is printed on every run — no silent cap. The gate also reverse-checks the two living media-root authorities (`docs/ARCHITECTURE_BLAST_RADIUS.md` and this rule) so neither may describe a registry-enforced root as pending/not enforced. The scan is AST too: it walks string/template literal nodes, so comments are not nodes and a `//` inside a string cannot mask neighbouring literals. Test fixtures are exempt (a fixture key is an input to the real resolver, not a production key builder); escape hatch `media-key-root-ok: <reason>` on the violation's line or the contiguous comment block directly above its statement. **Fail-closed everywhere:** an unparseable registry/taxonomy, a value the parser cannot resolve, a purpose with no root row, a shared root with contradictory authority, an anonymous exemption, a missing media-root authority doc, stale pending prose for an enforced root, or zero enforced roots all exit 1 — a parser that cannot see a value must never assume it. **Why a gate and not a test:** a re-inlined *identical* literal is invisible to any runtime assertion — comparing a consumer's output to a registry-derived value is a tautology (both move together), verified by running that exact mutation against the unit test (exit 0, all green). The gate owns the source-text invariant; `media-artifact-ingestion-policy.test.ts` owns the runtime producer ⊑ guard invariant. Neither alone closes the class. **Why AST and not regex (its own origin bug, fixed before it ever ran in anger):** the first version regex-matched `uploadAllowed:\s*true` and derived enforcement from it; executed against a registry whose voicemail row took its flags from a spread, it printed OK (exit 0), silently dropped `voicemail-drops`, let a real re-inline pass unflagged, and its "no silent caps" line **misattributed** the drop to B07's ownership — a green line actively lying. Meta-test: `backend/src/__tests__/media-object-key-roots-gate.test.ts` (tmpdir-scaffolded per test-intent §4.2, gate invoked from its real repo path so its `typescript` import resolves, with a scope-boundary fixture for every skip/allow/parse condition per testing-guardrails §8).
- `npm run gate:rules-wiring` checks the agent-rules loader wiring (the single-source guard, added 2026-07-02): every `@.claude/rules/*.md` reference in `CLAUDE.md` resolves; every `.claude/rules/*.md` path in `AGENTS.md` resolves; every file in `.claude/rules/` is discoverable from `AGENTS.md` (Codex's only discovery surface); and the retired `.codex/rules/` mirror has not silently returned. Replaces mirror-sync vigilance with a mechanical gate.
- `npm run gate:test-intent` checks: every test's `Proves:` IDs resolve in the PRD or competitive benchmarks (which themselves are central registries).
- `npm run check:ui-guardrails` (run via `check:ui`) checks: no raw design literal — hex/oklch/rgb, px/rem, shadow, easing, gradient, font-name, z-index, duration — outside the token source (`frontend/src/design-system/tokens/*.css`); no Tailwind drift from `foundations.ts`; no raw `axios` import in a component.
- `npm run check:ui-source-of-truth` (run via `check:ui`) checks: no inline user-facing COPY in `frontend/src` components — JSX text, the `aria-label`/`placeholder`/`title`/`alt` attributes, multi-word phrase literals (helper messages / reject reasons / basis lines), and capitalized default-prop values must all be sourced from `shared/src/copy/*` (`{COPY.<surface>.<key>}`; `fill()` for interpolation). AST-based (TypeScript compiler API); escape hatch is a `copy-ok` line marker; test fixtures + the `/ui` demo Gallery are exempt. Single-word state enums / CSS selectors / paths / dev strings are not copy and never flag (the i18n-hard single-word-in-code limit is documented in the gate header).
- `npm run check:ui-testid` (run via `check:ui`) checks: every exported HAND-ROLLED primitive in `frontend/src/components/ui/*.tsx` forwards a consumer's `data-testid` to a root — it must spread `...rest` onto the root, extend a DOM `*HTMLAttributes` type, OR declare `'data-testid'?: string` + render `data-testid={…}` on the root. AST-based (TypeScript compiler API); escape hatch is a `testid-exempt` line marker; Toast + Gallery + test/stories files are exempt. Prevents the Badge/Alert class of bug (a silently-dropped testid is a dead test selector) — the class bit twice (6410612, 83e2020) before the gate.
- `npm run check:ui` runs the four UI gates together (`check:ui-continuity` + `check:ui-guardrails` + `check:ui-source-of-truth` + `check:ui-testid`). *(This section previously claimed an "ESLint custom rule (added Sprint 0.1)" enforced these — that rule never shipped; the discipline is the `.mjs` gates above. The inline-copy gate was the missing leg, wired 2026-06-13; the data-testid passthrough gate was added 2026-06-16.)*
- `npm run check:layout` runs four sibling gates: **`check:layout-scroll`** (no page/feature code owns scroll/height/fixed — the app shell owns it; WARN-only until pages migrate, then ERROR) + **`check:layout-page-frame`** (ERROR: no bare `.<name>-page` root re-centers into a `max-width` + `margin: … auto` column instead of filling the shell full-width — the width-constrained + nav-bleed bug that bit numbers/lists/settings, copied from their standalone mocks; exact bare-root match, so inner centerings don't false-trip; escape hatch `/* page-frame-ok: <reason> */`; meta-test `backend/src/__tests__/layout-page-frame-gate.test.ts`) + **`check:header-surface`** (ERROR: every page-level `*__header`/`*__topbar` class carries the shared `.app-header` surface — co-applied `app-header` in JSX or a `background`; escape hatch `/* header-surface-ok: <reason> */`) + **`check:responsive`** (WARN: un-adapted-layout / dimension-fiddling shapes in page code — un-adapted grid, fixed height on a layout container, raw `vh`, fixed w+h box, `truncate` without `min-w-0`; per-rule WARN→ERROR adoption; escape hatch `/* responsive-ok: <reason> */`; `docs/design-system/responsive-design.md` §6). (`docs/design-system/layout-and-ia.md` §5).
- `npm run check:call-drop-gate` (WARN, in `gates:all` + the PostToolUse hook for pages/components edits) checks: no raw `hangup(` call in `frontend/src/pages`/`components` whose enclosing function lacks a server-side cancel (`api.calls.cancel` / `teardownActiveCall` / `cancelActiveLegs` / `endActiveCall`) — the journey-L13 orphaned-server-leg shape (a local hangup never reaches the webhook bridge; ARC-006/TCPA). Escape hatch `// call-drop-gate-ok: <reason>`; full rule in `auxara-dialer-frontend-rules.md` §"Call teardown routes through the ONE shared gate".

## 6. Migration / adoption

When old code with hardcoded values is touched:

- Touching the file → required to migrate that file's hardcoded values to the central registry in the same PR.
- Adding a new feature in a file with existing hardcoded values → also migrate (do not perpetuate).
- Pure refactor PRs that only migrate hardcoded values to registry are welcomed.

## 7. The point

In CoachAi, the most-expensive class of refactor was "we have to update this copy in 23 places and we'll definitely miss one." Centralization prevents this from being possible. In the dialer — where role × permission × scope × tenant config × compliance gate × disposition × event × copy can multiply into thousands of cells — the centralization doctrine is not optional. It's the only way the architecture stays maintainable past 5 paying tenants.

A junior agent reading this rule should walk away with: "I don't write user-facing strings in components. I don't put hex colors in CSS. I don't compare role names with `===`. I look for the central registry first."
