# Last run — 2026-07-27T14:06:31+04:00

Read-only audit completed. The canonical checkout is actually `${PROJECT:control-plane}`; the automation spec still names the nonexistent `${PROJECT:control-plane}-legacy-path`. The canonical global Claude dispatcher now emits `claudeCli` coordination coverage, but both Nuvo `origin/main` and CoachAI ship the older runtime contract that declares it uninstrumented. Canonical/global exit-sentinel doctrine, CoachAI proof-profile and overlay-parity improvements, and the installed bootstrap skill are already synchronized. Nuvo's current checkout is stale (`fix/integration-suite-repairs` at 2026-07-21), while `origin/main` contains the coordination engine.

Clarified to the user on 2026-07-27T16:15:05+04:00: the first candidate is bookkeeping needed before the shared coordinator can truthfully say it observed all dispatch methods; the second is a stale folder name in the audit's own canonical specification. Neither is a customer-facing product outage or requires immediate manual action.

Recheck on 2026-07-27T16:17:25+04:00 corrected the first conclusion: Nuvo `origin/main` commit `774d76f` (#294) has the exact canonical coordination coverage blob `f06ff3e2`; its open worktree is merely stale. CoachAI current branch commit `44098b1` contains the preceding blob `a5958920`, while canonical commit `abcace5` adds Claude-CLI dispatcher instrumentation. The real propagation gap is CoachAI only.

CoachAI update completed 2026-07-27T16:23:17+04:00 through the canonical `coachai-shared-runtime` overlay mapping, reconciled from reviewed digest `7178d2a6`. It changed only `coordination/coverage.mjs` (adds the Claude CLI coverage path and removes its standing uninstrumented blocker) and the stale generated `lifecycle-controller.mjs` import. Selected overlay parity and diff integrity passed; canonical dispatcher regressions ORG-COORD-CLI-001 and -006 passed. CoachAI `proof:changed` remains blocked by unrelated unmapped paths, and the complete organization suite has four pre-existing overlay-parity failures caused by unrelated dirty agent files. No commit was made; those unrelated edits were preserved.

Follow-up completed 2026-07-27T16:40:30+04:00 in a clean CoachAI worktree from `origin/main`. A full canonical CoachAI overlay install was required because the runtime depends on the coordination engine, assurance schemas, lifecycle updates, and generated project files—not only the two initially identified files. The generated portable lock was refreshed via CoachAI's own parity command and captured back to canonical. This exposed an additional real gate gap: the generated `.agents/skills/**` surface was unmapped by `proof:changed`. Canonical CoachAI proof profiles now assign it to `organization-control`, with a regression assertion. Verification passed: canonical overlay check, CoachAI overlay parity, `npm run test:organization-control-plane` (26/26), and `npm run proof:changed` (organization gates + 26 regressions + 1,516 doc/code checks). Committed and pushed both synchronized `main` branches: `Nuvoralink/ai-organization` `f886d0788a3254330a865cc9941db2f9f44eb4b7`; `Nuvoralink/Nuvora-CoachAi` `a2222322ff9062e689867cb023cced28895678ee`. Remote refs and clean worktrees were verified.
