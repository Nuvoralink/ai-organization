# Retired skills

These skill directories were retired during the 2026-07-02 skills consolidation. Their content was folded into a surviving skill and is preserved here untouched for reference and rollback. The authoritative changelog is `${DEPENDENCY:skills-root|backslash}\CONSOLIDATION_CHANGELOG.md`.

## 2026-07-02 — Skills consolidation

| Operation | Source → Target | Why | How to restore |
|---|---|---|---|
| RETIRE | `${HOME|backslash}\.codex\skills\gpt-tasteskill` → `_retired\gpt-tasteskill` | Content folded into merged `taste-skill` (`name: design-taste-frontend`) — AIDA structure, 2-3-line hero iron rule, gapless bento, GSAP scroll paradigms, meta-label ban. Colliding trigger with the taste baseline removed from active selection. | `Move-Item "${HOME|backslash}\.codex\skills\_retired\gpt-tasteskill" "${HOME|backslash}\.codex\skills\gpt-tasteskill"` then `cmd /c mklink /J "${HOME|backslash}\.claude\skills\gpt-tasteskill" "${HOME|backslash}\.codex\skills\gpt-tasteskill"`. Then revert `taste-skill\SKILL.md` to its pre-merge form if a full rollback is wanted. |
| RETIRE | `${HOME|backslash}\.codex\skills\soft-skill` → `_retired\soft-skill` | Content folded into merged `taste-skill` — double-bezel nested architecture, button-in-button CTA, fluid island nav/hamburger morph, 3 vibe & texture archetypes. Perf guardrails were near-verbatim duplicates and dropped. | `Move-Item "${HOME|backslash}\.codex\skills\_retired\soft-skill" "${HOME|backslash}\.codex\skills\soft-skill"` then `cmd /c mklink /J "${HOME|backslash}\.claude\skills\soft-skill" "${HOME|backslash}\.codex\skills\soft-skill"`. Then revert `taste-skill\SKILL.md` if a full rollback is wanted. |
| RETIRE | `${HOME|backslash}\.codex\skills\ai-build-lessons-capture` → `_retired\ai-build-lessons-capture` | Content folded into merged `docs-rules-guardrail-promotion` — narrative-lesson trigger list, pattern checklist, style guide. Both skills triggered at the same workflow moment (updating AGENTS/Cursor rules + guardrails). | `Move-Item "${HOME|backslash}\.codex\skills\_retired\ai-build-lessons-capture" "${HOME|backslash}\.codex\skills\ai-build-lessons-capture"` then `cmd /c mklink /J "${HOME|backslash}\.claude\skills\ai-build-lessons-capture" "${HOME|backslash}\.codex\skills\ai-build-lessons-capture"`. Then revert `docs-rules-guardrail-promotion\SKILL.md` to its pre-merge form if a full rollback is wanted. |
