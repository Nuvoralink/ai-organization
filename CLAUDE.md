# AI Organization Control Plane — Claude entry point

Use `AGENTS.md` as the cross-tool repository router. This repository is the canonical, portable authority for managed Claude/Codex orchestration assets and project orchestration overlays.

Before work, read the task-relevant files selected by `AGENTS.md` and `control-plane.manifest.json`. Never import secret-bearing or application/product data. Installed home/project copies are generated; make structural changes here first and verify parity.
