# Micro-Interactions

Fixture — redeclares `duration.fast` with a different numeric value (150ms vs the motion-design source-of-truth of 120ms). Triggers VF-FIND-039.

## Hover behaviors

#### Buttons
- **Duration:** `duration.fast` 150ms in.
