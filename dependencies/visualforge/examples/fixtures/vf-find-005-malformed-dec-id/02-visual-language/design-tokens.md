# Design tokens — fixture for VF-FIND-005

This file deliberately contains a malformed Specforge-style DEC-ID that is NOT contextualized as a Specforge cross-cite. Expected: validator FAILs.

Token ramp owned by DEC-CATEGORY-001 below.

### DEC-CATEGORY-001 — Bad shape ID
Should be rejected because it doesn't match `DEC-NNN(.M)?`.
